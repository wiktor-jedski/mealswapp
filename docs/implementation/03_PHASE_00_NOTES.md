# Phase 00 Notes

## 2026-05-19

- Created the application layout exactly at the repository root: `frontend/`, `backend/`, `worker/`, `db/migrations/`, `deploy/`, and existing `logs/`.
- Kept existing documentation and scripts in place. No docs or script paths were moved.
- Added README placeholders instead of code stubs for task 1 so later scaffold tasks can create framework-specific files without having to undo placeholder source code.
- The repository guidelines mention `scripts/check.py`, but that file is absent. Validation for task 1 was limited to direct filesystem inspection until a check script is added in task 6.
- Scaffolded the frontend with Vite's Svelte plugin and Vitest because Bun runs `package.json` scripts directly and there is no project-specific Bun template in the spec.
- #TODO Added Google Fonts imports for Inter and Roboto Mono as the initial font entry point. This is acceptable for local scaffold validation, but production/offline work may need self-hosting or a preload strategy.
- Bun's native `bun test` runner does not apply Vite/Svelte transforms from `vite.config.ts`, so the bootstrap smoke test checks the Svelte source directly. The Vitest-compatible config and component testing dependencies are present for later frontend component work.
- Verified task 2 with `bun install`, `bun test`, `bun run build`, and a local `bun run dev --host 127.0.0.1 --port 5173` response check.
- Initialized the backend as a separate Go module at `backend/` with module path `mealswapp/backend`; the spec did not define an import path.
- Added only health/readiness and `/api/v1/health` route stubs for the backend scaffold. Full versioned routing and envelope conventions remain assigned to task 16.
- Go's default build cache was not writable in the sandbox, so backend validation used `GOCACHE=/home/wiktor/Work/mealswapp/backend/.go-cache`.
- Verified task 3 with `go test ./...`, `go run ./cmd/api`, and `GET /health` returning `{"data":{"status":"ok","environment":"local"}}`.
- Implemented the worker entry point under `backend/cmd/worker` rather than as a separate Go module in top-level `worker/`, so it can share backend config and be included by `backend/go test ./...`.
- Worker local mode exits after no-op Redis/job hook initialization by default. `WORKER_IDLE=true` keeps it running until signal cancellation.
- Verified task 4 with `go test ./...` and `go run ./cmd/worker`.
- Added Compose-managed local PostgreSQL and Redis wiring because Appendix A defines Docker Compose as the development environment. The existing `scripts/start-services.sh` now falls back to system services when Docker Compose is unavailable.
- Verified task 5 with `docker compose config`, `bash -n scripts/start-services.sh`, and a backend config test proving `DATABASE_URL` and `REDIS_URL` resolve from env.
- Added `scripts/check.py` as the repository's single local check command because AGENTS.md referenced it but it did not exist.
- Task 6 migration validation is currently a placeholder that verifies `db/migrations/` exists and validates ordered SQL filenames once migrations are added.
- Verified task 6 with `python scripts/check.py`.
- Task 7 adds a `serving_unit` field even though DESIGN-005 emphasizes physical state and per-100 storage. The task list explicitly requires serving units, so the domain model and database check constraint support `gram`, `milliliter`, `piece`, and `serving`.
- Food item micronutrients are stored as JSONB for task 7 to match DESIGN-005's supplemental micro values. Task 11 will add canonical vocabulary validation.
- Verified task 7 with `go test ./...`, `python scripts/check.py`, applying `0001_food_items.up.sql` to local Compose PostgreSQL, inspecting the 21 expected columns, applying `0001_food_items.down.sql`, and confirming `to_regclass('public.food_items')` returned null.
- Task 8 implements user-owned meals with a required `user_id`, but does not add a users foreign key yet because the user/account schema starts in later authentication/profile tasks.
- Added a minimal PostgreSQL `MealRepository` before the broad repository-interface task because task 8 specifically requires repository CRUD tests for meals with multiple ingredients.
- Verified task 8 with `MEALSWAPP_TEST_DATABASE_URL='postgres://mealswapp:mealswapp@localhost:5432/mealswapp?sslmode=disable' go test ./internal/repositories/postgres -run TestMealRepositoryCRUDWithMultipleIngredients -count=1 -v` and `python scripts/check.py`.
- Recipe aggregate calculation currently treats `gram` and `milliliter` quantities as direct per-100 basis values, and `piece`/`serving` quantities as `quantity * food_items.serving_size`. Task 12 can centralize and expand this conversion behavior.
- Verified task 9 with `MEALSWAPP_TEST_DATABASE_URL='postgres://mealswapp:mealswapp@localhost:5432/mealswapp?sslmode=disable' go test ./internal/repositories/postgres -run TestRecipeRepositoryCalculatesAndPersistsTotals -count=1 -v` and `python scripts/check.py`.
- Task 10 uses tag kinds `diet`, `allergen`, `functionality`, and `curation` from the task list. DESIGN-005 only names `category` and `functionality`, so `category` is not accepted in the Phase 01 taxonomy.
- Verified task 10 with `MEALSWAPP_TEST_DATABASE_URL='postgres://mealswapp:mealswapp@localhost:5432/mealswapp?sslmode=disable' go test ./internal/repositories/postgres -run TestTagRepositoryAttachRemoveAndFilterFoodItems -count=1 -v` and `python scripts/check.py`.
- Task 11 seeds a minimal canonical micronutrient vocabulary in the migration because its verification requires seeded vocabulary values. Task 15 can still add broader dev/test seed data.
- Verified task 11 with `MEALSWAPP_TEST_DATABASE_URL='postgres://mealswapp:mealswapp@localhost:5432/mealswapp?sslmode=disable' go test ./internal/repositories/postgres -run TestMicronutrientVocabularyRepositoryValidatesSeededValues -count=1 -v` and `python scripts/check.py`.
- Task 12 rounds conversions to three decimal places to match the numeric precision used by the Phase 01 PostgreSQL migrations.
- Verified task 12 with `go test ./...` and `python scripts/check.py`.
- Task 13 uses a 20% calorie tolerance for macro-derived calories. The spec requires tolerance checks but does not provide a number; this value is intentionally permissive for real-world food label rounding and can be tightened if product requirements specify it.
- Verified task 13 with `go test ./...` and `python scripts/check.py`.
- Task 14 adds minimal foundation tables for users, preferences, entitlements, saved data, audit logs, and import records. These are intentionally small contracts for repository coverage; later auth, subscription, saved-data, audit, and admin tasks can extend them with stricter business rules.
- Added a transaction-aware `Store` that constructs repositories over either a `pgxpool.Pool` or transaction. This was needed to verify rollback behavior through repository methods rather than direct SQL.
- Verified task 14 with `MEALSWAPP_TEST_DATABASE_URL='postgres://mealswapp:mealswapp@localhost:5432/mealswapp?sslmode=disable' go test ./internal/repositories/postgres -run TestRepositoryInterfacesCRUDAndTransactionRollback -count=1 -v` and `python scripts/check.py`.
- Task 15 uses fixed UUIDs for seed fixtures so tests and later features can refer to stable IDs without external services. The admin password hash is the placeholder string `dev-only` and must not be used for production authentication.
- Verified task 15 with `MEALSWAPP_TEST_DATABASE_URL='postgres://mealswapp:mealswapp@localhost:5432/mealswapp?sslmode=disable' go test ./internal/seed -run TestApplyIsIdempotentAndLoadsKnownFixtureIDs -count=1 -v`, running `DATABASE_URL='postgres://mealswapp:mealswapp@localhost:5432/mealswapp?sslmode=disable' go run ./cmd/seed` twice, and `python scripts/check.py`.
- Task 16 keeps root `/health` and `/ready` for load balancer compatibility while also registering versioned `/api/v1/health` and `/api/v1/ready`. Later route tasks can add domain controllers under the same route registry.
- Task 16 response envelopes now include `success` and request ID metadata. Later error-handling tasks can expand error categories without changing the top-level envelope shape.
- Verified task 16 with `go test ./...` and `python scripts/check.py`.
- Task 17 adds reusable validation helpers instead of route-specific schemas because no concrete request DTOs exist yet. The helpers cover JSON decoding, required strings, UUID path params, integer query params, pagination bounds, and typed field errors.
- Verified task 17 with handler-level validation envelope tests, `go test ./...`, and `python scripts/check.py`.
- Task 18 introduces typed app errors with stable categories/codes and maps unknown internal errors to a generic `Internal server error` message to avoid leaking implementation details. More specific business errors can reuse these constructors in later controllers.
- Verified task 18 with `go test ./internal/http/...` and `python scripts/check.py`.
- Task 19 sets conservative API security headers globally: CSP `default-src 'none'`, frame denial, no-sniff, no-referrer, restrictive permissions policy, JSON content type, and `Cache-Control: no-store`.
- Verified task 19 with middleware success/error tests, `go test ./internal/http/...`, and `python scripts/check.py`.
- Task 20 defaults local/test CORS to the Vite frontend origins `http://localhost:5173` and `http://127.0.0.1:5173`. Staging and production stay closed by default and must provide comma-separated deployed frontend domains through `CORS_ALLOWED_ORIGINS`.
- Verified task 20 with allowed-origin, unlisted-origin, allowed-preflight, rejected-preflight, env parsing tests, `go test ./internal/http/... ./internal/config`, and `python scripts/check.py`.
- Task 21 implements in-process route-prefix rate limiting with explicit defaults for auth login, search, admin, webhooks, and a general `/api/v1` fallback. This is intentionally test-friendly and can later be swapped for Redis-backed distributed limiting before multi-instance production deployment.
- Verified task 21 with 429 envelope/retry metadata tests, retry-window reset tests, most-specific rule selection, health endpoint exemption, `go test ./internal/http/...`, and `python scripts/check.py`.
- Task 22 adds CSRF validation hooks for configured cookie/session mutation routes and for any unsafe request carrying the session cookie. Stripe webhook routes are exempt at this layer so later signature verification can be authoritative for webhook authenticity.
- Verified task 22 with safe-method bypass, missing-token rejection, matching-token acceptance, mismatched-token rejection, session-cookie mutation protection, webhook exemption, `go test ./internal/http/...`, and `python scripts/check.py`.
- Task 23 implements sanitizer utilities in `backend/internal/security` for search terms, profile names, admin import fields, and generic text. Unsafe HTML/script-like input is rejected rather than escaped so persisted/searchable text remains plain text by default.
- Verified task 23 with trimming, rune-based length limit, unsafe HTML/script rejection, Unicode preservation, multiple-violation tests, `go test ./...`, and `python scripts/check.py`.
- Task 24 adds JSON request logging with request ID, method, path, route pattern, status, latency, IP, and sanitized error code/category metadata. Raw error messages are intentionally omitted from logs to avoid leaking secrets.
- Verified task 24 with structured success log capture, sanitized internal-error log capture, `go test ./internal/http/...`, and `python scripts/check.py`.
- Task 25 adds an in-process metrics collector and `/metrics` text endpoint for request totals, error totals, latency sum, readiness state, and dependency gauges. Readiness now accepts dependency-check hooks so later database/Redis probes can drive load-balancer readiness without changing the route contract.
- Verified task 25 with collector metric tests, readiness dependency-failure routing/metrics tests, `go test ./internal/http/... ./internal/observability`, and `python scripts/check.py`.
- Task 26 adds `consent_records`, a consent repository, and a `ConsentManager` that blocks missing or stale privacy, terms, and nutrition-disclaimer acceptance with stable `consent_missing` errors. Task 27 can wire this manager into the registration HTTP flow.
- Verified task 26 with consent-manager missing-consent, metadata persistence, current-version checks, repository integration coverage for environments with `MEALSWAPP_TEST_DATABASE_URL`, `go test ./...`, and `python scripts/check.py`.
- Task 27 adds AuthController route contracts for register, login, logout, refresh, and current-user behind an `AuthService` interface. Password hashing, JWT creation, and durable session storage remain delegated to tasks 28, 29, and 30 rather than being hard-coded into the controller.
- Verified task 27 with HTTP flow tests for successful register/login/current-user/refresh/logout, consent-missing registration failure, invalid login, missing current-user token, `go test ./internal/http/...`, and `python scripts/check.py`.
- Task 28 adds Argon2id password hashing in PHC format with strength validation and parameter rehash detection. Production defaults use stronger memory/iteration settings while unit tests use lower-cost parameters for speed.
- Verified task 28 with valid/invalid verification, weak-password rejection, malformed-hash rejection, rehash-detection tests, `go test ./internal/security/... ./...`, and `python scripts/check.py`.
- Task 29 adds an HMAC-SHA256 JWT manager with typed access/refresh claims, expiry validation, in-memory revocation, and refresh rotation that invalidates the previous refresh token. Durable token-family storage can be introduced with the session manager in Task 30.
- Verified task 29 with access validation, expiry rejection, malformed-claim rejection, tamper rejection, refresh rotation/revocation tests, `go test ./internal/security/token ./...`, and `python scripts/check.py`.
- Task 30 adds auth cookie management for access and refresh tokens. Local/test cookies use `SameSite=Lax` without `Secure`, while deployed environments use `HttpOnly`, `Secure`, `SameSite=Strict`, optional domain scoping, and logout clearing.
- Verified task 30 with local/production cookie flag tests, logout clearing tests, AuthController cookie integration checks, `go test ./internal/http/... ./...`, and `python scripts/check.py`.
- Task 31 adds account and IP login-failure lockout tracking with configurable thresholds/windows, reset-on-success, expiry behavior, and generic public failure wording to avoid user enumeration. This complements the gateway rate limiter rather than replacing it.
- Verified task 31 with account threshold, lockout expiry, success reset, IP window, IP expiry, generic public failure tests, `go test ./internal/security/lockout ./...`, and `python scripts/check.py`.
- Task 32 adds goth-backed OAuth abstractions, HTTP start/callback route hooks, `oauth_identities` storage, duplicate-email linking behavior, and trial-start hooks for first OAuth-created accounts. Concrete Google/Apple provider setup remains configuration work for deployment/auth integration.
- Verified task 32 with provider callback success, duplicate-email linking, provider failure handling, route compilation, `go test ./internal/services/oauth ./internal/http/... ./...`, and `python scripts/check.py`.
- Task 33 adds one-time password reset and email verification token services, account-flow HTTP route hooks, and `password_reset_tokens`/`email_verification_tokens` persistence schema with `users.email_verified`.
- Verified task 33 with token creation, expiry rejection, reuse rejection, password-hash update, email-verified update, route compilation, `go test ./internal/services/accountflow ./internal/http/... ./...`, and `python scripts/check.py`.
- Task 34 adds ProfileController read/update route contracts for display name, email state, dietary settings, and metadata behind a profile-service interface. Shared auth middleware can replace the controller-local bearer checks later without changing response envelopes.
- Verified task 34 with authenticated read/update tests, trimmed display-name validation, unauthorized rejection, forbidden service-error propagation, `go test ./internal/http/... ./...`, and `python scripts/check.py`.
- Task 35 adds PreferenceManager defaults, validation, and persistence updates for theme, default search mode, macro toggles, excluded tags, and dietary filters using the existing preference repository contract.
- Verified task 35 with missing-preference defaults, valid update persistence, invalid preference rejection, repository-error propagation, `go test ./internal/services/preferences ./...`, and `python scripts/check.py`.
- Task 36 extends saved-data repository support with user/kind listing and adds a SavedDataManager plus route contracts for favorites, saved searches, and search history. Search-history deduplication currently uses matching labels as the stable dedupe key.
- Verified task 36 with CRUD/ownership checks, descending ordering, search-history deduplication, invalid-kind validation, route compilation, `go test ./internal/services/saveddata ./internal/http/... ./...`, and `python scripts/check.py`.
- Task 37 adds a data exporter for profile, preferences, entitlements, saved data/history, and consent records. Exported profile data omits password hashes, and saved payloads recursively redact token/password-like keys.
- Verified task 37 with export-shape, owned-user source calls, sensitive hash/token redaction tests, `go test ./internal/services/exporter ./...`, and `python scripts/check.py`.
- Task 38 adds an account deletion coordinator that disables users first, revokes sessions, purges user cache hooks, deletes owned data, preserves an audit event, and then deletes the user record.
- Verified task 38 with deletion coordination, deleted-user login denial, owned-data removal, audit preservation tests, `go test ./internal/services/accountdeletion ./...`, and `python scripts/check.py`.
- Task 39 adds protein/carbs/fat-only macro vectors and unit normalization helpers for food, meal, and recipe similarity inputs. Meal vectors use per-100 scaling for gram/milliliter quantities and direct scaling for serving/piece quantities until richer serving metadata is needed.
- Verified task 39 with food, meal, recipe, normalization, zero-vector, invalid-vector tests, `go test ./internal/domain/similarity ./...`, and `python scripts/check.py`.
- Task 40 adds cosine similarity scoring for normalized macro vectors plus deterministic descending sort with UUID tie-breaking. Zero-magnitude targets are skipped while invalid non-finite/negative targets still fail.
- Verified task 40 with exact-match, orthogonal profile, deterministic ordering, zero-target skip, invalid-source tests, `go test ./internal/domain/similarity ./...`, and `python scripts/check.py`.
- Task 41 adds the 0.40 default similarity threshold and page metadata with a hard maximum page size of 10 before serialization.
- Verified task 41 with below-threshold exclusion, page-size cap, pagination metadata, empty-page tests, `go test ./internal/domain/similarity ./...`, and `python scripts/check.py`.
- Task 42 maps similarity scores to green, light-green, yellow, and red tiers at the documented 85%, 70%, and 55% boundaries with stable display labels.
- Verified task 42 with all boundary tiers, below-55% red mapping, label checks, `go test ./internal/domain/similarity ./...`, and `python scripts/check.py`.
- Task 43 embeds stable SVG indicator assets and serves them under `/static/similarity/{tier}.svg` with immutable cache headers. Indicator mapping now includes the asset URL so later search responses can expose it directly.
- Verified task 43 with asset URL checks, static SVG route/cache-header tests, `go test ./internal/domain/similarity ./internal/http/... ./...`, and `python scripts/check.py`.
- Task 44 adds query parsing for single, replacement, and diet modes plus implicit replacement/similarity mode when the search query is empty and two or more ingredients are present.
- Verified task 44 with single/replacement/diet parsing, empty-query implicit behavior, validation-failure tests, `go test ./internal/services/search ./...`, and `python scripts/check.py`.
- Task 45 ranks autocomplete candidates in memory by normalized exact match, Levenshtein distance, normalized label length, normalized label text, and item ID. Empty queries return no suggestions so the future endpoint can avoid unnecessary repository work.
- Verified task 45 with ranking-order, deterministic tie, casing/whitespace, limit/empty-input, and 5,001-candidate latency tests under the 100ms target, `go test ./internal/services/search -count=1 -v`.
- Task 46 adds a repository-neutral filter processor that translates tag include/exclude filters, dietary includes, allergen excludes, macro toggles, and source-provider filters into a stable search query shape. Source providers are carried in the query object for the upcoming SearchController/repository wiring because the current `FoodItemRepository.Search` contract does not yet expose provider filtering.
- Verified task 46 with filter processor tests for tag whitelist/blacklist, dietary/allergen expansion, deduplication, macro validation, source normalization, invalid shapes, and combined filters, `go test ./internal/services/search -count=1 -v`.
- Task 47 applies the documented functionality-tag multiplier (`similarityScore * (1 + 0.2 * tagMatchCount)`) into a separate `FinalScore` while preserving the original `SimilarityScore` for response metadata.
- Verified task 47 with shared-tag boost, no-match behavior, deterministic UUID tie-break, and duplicate-tag counting tests, `go test ./internal/services/search -count=1 -v`.
- Task 48 adds `/api/v1/search` and `/api/v1/autocomplete` route contracts behind a `SearchService` interface. The controller owns JSON/query validation, page-size capping at 10, filter translation, response-envelope defaults, and graceful empty arrays; concrete database/cache orchestration remains in the service layer for the upcoming Redis/degradation tasks.
- Verified task 48 with API tests for single search with filters/pagination, replacement search, diet search, autocomplete, empty autocomplete short-circuit, graceful empty search results, and invalid pagination/limit behavior, `go test ./internal/http/... -count=1`.
- Task 49 adds server-side Redis cache key primitives with `mealswapp:{schema}:{namespace}:{id}` rendering, SHA-256 hashed payload IDs, readable raw-ID keys for session/job/user use cases, namespace tags, and TTL policy for search, item, similarity, session, job, and user entries. Live Redis get/set/delete calls are left for invalidation/degradation tasks because this task's acceptance criteria are key stability and policy.
- Verified task 49 with key-stability, namespace separation, schema-version invalidation, TTL policy, raw-ID normalization, and tag tests, `go test ./internal/cache -count=1 -v`.
- Task 50 adds tagged cache invalidation for food, recipe, tag, and import changes. The current strategy deletes affected search/item/similarity entries by namespace/item/tag/user tags rather than refreshing in place, which avoids serving stale results before concrete Redis read-through caching is added.
- Verified task 50 with in-memory tagged-cache integration tests for item update invalidation, tag update invalidation, deterministic/deduplicated tags, and noop events, `go test ./internal/cache -count=1 -v`.
- Task 51 adds a search execution boundary for degradation behavior: cache errors fall back to repository results with a `cache_unavailable` warning, repository/database errors return structured retryable dependency errors, and similarity errors preserve text results with a `similarity_unavailable` warning.
- Verified task 51 with Redis-hit, Redis-failure fallback, database dependency-error, similarity-degraded, and empty-result normalization tests, `go test ./internal/services/search -count=1 -v`.
- Task 52 adds frontend theme primitives and a Svelte `ThemeProvider` shell that resolves `system` preferences from `prefers-color-scheme`, persists explicit user choices in localStorage, applies live CSS variables to the root element, and exposes a preference sync hook for the later typed API client.
- Verified task 52 with Bun tests for invalid/default preferences, system resolution, persistence, CSS variable updates, API-sync hook calls, and system-change behavior, plus `bun run check` and `bun run build`.
- Task 53 adds frontend API contracts for search, app errors, profile, entitlement, optimization jobs, and admin import/curation types. The API client unwraps backend envelopes, sends credentials by default, maps failed envelopes to typed `ApiClientError`, and exposes search/autocomplete/profile/theme-sync methods.
- Verified task 53 with mocked API success, failure-envelope, network-error, autocomplete query, and theme-preference sync tests, plus `bun test`, `bun run check`, and `bun run build`.
- Task 54 adds the main `SearchView` component and a separate search state controller for default single-item mode, all macro toggles enabled, query updates, 150ms debounce, request payload construction, loading, empty, success, offline, and typed error states. The controller is isolated so Bun tests can verify behavior without relying on a Svelte transform in the test runner.
- Verified task 54 with SearchView state tests for defaults, payloads, debounce timing, loading/empty/success/error transitions, plus `bun test`, `bun run check`, and `bun run build`.
- Task 55 extracts a responsive `SidebarComponent` with search-mode controls, macro toggles, dietary/allergen filter chips, saved searches/history/favorites entry points, and profile/settings actions. Sidebar behavior is backed by a small state helper so emitted mode/filter/macro changes and the 640px responsive breakpoint can be tested without a Svelte transform dependency.
- Verified task 55 with sidebar state tests for default control state, collapse toggling, mode/macro changes, dietary/allergen filter emission, responsive breakpoint behavior, plus `bun test`, `bun run check`, and `bun run build`.
- Task 56 adds an `AutocompleteDropdown` plus a controller for debounced API suggestions, deterministic ordered display, keyboard movement via ArrowUp/ArrowDown and Tab/Shift+Tab, Enter selection, Escape/blur dismissal, direct pointer selection, highlighted match segments, and API error display.
- Verified task 56 with autocomplete state tests for debounce/order, keyboard behavior, blur/direct selection, API errors, and highlight matching, plus `bun test`, `bun run check`, and `bun run build`.
- Task 57 adds a `ResultsGrid` with stable result cards, loading skeletons, empty/error states, similarity indicator images, macro/calorie/quantity display, category-based placeholder image URLs, pagination controls, and responsive 1/2/3-column helper behavior. Placeholder assets are referenced by stable paths and can be replaced with real artwork when static frontend asset work is scheduled.
- Verified task 57 with helper tests for pagination, placeholders, macro scaling, similarity/matching labels, responsive columns, plus `bun test`, `bun run check`, and `bun run build`.
- Task 58 adds a `SettingsPanel` for theme, unit, macro preferences, profile display, JSON/CSV export entry points, guarded account deletion, and in-app nutrition/consent disclaimer copy. Export/delete frontend client methods target the profile routes specified in DESIGN-008 even though concrete backend route wiring is scheduled later.
- Verified task 58 with settings state tests for preference edits, profile update payloads, delete confirmation, disclaimer/consent defaults, API client export/delete entry points, plus `bun test`, `bun run check`, and `bun run build`.
- Task 59 adds a schema-versioned `LocalStorageManager` for up to 20 cached search responses and five recent unique history labels, with deterministic request keys, LRU eviction, byte-size enforcement, schema mismatch purge, and TanStack Query hydration entries. `SearchView` now writes successful results and can reuse cached results for offline searches.
- Verified task 59 with local-storage tests for cached result reuse, LRU dedupe/eviction, history limits, schema purge, size-limit eviction, hydration entries, and search-controller offline cache reuse, plus `bun test`, `bun run check`, and `bun run build`.
- Task 60 adds frontend error classification, user-safe message mapping, retry decisions, degraded-feature state, an `ErrorMessage` component, and an app-level `ErrorBoundary` fallback shell. The boundary uses explicit fallback state for now; richer automatic render-error capture can be expanded when Svelte component-level runtime tests are introduced.
- Verified task 60 with tests for validation/auth/entitlement/rate-limit/offline/dependency/unknown message mapping, API/timeout/unknown classification, retry decisions, boundary recovery, degraded-feature tracking, plus `bun test`, `bun run check`, and `bun run build`.
- Task 61 uses source/component-level accessibility checks instead of Playwright because the repo does not yet have a browser E2E harness. The added checks cover WCAG AA contrast, keyboard focus order, mobile/tablet/desktop shell layouts, ARIA relationships, text wrapping, focus-visible outlines, and reduced-motion CSS guards.
- Verified task 61 with `bun test src/lib/accessibility/accessibilityChecks.test.ts`; full project validation follows before continuing Phase 06.
- Task 62 adds a repository-backed EntitlementManager service that normalizes missing users to free, active trial/paid users to unlimited access, and expired/past-due/cancelled records back to free-scope access while preserving the billing status for UI messaging. Because the current backend search modes are `single`, `replacement`, and `diet`, the four product feature flags are represented separately as `single`, `ingredient`, `meal`, and `diet`; later UsageLimiter/Search API wiring can map replacement/list requests onto those feature flags.
- Verified task 62 with entitlement manager unit tests covering anonymous, free limit/mode gating, active trial, active paid, expired subscription fallback, cancelled subscription fallback, expired trial fallback, paid plan lookup, and entitlement JSON shape.
- Task 63 adds an in-process UsageLimiter with 24-hour rolling windows, anonymous fallback, optional token-to-user resolution, free single-mode gating, and an optional search-route dependency that returns entitlement-category 402 errors for blocked modes or exhausted free quota. Usage is recorded after a syntactically valid and allowed search request is accepted but before downstream search execution; if product policy should count only successful searches, the limiter can move behind the search service result later.
- Verified task 63 with limiter unit tests for free counting, fourth-search blocking, 24-hour reset, blocked-mode no-count behavior, authenticated paid bypass, plus search API integration tests for entitlement error envelopes and allowed-search limiter calls.
- Task 64 adds authenticated `/api/v1/subscription/status`, `/subscription/checkout`, `/subscription/portal`, and `/subscription/entitlement` route contracts behind a `SubscriptionService` interface. Stripe remains stubbed at the service boundary for this task; webhook-side signature/idempotency behavior is still assigned to task 65.
- Verified task 64 with API tests for missing auth rejection, status and entitlement response shape, checkout-session creation, customer-portal link creation, and checkout payload validation.
- Task 65 adds Stripe-style `Stripe-Signature` HMAC SHA-256 verification, event ID/type parsing, idempotent processing, duplicate suppression, failed-dispatch recording, and `/api/v1/webhooks/stripe` routing. Processed-event storage is currently an interface with an in-memory implementation for tests/local wiring; a durable `processed_events`/dead-letter repository can replace it when webhook persistence migrations are scheduled.
- Verified task 65 with webhook processor tests for success, duplicate delivery, bad signature, invalid payload, failed dispatch persistence, plus HTTP tests for signed webhook success/duplicate response, bad-signature 400, and dispatch-failure 500 for Stripe retry behavior.
- Task 66 adds a TrialTracker that implements the OAuth `TrialStarter` hook, starts exactly 7-day social-login trials, rejects non-social trial starts, prevents duplicate trial records when a prior trial entitlement still exists, leaves active paid users unchanged, and downgrades elapsed active trials to free-scope/expired entitlement decisions. Bulk expiry currently takes candidate user IDs because the foundation entitlement repository does not expose a list-expired-trials query yet.
- Verified task 66 with tests for trial creation, social-login-only eligibility, duplicate prevention, active paid preservation, elapsed-trial expiry, active/non-trial no-op expiry, and OAuth package compatibility.
- Task 67 adds a manually runnable Reconciler service that lists local subscription records, queries Stripe through a stub-friendly interface, repairs active/past-due/cancelled/trialing drift, and writes reconciliation audit events. The service uses a `LocalSubscription` abstraction because the current foundation `entitlements` table does not yet store Stripe subscription/customer IDs; a later persistence task can back this interface with concrete columns or a subscription table.
- Verified task 67 with Stripe stub tests for past-due repair, cancelled repair, trialing-to-trial repair, matching active no-op behavior, local-only skip behavior, and audit event creation.
- Task 68 adds frontend entitlement view-state helpers, subscription API client methods, sidebar mode-lock rendering, upgrade prompts, free usage labels, and checkout-return refresh detection. SearchView refreshes entitlement on load/checkout return and passes gates into SidebarComponent; full usage counters still depend on the backend exposing current-window usage in a later API response.
- Verified task 68 with frontend tests for free locked paid modes, free limit lockout, trial/paid unlocks, billing recovery locks, fallback mode selection, checkout-return refresh detection, and subscription API client endpoints, plus `bun run check` and `bun run build`.
- Task 69 adds optimization job request validation, queued job creation, 202 API submission responses with poll URLs, job status lookup, and a worker helper that reserves one queued optimization job for processing. Queue persistence is interface-backed with an in-memory implementation for local/test behavior; production Redis wiring can replace the store without changing the controller or worker helper.
- Verified task 69 with queue manager tests for queued job persistence, invalid payload rejection, processing reservation, API tests for 202 response/job status/auth/validation, and worker tests for reserve-and-process plus empty-queue behavior.
- Task 70 adds LP variable and constraint types plus a ConstraintBuilder for protein/carbs/fat tolerance bands, per-item quantity bounds, excluded item upper bounds, and infeasible constraint detection. Quantity upper bounds default to unbounded when candidate metadata does not provide a maximum.
- Verified task 70 with unit tests for macro tolerance bands, excluded item constraints, bounded/unbounded quantities, no-variable infeasibility, and excluded-required-variable infeasibility.
- Task 71 adds an LP objective representation with `minimize` sense and coefficients based on `caloriesPerUnit + diversityPenalty`, plus helper evaluation/preference functions for deterministic tests. Diversity penalties are included but remain zero unless task 72 assigns them.
- Verified task 71 with objective coefficient tests, lower-calorie fixture preference checks, objective value calculation, and invalid coefficient rejection.
- Task 72 adds a diversity penalty pass that increments `DiversityPenalty` for candidate variables whose item IDs appear in the original diet, plus an overlap counter for solution validation/reporting. The default penalty is intentionally large enough to prefer a modestly higher-calorie alternative over returning the exact same meal when both meet macro constraints.
- Verified task 72 with tests for original-item penalty assignment, immutable input variables, objective ordering changed by penalties, and overlap counting that ignores zero-quantity variables.
- Task 73 adds the LP solver interface and a deterministic fixture solver that enumerates bounded integer quantities with context cancellation. This is the sandbox-friendly test mode required by the task; a production CLP/go-coinor adapter can implement the same interface later without changing constraint/objective callers.
- Verified task 73 with feasible fixture solving, deterministic optimal objective value, infeasible error handling, and context cancellation behavior.
- Task 74 adds solution validation that converts LP quantities into diet alternatives, rejects non-finite/negative quantities, rejects excluded or unknown item IDs, verifies protein/carbs/fat tolerance bands, calculates calories from candidate variables, sorts meal quantities for stable output, and rejects duplicate or over-limit alternative sets.
- Verified task 74 with tests for valid alternatives, invalid quantities, excluded items, macro tolerance misses, duplicate alternatives, and more than three alternatives.
- Task 75 adds a JobStatusTracker for queued/processing/completed/failed polling views, progress metadata, completed result payloads, 1-hour result TTL hiding, 30-second processing timeout, and failed timeout responses with partial alternatives preserved. TTL enforcement is currently applied at the status view layer over the queue store; a Redis-backed store can enforce key expiry physically later.
- Verified task 75 with status tracker tests for processing/completed transitions, result payload exposure, completed result expiry after TTL, timeout-to-failed transition with partial results, and recent processing no-op behavior.
- Task 76 adds frontend optimization API methods, an optimization polling controller, and a diet-mode panel that submits current diet results as the source meals, shows queued/processing progress, supports local cancellation, maps timeout failures to the documented user-facing message, and renders completed or partial alternatives. Cancellation currently stops client polling only because the backend has no cancel endpoint yet.
- Verified task 76 with frontend tests for 202 submission handling, polling state updates, completed alternatives, failed timeout partial results, and cancellation, plus `bun run check` and `bun run build`.
- Task 77 adds an admin route boundary at `/api/v1/admin/summary`, a reusable `RequireAdminMiddleware` that validates the current access token through the existing auth service and stores `AdminContext`, plus a summary service interface for later curation/user/audit counts. Non-admin users receive a generic 403 response and unauthenticated requests receive 401 without leaking role details.
- Verified task 77 with API tests for admin summary access, non-admin 403 envelopes, unauthenticated 401 envelopes, and `go test ./internal/http -count=1`.
- Task 79 adds an OpenFoodFacts client against `/cgi/search.pl` with the legacy query parameters OpenFoodFacts still documents for JSON search results. A `User-Agent` is set explicitly because public OpenFoodFacts usage policy expects identifiable clients.
- OpenFoodFacts nutriment keys are preserved as provider-native keys such as `proteins_100g` for task 79. Task 80 owns mapping those provider-specific nutrient names into canonical macros/micros and can decide how strict to be about package-vs-per-100 values.
- Verified task 79 with OpenFoodFacts fixture tests for query parameters, response parsing, timeout handling, rate-limit handling, server errors, malformed payloads, plus `go test ./internal/services/externaldata -count=1`.
- Task 80 treats USDA search nutrient values as already per-100 because USDA FoodData Central search payloads report the common nutrient fields on that basis, even when a serving size is present for display. OpenFoodFacts records use `_100g`/`_100ml` keys as per-100 and otherwise use positive serving size for conversion.
- Task 80 returns calorie mismatches as normalization warnings with the candidate preserved. Missing required macros still reject the candidate because downstream search/import behavior depends on a complete protein/carbohydrate/fat vector.
- Task 80 maps only active micronutrient vocabulary entries into canonical keys and emits `rejected_nutrient` warnings for recognizable micronutrients that are absent from the active vocabulary.
- Verified task 80 with USDA/OFF fixture tests, serving-based conversion tests, missing-image and rejected-nutrient warnings, calorie-mismatch warning behavior, required-field rejection, plus `go test ./internal/services/externaldata -count=1`.
- Task 81 exposes `GET /api/v1/admin/external-search` rather than a POST endpoint because the design frames external search as query/pagination parameters and the request is read-only. The endpoint accepts `query`, `provider`, `page`, and `pageSize`.
- Task 81 keeps provider fan-out in `ExternalSearchProxy` under `services/externaldata`; the HTTP handler only enforces admin access, parses query params, and maps provider errors into existing envelope categories.
- Task 81 treats `provider=all` as partial-success capable: one provider failure becomes a warning while available candidates from the other provider still return. A single-provider rate limit or outage propagates as the corresponding API error so the admin UI can display the provider-specific blocked state.
- Verified task 81 with proxy tests for selected-provider normalization, `all` partial success, rate-limit propagation, bad provider rejection, plus API tests for admin-only access, OFF search response shape, bad-provider mapping, and rate-limit mapping.
- Task 82 implements `DataImporter` as a service-layer boundary first. It converts normalized candidates into curated drafts, validates the edited draft with the food item domain rules, creates the food item, writes an `import_records` row with warning payload, and invalidates search/similarity cache tags.
- Task 82 marks imported curated items as `approved` because the importer represents the admin-confirmed import action. Later ItemCurator work can add explicit draft/approve/reject transitions for manually edited items.
- Task 82 duplicate detection checks both source provider/external ID and exact normalized name using the existing food repository search surface. A stronger repository method for exact source lookup would be useful before production-scale imports.
- Verified task 82 with importer tests for successful import, warning persistence, duplicate source/name conflicts, invalid draft rejection, curated draft defaults, and cache invalidation, plus `go test ./internal/services/externaldata -count=1`.
- Task 83 exposes admin item routes under `/api/v1/admin/items`: list, create, read, update, delete, and transition actions at `POST /admin/items/:id/:transition` for `approve`, `reject`, and `deactivate`.
- Task 83 extends food item repository updates to persist source metadata and `curation_state`; earlier updates only changed nutrition/display fields, which was not enough for admin curation.
- Task 83 keeps search visibility simple: repository search returns `draft` and `approved` items while hiding `rejected` and `inactive` items. This preserves admin draft visibility while preventing rejected/deactivated foods from appearing through normal search-backed reads.
- Verified task 83 with ItemCurator service tests for CRUD, edit macros/source metadata, transitions, invalid transition rejection, cache invalidation, and search visibility, plus admin API tests for item CRUD/validation/transitions.
- Task 84 adds admin tag routes under `/api/v1/admin/tags` and item tag assignment routes under `/api/v1/admin/items/:id/tags`. Tag merge is exposed as `POST /admin/tags/merge`; deactivation is `POST /admin/tags/:id/deactivate`.
- Task 84 adds `Deactivate` and `Merge` operations to the PostgreSQL tag repository. Merge copies item assignments to the target tag with `ON CONFLICT DO NOTHING`, removes source assignments, then deactivates the source tag.
- Task 84 keeps taxonomy validation aligned with the existing Phase 01 taxonomy: `diet`, `allergen`, `functionality`, and `curation`. The design text mentions category/functionality, but the current schema and task 10 intentionally use the four-kind taxonomy.
- Verified task 84 with TagManager service tests for upsert, assignment/removal, merge, deactivation, invalid taxonomy, invalid merge, cache invalidation, plus admin API tests for tag CRUD, assignment/removal, merge, deactivation, and invalid taxonomy rejection.
- Task 85 exposes admin user routes under `/api/v1/admin/users` for list, detail with entitlement summary, disable, reset-lockout, and per-user audit history. The detail route returns entitlement data when present and omits it when no entitlement row exists.
- Task 85 adds paged `UserRepository.List` and `AuditLogRepository.ListByTarget` methods to support the admin panel without querying tables directly from HTTP handlers.
- Task 85 adds `ResetAccount` to the in-memory lockout tracker. It clears account lockout state by normalized email and intentionally leaves IP lockout state untouched because an admin resetting one account should not clear a shared IP abuse window.
- Task 85 disables accounts by setting `users.disabled = true`. The repository/auth contract now has the durable disabled flag; concrete login denial remains enforced by auth service implementations that consult `UserEntity.Disabled` because this repo currently exposes auth as an interface-backed route contract rather than a concrete user-store login service.
- Verified task 85 with UserAdminPanel service tests for listing, entitlement detail, disable, lockout reset, audit history, admin API tests for user actions/admin-only denial, lockout tracker reset tests, and repository tests for user listing/audit history.
- Task 86 adds a reusable `AuditLogger` service over the existing `audit_logs` repository. It records actor, action, target, request ID, redacted metadata, and returns a timestamped view while PostgreSQL remains the source of truth for persisted `created_at`.
- Task 86 stores `requestId` inside the audit metadata because the current `audit_logs` table does not have a dedicated request ID column. A later migration can promote it to a first-class column if query patterns require it.
- Task 86 redacts metadata keys containing password, token, secret, credential, or hash recursively before persistence. This covers admin import/item/tag/user mutations, auth-sensitive events, subscription reconciliation, and account deletion events through a shared action string.
- Verified task 86 with audit logger tests for actor/action/target/request ID/timestamp view fields, recursive redaction, persisted metadata payloads, and the required audit action families.
- Task 87 adds the admin frontend behind a lightweight `/admin` path check in `App.svelte` instead of introducing a router dependency. This keeps the existing single-page shell intact while giving admin workflows a distinct first screen.
- Task 87 adds typed admin API client methods and an `adminState` controller for external search/import, item curation transitions, tag management, user admin actions, and audit history. The state helper follows the existing frontend testing pattern used by search, autocomplete, settings, and optimization.
- Task 87's admin import action uses the normalized external candidate payload directly to create an approved admin food item. If a later product flow needs a multi-step draft editor before import, the same controller can keep the selected candidate as an editable draft before calling `adminCreateItem`.
- Task 87 frontend API types accept both lower-camel JSON and current Go struct field JSON shapes for admin food/tag/user records because several backend admin responses currently serialize domain/repository structs without explicit JSON tags.
- Verified task 87 with admin API client endpoint tests, admin state tests for external import, item transitions, tag assignment/merge, user disable/lockout/audit, plus `bun test`, `bun run check`, and `bun run build`.
- Task 88 adds a production-only service worker registration from `main.ts` and serves the worker from `frontend/public/sw.js` so Vite copies it without a plugin dependency.
- Task 88 uses network-first caching for shell assets and cacheable API GETs, cache-first for indicator/placeholder/image requests, and adds `X-MealSwapp-Stale: true` when a cached response is served after network fallback. Existing localStorage search caching remains the primary offline search data path for POST search requests.
- Task 88 includes update handling through `SKIP_WAITING`, cache version cleanup on activate, and offline navigation fallback to the cached shell.
- Task 88 verification uses unit tests for service worker registration/cache policy and build-output checks for `dist/sw.js`; the repo still does not include a Playwright E2E harness, so browser offline rendering coverage remains deferred to task 99.
- Verified task 88 with `bun test`, `bun run check`, `bun run build`, and confirming `dist/sw.js` exists.
- Task 89 adds an `OfflineController` that subscribes to browser `online`/`offline` events, preserves the existing search controller state, queues one active search retry for reconnection, and tracks blocked offline mutations for the banner message.
- Task 89 wires `OfflineBanner` into `SearchView`. The diet optimization submit path is blocked while offline because it creates a server-side job; read-only search remains eligible for cached results and reconnect retry.
- Task 89 keeps retry/backoff timing out of scope for now because task 90 owns the dedicated `RetryManager`; the queued reconnect retry is immediate when the browser reports online.
- Task 89 verification uses controller/unit tests plus Svelte type/build checks. The repo still does not have a Playwright E2E harness, so browser-level connection toggling remains deferred to task 99.
- Verified task 89 with `bun test src/lib/offline/offlineState.test.ts src/lib/search/searchState.test.ts`, `bun run check`, and `bun run build`.
- Task 90 adds a reusable frontend `RetryManager` with exponential backoff, policy-controlled retryable categories, state notifications, and cancellation through a `RetryCancelledError`.
- Task 90 wires the search controller through the retry manager because search is the primary user-facing transient API flow in DESIGN-001/DESIGN-017. Other controllers can pass the same manager shape later without changing their public APIs.
- Task 90 keeps validation, auth, entitlement, and other business-rule failures non-retryable by relying on `AppError.retryable` plus the retryable category allowlist.
- Verified task 90 with retry manager tests for transient retries/backoff success, non-retryable validation/auth/entitlement failures, cancellation, existing error handling/search tests, plus `bun run check` and `bun run build`.
- Task 91 adds `UserCachePurger` helpers for logout and account deletion. They clear the local query/history cache, notify the active service worker to purge user cache buckets, delete `mealswapp-sw-*` Cache API buckets directly when available, and report partial purge errors without throwing.
- Task 91 wires account deletion in `SearchView` through the account-deletion purge helper. The logout purge helper is implemented with the same behavior for future logout UI integration; there is currently no frontend logout control to wire.
- Task 91 enforces Service Worker Cache API bounds with LRU-style trimming by insertion order: shell 20 entries, images 60 entries, and cacheable API GETs 30 entries. Existing `LocalStorageManager` already enforced 20 query entries, five history labels, and byte-size eviction.
- Verified task 91 with localStorage LRU tests, user cache purger tests for logout/deletion and partial failures, service worker cache-limit policy tests, plus `bun run check` and `bun run build`.
- Task 92 fixes admin accessibility gaps found in the source pass: active admin tab state now exposes `aria-current`, admin errors use `role="alert"`, external provider controls and tag-kind controls have labels, item action buttons include item-specific labels, user action buttons include user-specific labels, and admin table headers use `scope="col"`.
- Task 92 extends the accessibility source checks beyond the original search shell to cover offline banner announcements, optimization status, settings/account data labels, and admin workflow labels/table structure.
- Task 92 remains source/component-level because the repo still does not include Playwright flows; keyboard-only browser validation remains assigned to task 99.
- Verified task 92 with `bun test src/lib/accessibility/accessibilityChecks.test.ts`, `bun run check`, and `bun run build`.
- Task 93 adds a deterministic search HTTP handler P95 gate under `backend/internal/http` using in-memory dependencies. It proves the route/middleware/validation/envelope path stays below the 2-second requirement but does not replace staging load tests with PostgreSQL/Redis/provider-backed data.
- Task 93 keeps the existing autocomplete 100ms seeded-data latency test as the autocomplete gate and documents its exact command in `docs/implementation/04_PERFORMANCE_GATES.md`.
- Task 93 adds `scripts/performance_gates.py` and wires it into `scripts/check.py` after the frontend production build. Current production build sizes are about 33.6 KiB JS gzip, 3.1 KiB CSS gzip, and 36.9 KiB total HTML/CSS/JS gzip against budgets of 150 KiB, 30 KiB, and 200 KiB.
- Task 93 documents a 64 MiB client heap target, but leaves it manual/staging-only until task 99 adds browser automation or a CDP harness.
- Verified task 93 with `go test ./internal/http -run TestSearchControllerP95LatencyGate -count=1`, `go test ./internal/services/search -run TestRankAutocompleteLatencyOnSeededData -count=1`, `bun run build`, and `python scripts/performance_gates.py`.
- Task 94 adds a secret-free monitoring manifest at `deploy/monitoring/alerts.json` covering `/health` and `/ready` uptime checks, API P95 latency warning/critical alerts, 5xx error-rate alerting, database and Redis dependency health, optimization queue failures, and Stripe webhook failures.
- Task 94 adds `scripts/validate_monitoring.py` and wires it into `scripts/check.py` so monitoring config can be validated locally/staging without production notification channels. Notification targets are represented by `secretRef` placeholders.
- Task 94 keeps provider-specific GCP Cloud Monitoring policy IDs/resource names out of the manifest for now; task 98 can translate this contract into concrete staging/production infrastructure.
- Verified task 94 with `python scripts/validate_monitoring.py`.
- Task 95 adds a retention policy service under `backend/internal/services/retention` with explicit rules for sessions, search history, exports, deleted accounts, audit logs, import records, optimization jobs, and 30-day backup retention validation.
- Task 95 policy choices: sessions 30 minutes, search history 365 days, generated exports 7 days, deleted-account production cleanup immediate, audit logs 7 years, import records 365 days, optimization job results 1 hour, backups 30 days.
- Task 95 uses a `DeleteBefore(dataClass, cutoff, dryRun)` store interface because several concrete stores are still interface-backed or Redis-backed. The policy and dry-run cutoffs are tested now; PostgreSQL/Redis maintenance jobs can implement the interface without changing policy rules.
- Verified task 95 with `go test ./internal/services/retention -count=1`.
- Task 96 adds a Cloud SQL backup policy contract at `deploy/cloudsql/backup-policy.json`: automated daily backups at 03:00 UTC, 30-day/30-backup retention, point-in-time recovery enabled, 7-day transaction log retention, and monthly restore rehearsals to staging or local.
- Task 96 documents the restore rehearsal checklist in `docs/implementation/07_BACKUP_RESTORE.md`, including backup ID, restore timing, migration check result, readiness result, and failed-restore evidence retention.
- Task 96 adds `scripts/validate_backup_policy.py` and wires it into `scripts/check.py`. Concrete GCP resource creation remains part of task 98 deployment architecture.
- Verified task 96 with `python scripts/validate_backup_policy.py`.
- Task 97 adds production TLS enforcement middleware. Local/test environments remain exempt; deployed environments redirect HTTP to HTTPS and set HSTS when `X-Forwarded-Proto: https` or equivalent indicates secure traffic behind a proxy.
- Task 97 keeps secure-cookie behavior in the existing session manager: non-local/non-test environments use `Secure`, `HttpOnly`, and `SameSite=Strict`; local/test remains `SameSite=Lax` without `Secure` for developer ergonomics.
- Task 97 adds a Secret Manager-compatible resolver abstraction in `backend/internal/config`: raw env values are used locally, while `projects/.../secrets/.../versions/...` resources resolve through an injected secret manager client.
- Task 97 adds an AES-256-GCM envelope encryption helper under `backend/internal/security/encryption` with key version, nonce, ciphertext, and algorithm metadata. Repository-level PII field wiring is not yet applied because the current persistence layer stores several records through broad repository structs; future sensitive-field migrations can use the helper directly.
- Verified task 97 with `go test ./internal/http/middleware ./internal/security/encryption ./internal/config -count=1`.
- Task 98 adds Docker build contracts for backend API/worker/seed and frontend static assets, plus Nginx static serving config for local image validation/CDN parity.
- Task 98 adds `deploy/gcp/services.json` covering Cloud Run API and worker services, Cloud SQL PostgreSQL 16, Memorystore Redis 7, Cloud Storage/CDN frontend hosting, Artifact Registry, and required Secret Manager names. Secret values and concrete project IDs remain GitHub/GCP environment configuration, not repository content.
- Task 98 adds a manual GitHub Actions deploy workflow using Workload Identity Federation, full repository checks, backend image build/push, Cloud Run deploys, frontend build, and Cloud Storage sync.
- Task 98 updates the existing checks workflow to use `go-version-file: backend/go.mod` so CI follows the repo Go version instead of a stale hard-coded version.
- Task 98 adds `scripts/validate_deployment.py` and wires it into `scripts/check.py` to validate required deployment files, secret references, health/readiness gates, service names, and workflow commands without production credentials.
- Verified task 98 with `python scripts/validate_deployment.py`.
- Task 99 adds Playwright E2E coverage under `frontend/e2e` with deterministic network fixtures for registration/login API contracts, basic search rendering, paid-mode gating for replacement/diet, saved/history/favorites/settings/account deletion controls, and admin external import.
- Task 99 wires `bun run e2e` and `bun run e2e:list` into frontend scripts and adds a CI step that installs Chromium before running Playwright.
- Task 99 surfaced two browser-only defects that are now fixed: Svelte 5 app bootstrap now uses `mount(App, ...)`, and the default API client fetcher is bound to `globalThis` so native browser `fetch` does not throw before sending requests.
- Task 99 currently exercises registration/login as API contracts with deterministic fixtures because the frontend still has no dedicated auth form view. Export is covered as reachable settings UI; the export button callback is not yet wired to the API in the current UI.
- Verified task 99 with `bun run e2e:list`, `bun run check`, and `bun run e2e` after installing Chromium locally with `bunx playwright install chromium`.
- Task 100 adds `docs/implementation/09_PHASE_ACCEPTANCE.md` with automated evidence, staging/manual acceptance checks, accepted coverage exceptions, and current integration risk summary.
- Task 100 explicitly classifies the current state as staging-ready pending real external-provider, Stripe, GCP, backup-restore, and deeper browser workflow validation.
