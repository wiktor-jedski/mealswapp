# Open Points

## Phase XX - Template - do not edit, add next after this one

Note: not all fields need to be filled. Don't add unnecessary information.

### Assumptions

- Write here anything that has been assumed about the implementation AND is missing in the design.

### Clarifications

- Write here anything that needs to be clarified - insert here all of your questions.

### Actions needed

- Immediate problems that need to be solved and will block us in the future.

## Phase 00

No project-owner action is required for Phase 00 at this time.

### Assumptions

- The implementation task list at `docs/implementation/02_TASK_LIST.md` is the authoritative task list. The root `docs/02_TASK_LIST.md` file is a pointer to avoid duplicate task status sources. - resolved, removed pointer docs
- Phase 00 uses development-only local credentials from `.env.example` and `docker-compose.yml`; production secrets remain outside the repository. - OK, accepted
- OpenAPI type generation is documented as deferred until domain request and response contracts exist in later phases. - added to phase 02

### Testing coverage deviations

- Backend coverage is measured and reported by `python3 scripts/check.py` with `go test ./internal/... -coverprofile=coverage.out`; the aggregate check does not enforce a 100% threshold. Deviations are reviewed and recorded per phase instead of being hidden behind a pass/fail percentage gate.
- Backend `cmd/*` entrypoints remain covered by build/smoke verification rather than line coverage, because they are process bootstrap commands that bind ports, connect to local services, or run migrations.

## Phase 01

### Assumptions

- Resolved: add an optional `averageServingVolumeMilliliters` field for liquids because food-data sources provide milliliters per serving. Keep `averageUnitWeightGrams` for solid serving-to-gram conversion. Do not use grams as a 1:1 milliliter proxy.
- Resolved: require positive `densityGramsPerMilliliter` and `densitySourceKind` for liquids. Derive density from trusted USDA volume portions with gram weights when available, preferring `ml`, `cup`, `tbsp`, `tsp`, then `fl_oz`. Provider and source food ID remain optional for manual or estimated values. Do not silently assume `1 ml = 1 g`.
- Resolved: defer the dedicated saved-diet table until Phase 07 diet optimization. `saved_items.item_id` remains polymorphic for `favorite`, `saved_meal`, and future `saved_diet` rows. Until Phase 07 adds the target table, repositories must reject attempts to save the reserved `saved_diet` kind.
- Resolved: composite-meal ingredients remain limited to food-item references. Nested composite meals are intentionally unsupported, so recipe-cycle prevention is unnecessary.
- Accepted: `RecipeIngredientEntity` includes `position`, although `DESIGN-005` does not explicitly list it. SQL row order is undefined, so the field preserves deterministic ingredient display and editing order.
- Accepted: `RepositoryQuery` includes normalized-name, classification, preparation-time, pagination, and repository-context fields, although `DESIGN-005` does not explicitly list them. These fields are Phase 01 persistence-layer candidate-retrieval primitives for later search orchestration; they do not perform similarity ranking. Macro-range fields were intentionally removed because target-based similarity search accepts a concrete macro vector instead of speculative advanced filters.
- Accepted: OAuth login identities are stored separately from `AuthUser` password credentials in `oauth_identities`. `AuthUser.passwordHash` and `AuthUser.passwordSalt` are optional as a pair, and OAuth-only users do not store placeholder password credentials. This supports linking multiple authentication methods to one account.
- Resolved: add Dietary Presets in Phase 04 search as named bundles that produce Exclusion Rules. Keep Food Object classification based on Food Categories, Culinary Roles, and Allergens.
- Resolved: persist search history for authenticated users only after a completed search returns valid results. Keep duplicate searches, retain the latest 100 rows per user, expose clear-history behavior, and do not persist anonymous searches. Duplicate collapsing is deferred unless the UI becomes noisy.
- Resolved: keep rolling 24-hour usage counters PostgreSQL-backed for Phase 06. The timestamped usage rows and caller-supplied cutoff provide durable enforcement. Consider Redis only if measurements show entitlement checks are a bottleneck.
- Resolved: account deletion uses `pending -> processing -> completed|failed`, with `failed -> processing` permitted for retry and `completed` terminal. Store a sanitized transient, permanent, or unknown failure category. Retry transient failures automatically with exponential backoff up to 3 attempts. Require admin-triggered retry after investigation for permanent, unknown, or exhausted failures, and alert when requests fail or exhaust retries.
- Resolved provisionally: retain a minimal pseudonymous deletion receipt after account erasure for GDPR accountability. Store a random receipt ID, request and completion timestamps, final outcome, and sanitized failure category when applicable. Do not retain the deleted user ID, email, or account data. Use a provisional three-year retention period; pre-production legal review is tracked in `docs/implementation/01_PLAN.md`.
- Resolved: validate liquid macros as non-negative, but do not apply the solid `protein + carbohydrates + fat <= 100 g` rule to values stored per `100 ml`. Without density data, values above that threshold can be legitimate. Add external-import warnings for suspicious liquid values instead of rejecting them solely for exceeding `100 g`.
- Resolved: normalize mixed solid/liquid composite meals using required liquid density. Missing persisted density is invalid data and returns an error. Do not invent density conversions.

## Phase 02

### Assumptions

- Add dedicated security-audit persistence for request-correlated authentication, API error, CSRF, rate-limit, and future admin events. Keep it separate from the Phase 01 admin-audit table because the event shapes and fail-closed security-mutation behavior differ.
- Implement AES-256-GCM envelope encryption, key versions, key-loader interfaces, and a production GCP Secret Manager adapter boundary in Phase 02. Use explicit local test fixtures for development and tests. Defer wiring encryption into concrete PII repository fields until Phase 03 authentication and profile services define the plaintext service boundaries.
- Implement local structured JSON logging, in-memory metrics test sinks, emitted metric names, probe cadence, and alert-rule configuration in Phase 02. Defer deployed GCP Cloud Monitoring resources, notification channels, backup monitoring resources, and dashboards until Phase 09 production hardening.
- Resolved: Phase 02 rejects `MEALSWAPP_TRUST_PROXY=true` and ignores `X-Forwarded-Proto`. Phase 09 must deploy and verify restricted trusted ingress before forwarded-scheme handling can be implemented.
- Resolved: Phase 02 uses Fiber v2 CSRF middleware, binds tokens to Fiber sessions, and exposes `GET /api/v1/auth/csrf-token` for safe SPA token delivery. Every mutation route must explicitly choose CSRF protection or an exemption.
- Accepted: Phase 02 request deadlines are cooperative. Handlers and dependencies must honor context cancellation and propagate deadline errors. Non-cooperative handlers are defects.
- Resolved: encrypt `users.email`, `oauth_identities.email`, `oauth_identities.provider_user_id`, `user_profiles.display_name`, and persisted search-history query text at rest. Permit plaintext only at the narrow service boundaries that need it: authentication and account export/deletion for account email, OAuth linking and account export/deletion for OAuth identity fields, profile and account export for display name, and history and account export for search-history query text. Password hashes, password salts, token hashes, UUIDs, preferences, roles, and timestamps do not need an additional encryption envelope.
- Resolved: support normalized-email uniqueness and lookup with a deterministic keyed HMAC-SHA-256 digest stored alongside encrypted email. Use a dedicated, versioned lookup key that is separate from AES-256-GCM encryption keys and stored outside the database. Phase 03 must define lookup-key rotation and reindexing behavior before wiring encrypted account email persistence.
- Resolved: keep Fiber session storage in-process for local development and single-instance deployment. Before horizontally scaled deployment, wire the Fiber session store to the documented Redis session namespace so CSRF state is shared between API instances.
- Resolved: Phase 03 login, refresh, password-reset completion, and logout handlers must call `RegenerateAuthorizationState` or `InvalidateAuthorizationState` before returning success.

### Clarifications

- Resolved: Phase 03 treats verification as Login Method state and keeps `users.email_verified` as an account-level projection named `hasVerifiedLoginMethod` in `DESIGN-006`. Email-and-password methods require Mealswapp verification; External Login Identities rely on provider-asserted verification. Paid feature checks use the projection, which is true when at least one linked Login Method is verified.
- Resolved: Phase 04 treats Substitution Search as one operation with one or more Substitution Inputs in `DESIGN-001` and `DESIGN-002`. Adding input Food Objects refines the same Substitution Search. Multiple-input searches combine Food Quantities into one Macro Profile for Nutritional Similarity and do not apply per-input Culinary Role ordering. Contradictory filters and Exclusion Rule conflicts return user-facing `SearchRejection` feedback instead of failing silently.

## Phase 03

### Assumptions

- Accepted for planning: normalized-email uniqueness and lookup use a versioned keyed HMAC-SHA-256 digest stored alongside encrypted email. Phase 03 defines lookup-key metadata and a digest reindex command or repository method so rotation can add a new digest version and rebuild existing account lookup digests without decrypting or logging PII outside the service boundary.
- Accepted for planning: OAuth first-login trial activation is represented in Phase 03 as an explicit no-op entitlement hook because ARCH-007 subscription and trial persistence are implemented in Phase 06. Phase 06 must replace this hook with real trial creation and entitlement reconciliation.
- Accepted for Phase 03.1: account export returns an empty-but-typed `customItems` section until the repository gains a user-owned custom item schema. Current `food_items` rows are global and do not include a `user_id` owner predicate, so exporting them as account data would violate user scoping. Do not invent fake custom item rows in real account exports.
- Phase 03 email verification is limited to an authenticated, CSRF-protected verification hook that updates the server-derived user only. Production email delivery and signed email-verification tokens are deferred until the email provider integration is introduced.
- Accepted for Phase 03.1: keep the Phase 02 Fiber failed-login IP limiter as the Phase 03 IP-level brute-force protection. Do not add a separate persisted IP failure counter in Phase 03.1 unless the design changes; persisted account lockout remains required.
- Resolved for Phase 03.1: account deletion is implemented with account write lockout, transactional production deletion and receipt persistence where possible, sanitized failure classification, exponential retry scheduling up to three attempts, admin-retry state for permanent/unknown/exhausted failures, alert metadata, session invalidation, cache purge handling, and pseudonymous receipt constraints.
- Accepted for Phase 03.1 evidence: generate separate `docs/implementation/implemented/03.1_PHASE_UAT.md` and `docs/implementation/implemented/03.1_PHASE_REPORT.html` artifacts so the original Phase 03 UAT/report remain available.
- Phase 03.1 production bootstrap uses `MEALSWAPP_LOCAL_SECRET_KEY` for local JWT signing, PII encryption, and deterministic lookup digest key material. Development has a local fallback; production fails closed unless the variable is set. Replace this local loader with the documented Secret Manager-backed key loaders before deployment.
- Accepted: `localKeyLoader` uses a static `"local-v1"` version. Since it only holds a single active key, configuring a dynamic version via environment variables would not enable testing multi-version key rotation (as it cannot serve historical keys concurrently). Multi-version key rotation testing is deferred to unit test mocks or the production Secret Manager key loader integration.
- Resolved: `localKeyLoader.LookupKey` and `localKeyLoader.SigningKey` now forward the incoming context to the shared key lookup instead of replacing it with `context.Background()`.
- Resolved: `AuthController.Refresh` now logs a warning when best-effort authenticated-cookie clearing fails after refresh-token rejection, while preserving the original authentication error response.
- Resolved: `httpapi.Controller` now formalizes the `Routes() []RouteDefinition` contract, all Phase 03 HTTP controllers have compile-time guards for it, and `NewProduction` registers routes by flattening a typed controller slice.
- Resolved: `GenericInvalidCredentialMessage` is now a constant instead of a function, keeping the failed-login message reusable without exposing a function-shaped API.
- Resolved: deterministic password test-fixture hash and salt generation now live in `password_test.go` instead of production `password.go`.
- Resolved: `parseHashParams` now ranges over `strings.SplitSeq` instead of allocating a slice with `strings.Split`.
- Resolved: `ExportBundle` no longer carries the transport-level `format` field; the export format enum now lives at the API query-parameter/type boundary.
- Resolved: fallback disclaimer Markdown strings now live in named package-level constants.
- Resolved: database repositories, HTTP controllers, and local infrastructure adapters were audited for compile-time interface guards. Missing guards were added for concrete PostgreSQL repositories, security audit logging, observability sinks, local key loading, OAuth fail-closed gateway, and Redis cache purging. Cross-package service-boundary guards that would introduce import cycles remain intentionally omitted.
- Resolved: the pre-production `users` schema no longer includes plaintext `email` or generated `normalized_email` columns. User email uniqueness and lookup now rely on the encrypted-email metadata and `normalized_email_digest`, and `encrypted_user_create.sql` no longer writes a placeholder legacy email value.
- Resolved: all current database mutation SQL was audited for idempotency and duplication safety. Registration, saved items, consent, profile creation, deletion requests, OAuth identity linking, curated imports, classification/vocabulary upserts, food item creation, Stripe event recording, and usage windows rely on unique constraints, `ON CONFLICT`, or state-machine transitions. Profile/password/verification/session revocation updates are absolute or repeat-safe; password-reset token consume and refresh-token reuse intentionally reject replay; audit/security/history rows and login counters are intentionally append-only event records. Raw create primitives such as meal creation and future admin/custom-item creation must not be exposed through retryable REST flows without the cross-phase `Idempotency-Key` standard now recorded in `docs/implementation/01_PLAN.md`; Phase 06 checkout/webhook and Phase 08 admin/custom-item creation are called out explicitly.
- Phase 03.1 production bootstrap composes auth, OAuth, profile, saved-data, export, account-deletion, disclaimer, CSRF, and JWT routes from real repositories. OAuth routes fail closed until Google/Apple provider credentials and callback exchange are configured.
- Deferred to Phase 08: add an explicit user-owned custom item persistence model before relying on `customItems` in account export or account deletion. Until then, Phase 03 account export keeps `customItems` empty and typed.
- Deferred to Phase 09: add signed, single-use email-verification tokens and outbound email delivery before production paid-feature unlocks can rely on email-and-password verification.

### Security review notes

- `golang-security` review on 2026-06-03 found and fixed one verification authorization issue: `/api/v1/auth/verify-email` no longer trusts a client-supplied `userId`; it requires JWT cookies plus CSRF and marks only the authenticated user.
- Review confirmed account routes ignore identity headers, auth/profile/export/delete routes derive user scope from validated JWT/session state, refresh and reset tokens persist only hashes, PII fields cross plaintext boundaries only in auth/profile/export/delete services, and auth/CSRF cookies are HttpOnly with SameSite=Strict and Secure when TLS enforcement is enabled.

### Testing coverage deviations

- Phase 03 coverage deviation accepted on 2026-06-03: backend internal coverage is 90.3% after auth, OAuth, profile, export, deletion, and repository hardening. The uncovered lines are primarily defensive error branches, constructor defaults, and future-provider failure paths; Phase 04 should add targeted tests when those branches become active product behavior instead of inflating brittle tests solely to satisfy line coverage.

### Actions needed

No Phase 03 project-owner action is required at this time.

### Code Review Findings

No unresolved Phase 03 code review findings remain at this time.

## Phase 04

### Assumptions

- Accepted for planning: Dietary Presets are deterministic backend-owned named bundles that expand into Exclusion Rules at search time. They are not stored as Food Object classifications and should not create misleading Food Category or Culinary Role rows.
- Accepted for planning: Phase 04 supports the Daily Diet Alternative Search request shape at the search API boundary, but does not implement Phase 07 LP optimization jobs or saved-diet persistence. When required Phase 07 data is unavailable, the API returns a deterministic user-facing `SearchRejection` instead of creating worker/job side effects.
- Task 115 implementation assumes defensive request-boundary limits of 200 runes for search queries, 120 runes for autocomplete queries, and maximum page `10000`; project owner should adjust these before public launch if product search UX requires different bounds.
- Task 115 implementation validates substitution units as canonical API units only: `g`, `ml`, `oz`, and `fl_oz`. It also validates `dailyDietId` as UUID-shaped when present; required-vs-optional daily-diet semantics remain deferred to Task 124.
- Task 119 implementation uses schema versions `search-response-v1`, `autocomplete-response-v1`, and `similarity-calculation-v1`, with default TTLs of 5 minutes for search responses, 2 minutes for autocomplete responses, and 15 minutes for similarity calculations. DESIGN-011 requires schema versions and TTLs but does not specify concrete values.
- Task 117 implementation treats Dietary Preset rule IDs such as `dairy`, `gluten`, and `meat` as backend-owned Exclusion Rule names, not classification rows. Until a dedicated allergen persistence model exists, repository-backed allergen filters use existing classification association IDs as the only available persisted exclusion surface.
- Task 123 implementation keeps the current `SearchResponse` DTO stable: substitution results expose final ranking scores in `SimilarityScores`, while DESIGN-003 tier and image metadata remains verified through the internal `SimilarityResult` path until the public API response field is finalized.
- Task 138 OpenAPI extensions are now implemented: `api/openapi.yaml` `FoodObject` schema includes classification summaries (`id`, `name`, `kind`), an explicit primary Food Category, protein/carbohydrate/fat macros with `100g`/`100ml` basis, and non-negative server-calculated calories. Backend `foodObjectDTO`, `foodItemsData` mapper, and exported `search.CalculateCalories` populate the fields. Frontend generated types regenerated and drift check passes. Task 146 is unblocked.

### Testing coverage deviations

- Phase 04 coverage audit updated on 2026-06-18: backend internal line coverage is 99.2% using `go test ./internal/... -coverprofile=coverage.out`. `app`, `cache`, `compliance`, `config`, `database`, `migrations`, `observability`, `profile`, `repository`, `search`, `seed`, and `worker` are at 100%; `auth` is 99.2%, `httpapi` 97.0%, `security` 99.5%, and `userdata` 99.3%.
- `cmd/*` remains intentionally outside the coverage profile. Command packages are process bootstrap code that bind ports, connect to external services, or run migrations; they remain verified by build, migration, and API smoke checks.
- Remaining non-HTTP gaps are implementation-impossible error guards under current concrete types: JSON marshaling fixed internal DTOs, the standard-library hash writer returning an error, and the default password-hasher constructor rejecting its own compile-time-valid defaults. Adding injection solely to force these branches would increase production complexity without testing product behavior.
- Remaining `httpapi` gaps are primarily error returns from Fiber response serialization/cookie writes, observability fallback reporting, `runtime.Caller` failure, and middleware-preempted controller guards. Active authentication, OAuth, profile, export, deletion, user-data, search, cache, validation, repository-failure, and malformed-input paths are covered. These lines are deferred unless their collaborators gain practical injectable failure modes; they are not exempted from future targeted tests when behavior changes.

### Actions needed

- Task 138 OpenAPI extensions are now implemented: `api/openapi.yaml` `FoodObject` schema includes classification summaries (`id`, `name`, `kind`), an explicit primary Food Category, protein/carbohydrate/fat macros with `100g`/`100ml` basis, and non-negative server-calculated calories. Backend `foodObjectDTO`, `foodItemsData` mapper, and exported `search.CalculateCalories` populate the fields. Frontend generated types regenerated and drift check passes. Task 146 is unblocked.
- Remaining Task 138 description items (similarity presentation cleanup, deterministic ordering cleanup, naming cleanup, focused test hygiene) are intentionally split and not tracked as 04_OPEN.md action points.

### Code Review Findings

- No unresolved Phase 04 code review findings remain at this time.

## Phase 05

### Assumptions

- Accepted: Phase 05 uses TanStack Query for server state and localStorage for the SW-REQ-003 cache of the 20 most recent unique normalized request/result pairs. Cache recency is updated on reads and writes; malformed or schema-version-mismatched entries are discarded. Phase 09 remains responsible for service-worker API/image interception and broader offline hardening.
- Accepted: Phase 05 renders authenticated history and favorites in the Activity Sidebar from the existing Phase 03 generated contracts. Anonymous users receive empty/sign-in guidance, and activity API failure does not block public Catalog Search.
- Accepted: Task 139 implementation added pinned `@tanstack/svelte-query@6.1.34`, `@playwright/test@1.61.0`, and `@axe-core/playwright@4.11.3` to `frontend/package.json` with `preview` and `test:e2e` scripts. `bunfig.toml` scopes `bun test` to `src/` to keep Playwright specs under `tests/` out of the unit test runner. The `check` script intentionally excludes `test:e2e` to keep the unit/build/drift gate deterministic and not dependent on browser binaries.
- Accepted Project-owner decision: Phase 05 does not implement macro visibility toggles. Result cards always show the required protein, carbohydrate, and fat values from the generated search contract; SettingsPanel scope is limited to unit and theme preferences.
- Accepted Project-owner decision: Phase 05 keeps the Daily Diet Alternative UI as a scaffold only. Full Daily Diet Alternative execution, field-level rejection UX, saved-diet data, and optimization job behavior move to Phase 07.
- Accepted Project-owner decision: the sidebar theme control intentionally exposes only explicit light/dark switching. The underlying ThemeProvider still supports `system` for defaults, invalid-value fallback, and live system-theme resolution, but a visible `system` sidebar option is out of scope.
- Accepted: Task 142 implementation maps HTTP 429 to `server` category and 422 to `validation` category (code `search_rejected`) since the generated `ErrorCategory` enum has no `rate_limit`/`rejection` category. This stays within the generated contract.
- Accepted: Task 143 component tests use static-source assertions rather than DOM rendering because no DOM library (jsdom/happy-dom) is installed in the Bun environment. `vite build` validates Svelte source compilation. A future phase may add happy-dom + render tests for stronger behavioral coverage.
- Accepted: Task 145 implementation added `autocomplete-controller.ts` as a helper module (injectable timers/fetch) because Bun lacks Jest-style `useFakeTimers` for `setTimeout`. This enables deterministic 150ms debounce testing. Playwright autocomplete flows are `test.skip` scaffolds pending Task 151 wiring the dropdown into `SearchShell`.
- Accepted: Task 147 implementation uses `GET /api/v1/profile` to detect signed-in state (401 = anonymous, no error), `GET /api/v1/search-history` for history, and `GET /api/v1/saved-items?kind=favorite` for favorites. All fetches use `credentials: "include"` and inline try/catch error handling that never propagates to the parent so core search stays usable.
- Accepted: Task 148 implementation is SSR-safe and delegates staleness to Task 141's `LocalQueryCache.isStale`. `OfflineBanner` is not yet wired into `SearchShell` (Task 151 scope). Tests include explicit Phase 09 service-worker non-coverage disclaimers.
- Accepted: Task 149 implementation extends the Phase 00 `theme.ts` store (preserving the public API) with live `matchMedia` system-theme subscription, `cleanupTheme()` for listener teardown, and storage-unavailable try/catch fallback. `App.svelte` calls `initTheme()` at startup but not `cleanupTheme()` — Task 151 should confirm no double-subscription on route changes.

### Clarifications

- Resolved by project-owner decision: Task 138 extends each search-result item with server-derived classification summaries (`id`, `name`, and `kind`), an explicit primary Food Category, protein/carbohydrate/fat macros with a `100g` or `100ml` basis, and calories. OpenAPI and generated frontend types must expose these fields before Task 146 implements result cards and category placeholders.
- Resolved by project-owner decision: SW-REQ-006 multi-meal Daily Diet aggregation moves to Phase 07 alongside the saved-diet model and optimization worker. Phase 05 keeps only the Daily Diet Alternative scaffold and does not claim Daily Diet Alternative execution or field-level rejection UX compliance.
- Step B UI iteration adds `GET /api/v1/food-objects/{id}` as a small UX support endpoint so autocomplete-selected Substitution Inputs can hydrate into the same rich FoodObject card view as Catalog-added items. The endpoint reuses the existing FoodObject DTO/OpenAPI schema and does not change autocomplete ranking or substitution-search behavior.

### Testing coverage deviations

- Task 153 closed the remaining `.ts` line- and function-coverage gaps so `bun test --coverage` reports `All files | 100.00 | 100.00` (functions and lines). Targeted tests added: `AutocompleteController` dispose-while-in-flight abort, `currentQuery` getter, and default `setTimeout`/`clearTimeout` fallback arrows (`autocomplete-controller.ts`); `updateSubstitutionInput` no-match branch and `compareFilter`/`compareSubstitutionInput` equal-id comparators (`search.ts`); `readSystemTheme`/`ensureSystemThemeSubscription` `matchMedia`-unavailable fallbacks and the subscribe-exactly-once early return (`theme.ts`). Svelte `.svelte` components remain outside Bun's line-coverage report and are verified by static-source assertions plus Playwright e2e (75 passed, 1 scaffold skipped) and `vite build`.

### Accepted accessibility deviations (Task 152)

- Resolved: Task 152 decorative `color-contrast` deviations were fixed by adding theme-aware `--color-on-primary`, `--color-on-accent`, and `--color-on-muted` foreground tokens and applying them to result badges, category chips, placeholder text, and substitution action/filter chips. The accessibility gate now requires zero serious or critical axe violations without disabling `color-contrast`, and token contrast checks cover normal text plus on-token foreground/background pairs in light and dark themes.

### Actions needed

- Implemented in Phase 07: `frontend/src/lib/stores/search.ts` now defines `SearchState` as a discriminated union of Catalog, Substitution, Daily Diet, and Daily Diet Alternative states. Mode-only fields are owned by their corresponding variants, with typed projections and transition tests preventing impossible cross-mode combinations.
- Resolved: `frontend/src/lib/api/search-client.ts` `categoryForStatus()` now groups `400`, `404`, and `422` under the same `validation` branch, while `defaultCodeForStatus()` still distinguishes `404` as `not_found` and `422` as `search_rejected`. Regression coverage verifies the default 404 category and code.
- Resolved: handwritten frontend `.ts` files were scanned for missing TSDoc and design traceability. Added TSDoc/`@remarks Implements ...` comments to search-client envelope/error helpers, local-query cache schema validation, service-worker registration options, and unit display helper types. Generated files remain excluded; their traceability stays in sidecar documentation.
- Resolved: `frontend/src/lib/components/AutocompleteDropdown.svelte` `onOptionClick()` now accepts only the selected option index, and the click binding no longer passes an unused autocomplete item.
- Deferred to Phase 08: `frontend/src/lib/components/SubstitutionInputs.svelte` hardcodes allergen, dietary preset, and physical-state filter options in the component. This is acceptable as Phase 05 UI scaffolding, but it must be replaced before admin classification management or localization-sensitive filter UI. Phase 08 should introduce a backend-owned filter-option source, ideally exposed through an API such as `/api/v1/search/filter-options?mode=substitution`, backed by persisted classification/allergen vocabularies and backend-defined dietary-preset policy; keep the frontend responsible for rendering and merging selected-item classification options.
- Resolved: `frontend/src/lib/components/OfflineBanner.svelte` and `frontend/src/lib/stores/offline.ts` stale state is now reachable. `LocalQueryCache.peek()` preserves stale metadata, the search query function returns a stale cached response only when a browser-offline fetch fails, and `SearchResults.svelte` updates both `setShowingCached()` and `setShowingStale()` based on `LOCAL_CACHE_STALE_MS`.
- Resolved: `frontend/src/lib/components/SidebarComponent.svelte` mobile sidebar trigger is now an icon-only button with stable square dimensions and the accessible `aria-label="Open activity sidebar"` preserved.
- Resolved: `frontend/src/lib/components/AutocompleteDropdown.svelte` search input now uses Tailwind `truncate`, and `SearchShell.svelte` uses shorter mode-specific placeholder text for Substitution and Daily Diet Alternative so narrow mobile widths do not cut off awkwardly.
- Resolved: comment-only empty frontend `catch` branches in handwritten `.ts` and `.svelte` files were audited and replaced with explicit fallback statements. Storage/browser optional dependency failures now return, reset to defaults, or keep in-memory state; API and component catches either map errors to typed/user-visible state or intentionally keep fallback UI usable, such as anonymous activity guidance, inline history/favorites errors, in-memory caches, metric/default preferences, and label-only Substitution Input hydration.
- Resolved: handwritten frontend `.svelte` components now consistently use Svelte 5 runes for component props, mutable state, derived values, and effects. Legacy `export let` props and `$:` reactive declarations were removed, mutable component state was promoted to `$state(...)`, derived values to `$derived(...)`, side effects to `$effect(...)`, and event directives to Svelte 5 event attributes.
- Resolved: a full headless Neovim LSP diagnostic pass was run across handwritten frontend `.svelte` files using the Mason-installed `svelte` language server. The pass initially exposed editor-only TypeScript diagnostics that `vite build` tolerated: missing Vite env typing for `App.svelte`, widened substitution-unit literals in result/source summary components, and `SearchResults.svelte` store-bridge inference issues after the runes migration. These were fixed with `frontend/src/vite-env.d.ts`, explicit unit/query option typing, and simpler `$derived(...)` store bridges. The final LSP pass attached with root `/home/wiktor/Work/mealswapp/frontend` and returned zero diagnostics.
- Resolved: the `App.svelte` LSP type mismatch for `registerServiceWorker({ enabled: import.meta.env.PROD })` was fixed by adding `frontend/src/vite-env.d.ts` with the standard `vite/client` type reference. This gives the Svelte/TypeScript language server the Vite `ImportMetaEnv` declaration where `PROD` is boolean, without casting at the call site or changing JSON config.

## Phase 06

### Assumptions

- Accepted for planning: Phase 06 uses Stripe-hosted Checkout/session creation for subscription signup, as named in `docs/implementation/01_PLAN.md` and `SW-REQ-044`. This keeps raw card data outside the application server and UI.
- Accepted for planning: Stripe monthly and annual price IDs are environment-configured sandbox values that map to SW-REQ-050's $3.00 monthly and $25.00 annual plans. The repository must not commit real Stripe keys, product IDs tied to production accounts, customer data, or webhook secrets.
- Accepted for planning: Stripe webhook and reconciliation tests use signed sandbox fixtures, Stripe CLI forwarding, or injectable Stripe gateway fakes. Live Stripe network calls are local verification evidence, not a requirement for deterministic CI.
- Accepted for planning: checkout/session creation is treated as a non-idempotent business action and therefore requires the cross-phase `Idempotency-Key` standard. Stripe webhooks use provider event IDs as their idempotency keys.

### Actions needed

- Resolved on 2026-07-03: the product requirement now says Stripe-hosted Checkout rather than embedded Stripe Elements. Phase 06 billing remains aligned with the implementation plan's checkout/webhooks wording.
- Resolved for Task 173 repair on 2026-07-02: aggregate verification previously hit local Docker bind failures on PostgreSQL `5432` and Redis `6379` when those ports were already occupied by usable host services. `scripts/verify-local-stack.py`, `scripts/verify-phase02-uat.py`, and `scripts/verify-phase03-uat.py` now reuse existing local PostgreSQL and Redis listeners on those ports before attempting Docker Compose startup; `python3 scripts/check.py` completed successfully with that repaired path.
- Resolved on 2026-07-07: checkout creation now derives a Stripe provider idempotency key from authenticated user scope, checkout route, method, and client `Idempotency-Key`, passes it to Stripe's `/v1/checkout/sessions` request, and covers the ambiguous Stripe response retry case with focused checkout service and gateway tests.
- Resolved on 2026-07-07: Stripe reconciliation now maps `active`/`trialing` to local `paid:active`, recoverable blocked states `past_due`/`unpaid`/`incomplete`/`paused` to local `paid:past_due`, and terminal states `incomplete_expired`/`canceled`/`cancelled` to local `paid:cancelled`. Unknown statuses leave local state unchanged and emit a sanitized operator warning with Stripe subscription ID, customer ID, and provider status only. Focused reconciliation tests cover stale active repair and unknown-status observability.
- Resolved on 2026-07-07: Stripe webhook dead-letter persistence now stores bounded diagnostic codes in `error_message` instead of raw error text or one generic value. Context and repository failures map to allow-listed codes such as `context_deadline_exceeded`, `repository_connection`, `repository_retryable`, and `repository_internal`; unknown raw errors fall back to `stripe webhook processing failed`. Focused webhook tests verify useful diagnostics while preventing card, email, SQL, and secret text from being persisted.
- Resolved on 2026-07-07: Stripe webhook handling keeps the existing local entitlement model and explicitly maps entitlement-relevant billing events. `customer.subscription.paused` and `invoice.payment_action_required` now map to local `paid:past_due`; `customer.subscription.resumed`, `created`, and `updated` share the reconciliation subscription-status mapper for `active`/`trialing`, recoverable blocked, and terminal statuses. Recognized no-op billing events such as `customer.subscription.trial_will_end` are persisted idempotently without entitlement mutation and emit sanitized structured logs through the webhook service log sink. Focused webhook tests cover paused, resumed, payment-action-required, terminal status, recognized no-op logging, and unknown-event silence.
- Resolved on 2026-07-07: removed the package-level `max` helper from `backend/internal/entitlement/usage_limiter.go`. Remaining-count calculations in `usage_limiter.go` and `status.go` now resolve to Go's built-in `max`, preserving the existing negative-remaining clamping behavior.
- Resolved on 2026-07-07: `UsageLimiter.CheckSearchAllowed` now treats inactive paid/trial users as effective free users for allowed free-scope searches, so Catalog and single-input Substitution use the rolling free-tier cap when paid access is not active. Active paid/trial users remain unmetered for paid-scope access. Regression tests cover `paid:past_due` and `paid:cancelled` free-scope limit denial, counted inactive paid free-scope completion, and unchanged active paid/trial behavior.
- Planned as Phase 06.01 tasks 177-188: manual webapp acceptance of Phase 06 subscription checkout is blocked because the backend authentication API exists, but the frontend does not provide a sign-in/register surface that creates an authenticated browser session. Phase 06.01 adds ARCH-018/DESIGN-018 frontend auth session architecture, generated auth client, login/register/consent UI, authenticated checkout guard, authenticated Search/Subscription sidebar navigation, browser workflow coverage, SWE.5 obligations, and UAT evidence. A local development auth shortcut is explicitly not accepted as project-owner checkout UAT evidence.
- **OPEN REVIEW ACTION (2026-07-14; owner: Phase 06/06.01 frontend maintainer):** remove the secondary-color highlight background from the `Checkout completed. Billing access is active.` status because the treatment harms legibility, then purge every rendered use of that background across the repository rather than fixing only this message. The current `bg-[var(--color-secondary)]` occurrences cover the checkout-success status and billing loading placeholders in `SubscriptionBilling.svelte` plus the registration status in `RegisterView.svelte`; replace status surfaces with an unhighlighted, accessible treatment and loading placeholders with an appropriate neutral skeleton treatment. Search all frontend source, tests, stories, fixtures, and generated evidence for equivalent direct-variable, utility, inline-style, or hard-coded secondary-background use, remove every occurrence, update stale assertions and screenshots, and delete the `--color-secondary` token plus responsive-test probes if the audit leaves it unused. Preserve semantic `role="status"` announcements, readable text, visible borders/focus behavior, loading-state meaning, and light/dark-theme WCAG AA contrast. Verification: repository-wide searches find no rendered secondary highlight background or equivalent hard-coded color; component tests cover checkout success, registration status, and billing loading states without the highlight; desktop/mobile Playwright and axe checks pass in both themes; frontend typecheck, unit tests, build, and screenshot verification pass.

### Testing coverage deviations

- Accepted on 2026-07-02 for Task 173 aggregate evidence: backend internal coverage is 95.7% using `go test ./internal/... -coverprofile=coverage.out`. Phase 06 packages remain below 100%: `backend/internal/entitlement` is 75.1% and `backend/internal/subscription` is 79.6%. The uncovered Phase 06 backend functions are primarily `StatusService` read-model projection branches, hourly reconciliation loop/bootstrap branches, Stripe HTTP pagination and malformed-provider response branches, signature/payload defensive branches, and dead-letter fallback paths. The required product paths are covered by focused entitlement, usage-limit, checkout, webhook, duplicate-webhook, reconciliation, repository, and billing workflow tests; adding more seams solely to force external Stripe transport and infinite-loop branches would increase production complexity without changing Phase 06 behavior.
- Accepted on 2026-07-02 for Task 173 aggregate evidence: frontend `bun test --coverage` reports `All files | 98.85 | 96.76`. The remaining Phase 06 frontend gaps are `src/lib/api/entitlement-client.ts` malformed billing-envelope and status-fallback mapping branches, and `src/lib/search-entitlement.ts` non-auth entitlement-error fallback and label branches. Generated-contract use, checkout idempotency, recoverable billing errors, entitlement stores, anonymous Catalog allowance, free-user paid-mode blocking, usage-limit feedback, billing recovery blocking, and Playwright/axe billing/search flows are covered.

## Phase 06.01

### Assumptions

- Accepted for Task 190 repair on 2026-07-07: the agreed handheld/mobile issues were the mobile sidebar focus order after the unit preference selector, auth registration screenshot capture against the combined legal-consent checkbox, direct mobile evidence for no horizontal scrolling, text overlap, clipped controls, accessible auth/subscription navigation, and the Search return flow preserving search state. The repair keeps desktop workflows unchanged and ties changed handheld styles/navigation to `DESIGN-016`.

### Testing coverage deviations

- Accepted on 2026-07-07 for refreshed Phase 06.01 aggregate evidence: frontend `bun test --coverage` reports `All files | 95.68 | 94.59`. The remaining Phase 06.01 frontend gaps are defensive and fallback branches rather than uncovered nominal auth workflows. `src/lib/api/auth-client.ts` leaves malformed auth-envelope handling, status fallback category mapping, and default auth error-code mapping below 100%; generated auth DTO use, credentialed CSRF/register/login/logout/session calls, safe error messages, retry metadata, and token stripping are covered. `src/lib/api/entitlement-client.ts` retains the previously accepted malformed billing-envelope/status fallback branches while Phase 06.01 reuses the same authenticated checkout path. `src/lib/api/generated.ts` leaves one generated helper line uncovered; generated-contract drift is checked separately by `bun run check:api-types`, and generated DTO/helper imports are covered by API client tests. `src/lib/components/oauth-entry-point.ts` leaves the default `window.location.assign` navigation dependency branch below 100% because tests inject navigation to avoid mutating the browser location; provider-route validation, fail-closed unavailable handling, accepted `Google` provider label behavior, and callback refresh are covered. `src/lib/components/register-controller.ts` leaves bundled consent-loader, invalid-form early return, non-object error extraction, fallback registration error construction, and some field-specific validation branches below 100%; consent gating, password mismatch, stale consent, duplicate email, unverified-login-method, successful registration, and password clearing are covered. `src/lib/search-entitlement.ts` leaves non-auth entitlement-error fallback and some mode-label branches below 100%; anonymous Catalog allowance, paid-mode sign-in guidance, free limits, blocked daily-diet modes, and trial/paid allowance are covered. `src/lib/stores/auth-session.ts` leaves logout/OAuth/authentication failure projection branches, optional projection-field sanitization, storage parse fallback, storage write fallback, and stored-projection validator branches below 100%; server session probing, login/register session creation, logout success clearing, OAuth refresh success, entitlement refresh coordination, storage failure tolerance, no-token persistence, and anonymous/expired/locked/error states are covered.

### Actions needed

- Resolved: Phase 06.01 UAT evidence must come from the real frontend sign-in/register surface and HttpOnly-cookie session workflows. A local development auth shortcut remains test-only support and is not accepted as checkout UAT evidence.
- Resolved on 2026-07-10: removed the unused permissive HTTP validation wrappers `ValidateCheckoutCreateRequestBody` and `ValidateBillingPortalRequestBody` from `backend/internal/httpapi/billing_validation.go`. Production routes and unit tests now call only their `...ForOrigin` counterparts with an explicit allowed frontend origin.
- Resolved on 2026-07-10: `SubscriptionController.Routes()` now rejects route registration when the billing redirect origin is missing or blank, and the underlying billing URL validator always requires a same-origin match. Subscription controller and billing workflow tests configure an explicit frontend origin, with focused coverage for the missing-origin failure.
- Resolved by product decision on 2026-07-10: removed the separate `AuthSurface` page and `/auth` / `/auth/register` UI paths; the SearchShell modal is the product's sole sign-in and registration surface. Deleted `DisclaimerPanel` and all auth-specific disclaimer loading because medical-disclaimer information belongs in the Terms of Service and future About section under revised SW-REQ-071. The `/auth/callback` selector now opens the modal over the normal SearchShell and preserves the existing server-backed OAuth session and entitlement refresh without a separate visible auth page.
- Resolved on 2026-07-10: OpenAPI-generated frontend DTOs are ordinary fixed-shape interfaces, `Envelope<TData>` accepts unconstrained payloads, JSON mutation helpers accept `TRequest extends object`, and generated auth/checkout/portal header types combine required known fields with `Record<string, string>` so their request-init objects satisfy browser `HeadersInit`. `AuthApiClient` decoding accepts fixed-shape response objects. The focused `bun run typecheck` production-source gate is part of both `frontend/package.json` `check` and root `scripts/check.py`, preventing these generated DTO, envelope, header, and `fetch` incompatibilities from passing unnoticed while unrelated test-fixture typing remains outside this repair.
- Deferred to Phase 09: add and validate production Google and Apple OAuth provider gateway configuration before enabling live external login. At that point, reshape the current Google-only fields in `config.OAuthConfig` into provider-specific nested configuration (for example, `OAuthConfig.Google GoogleOAuthConfig` and `OAuthConfig.Apple AppleOAuthConfig`) rather than forcing Apple credentials into Google-oriented or falsely generic fields. Until then, Phase 03 production bootstrap fails OAuth routes closed.

## Phase 07

Phase 07 open points were reviewed for Task 211 on 2026-07-11 with Task 210
already `PASSED`. Implemented behavior is marked `IMPLEMENTED`, retained
planning constraints are marked `ACCEPTED`, obsolete items are marked
`CLOSED`, and deferred work has an owner, target date, and focused verification
condition. No unowned or disposition-less Phase 07 entry remains.

### Assumptions

- **IMPLEMENTED (2026-07-11):** a saved daily diet is a user-owned aggregate with a dedicated parent row and ordered meal-entry rows. Each entry references an existing meal and stores a positive quantity plus canonical unit; aggregate protein, carbohydrates, fat, and calories are recalculated from current server-side meal data rather than trusted from the client. The `saved_items` row with kind `saved_diet` references the parent daily-diet row. Evidence: `backend/internal/repository/types.go`, migration `database/migrations/000019_saved_diet_persistence.up.sql`, and `backend/internal/dailydiet/service.go`.
- **IMPLEMENTED (2026-07-11):** saved-diet creation and optimization submission require the cross-phase `Idempotency-Key` contract. Create/submission retries replay the durable response for the same body; changed-body reuse conflicts. Full replacement is absolute `PUT`, and deletion uses the stable saved-diet identifier. Evidence: `backend/internal/dailydiet/service.go`, `backend/internal/httpapi/optimization_controller.go`, and `backend/internal/repository/saved_diet_mutation_repository.go`.
- **IMPLEMENTED for Phase 07 (2026-07-11):** optimization jobs and results are visible only to the authenticated submitting user. Submission reloads the saved diet under the authenticated owner, the worker reloads repository inputs with the job owner, polling applies the same owner predicate, and job state/result data uses the DESIGN-004 one-hour TTL with an owner marker after expiry. Evidence: `backend/internal/httpapi/optimization_controller.go`, `backend/internal/worker/optimization_processor.go`, and `docs/implementation/reviews/task-210-review.md`.
- **DEFERRED TO PHASE 09:** account deletion does not yet cancel or proactively delete optimization job, result-marker, attempt, lock, and enqueue keys; the current `redisCachePurger` only deletes the generic `user:<id>` key. The owned implementation action is now recorded under Phase 09 rather than treated as implemented Phase 07 behavior.
- **IMPLEMENTED (2026-07-11):** the required client completion mechanism is bounded polling. **DEFERRED TO PHASE 09:** the optional ARCH-004 WebSocket notification path remains out of Phase 07. The Phase 09 product/architecture decision must use production polling evidence to close the option or plan its authenticated contract. The `202 Accepted` contract and worker isolation are unaffected.
- **ACCEPTED (2026-07-11):** queue and solver infrastructure are injectable at the Go boundaries. Unit tests use deterministic fakes; Redis Streams and the packaged native CLP path are exercised by focused integration evidence, while CI does not require a live external provider. The local Redis/CLP prerequisites remain explicit in the UAT checks.

### Clarifications

- **CLOSED (2026-07-11):** the Phase 05 text-entry `dailyDietId` scaffold was temporary. Phase 07 replaces it with selection of an authenticated saved-diet collection; users do not manually copy UUIDs into the finished workflow. `dailyDietId` remains a server-selected resource identifier in the optimization request.
- **IMPLEMENTED (2026-07-11):** DESIGN-004's detailed failure names (`failed_validation`, `solver_timeout`, `solver_infeasible`, and `queue_unavailable`) are exposed as stable error codes beneath the public terminal job status `failed`, keeping the OpenAPI status vocabulary aligned with ARCH-004 while preserving actionable client messages.

### Actions needed

- **IMPLEMENTED (2026-07-11):** the production optimization queue uses Redis Streams through the existing `github.com/redis/go-redis/v9` dependency, not Machinery or Redis Lists. `XADD` publishes server-created job IDs; one consumer group uses `XREADGROUP`, `XACK` follows terminal handling, and `XAUTOCLAIM` reclaims abandoned deliveries. Idempotent job-state/result publication, single-solver visibility exceeding the 30-second solver deadline, a three-attempt terminal limit, bounded queue metrics, and worker-only solving are implemented. Full ownership protection across multi-alternative processing remains open under the action below. Evidence: `backend/internal/queue/job_queue.go`, `backend/internal/worker/optimization_processor.go`, and the Task 203/210 integration reviews. A separate Redis Lists exercise is outside production scope.
- **IMPLEMENTED; prior Task 201 action CLOSED (2026-07-11):** the worker invokes the native COIN-OR CLP `1.17.11` executable through an injectable pure-Go child-process wrapper, never through CGO or the Fiber API. Validated models are serialized to generated-name LP input, `exec.CommandContext` enforces the hard 30-second deadline, output is bounded/sanitized, private per-job temporary directories are cleaned, and worker readiness performs the exact version check. `CGO_ENABLED=0` worker build evidence is recorded in the Task 209 review. `docs/design/DESIGN-004.md`, `docs/architecture/ARCH-004.md`, `docs/architecture/01_SOFT_ARCH_DESIGN.md`, `docs/architecture/02_APPENDIX_A.md`, and `docs/design/01_TECH_STACK.md` now contain the child-process decision; no Task 201 documentation action remains open.
- **DEFERRED TO PHASE 09 (owner: backend/platform maintainer):** integrate optimization job cancellation and Redis-key invalidation into account deletion, covering the job/result, retained owner marker, retry, lock, and enqueue namespaces. The canonical open action and focused verification now live under Phase 09.
- **IMPLEMENTED (2026-07-12):** imperial saved-diet entries are normalized at the optimizer constraint boundary through the shared repository unit converter. Solid `g`/`oz` entries use the per-100 g nutrition basis, liquid `ml`/`fl_oz` entries use the per-100 ml basis, and explicit cross-basis units remain invalid. Equivalent metric and imperial fixtures produce identical constraint models, macro targets, and solver inputs. Evidence: `backend/internal/optimization/constraints.go` and `backend/internal/optimization/constraints_test.go`.
- **IMPLEMENTED (2026-07-12):** new Daily Diet draft entries derive their editable quantity and unit together from the meal nutrition basis and active unit preference. Metric entries start at `100 g` or `100 ml`; imperial entries start at `3.53 oz` or `3.38 fl oz`, whose round-trip conversion remains within 0.1 base units of the required 100 g/ml aggregate basis. Evidence: `frontend/src/lib/units.ts`, `frontend/src/lib/units.test.ts`, and `frontend/src/lib/components/DailyDietCollection.svelte`.
- **IMPLEMENTED (2026-07-12):** one 30-second job-scoped context now covers repository loading and every alternative solver attempt, followed by at most 5 seconds for terminal timeout publication. The combined 35-second window is explicitly tested to remain below the 45-second Redis visibility timeout and 44-second logical lock TTL, preventing a second worker from reclaiming a live job. Parent cancellation remains distinct so shutdown leaves the delivery pending for normal recovery; internally timed-out jobs retain validated partial alternatives and publish `solver_timeout` through the bounded finalization context. Evidence: `backend/internal/worker/optimization_processor.go`, `backend/internal/worker/optimization_processor_deadline_test.go`, `docs/design/DESIGN-004.md`, and `docs/architecture/ARCH-004.md`.
- **IMPLEMENTED (2026-07-12):** a Go/Redis admission gate now permits one active queued or processing optimization job per authenticated user and 10 newly accepted jobs per fixed UTC hour. The active reservation uses atomic `SET NX`; the counter uses ordinary Redis transaction commands with no Lua or additional runtime. Exact idempotency retries reuse the same server job without incrementing the rate, compare-and-delete release cannot remove a newer job's slot, raw user IDs are hashed in Redis keys, Redis failure returns 503, and 429 rejections include `Retry-After` without creating idempotency, job, or stream records. Terminal worker publication releases capacity; failed submission side effects release newly acquired reservations. Evidence: `backend/internal/worker/optimization_admission.go`, `backend/internal/worker/optimization_admission_integration_test.go`, `backend/internal/httpapi/optimization_controller.go`, and `backend/internal/httpapi/optimization_controller_test.go`.
- **IMPLEMENTED (2026-07-12):** `PostgresMealRepository.Search` now runs a filter-equivalent embedded count query, returns immediately for offsets beyond the total, and applies `LIMIT`/`OFFSET` in the embedded meal-search SQL before hydrating results. The optimizer's existing 100-item loop therefore hydrates each catalog meal only on its own page. Executor regressions prove pagination arguments reach SQL and out-of-range pages issue no hydration query; a 201-meal optimizer fixture completes in exactly three page calls with one direct saved-diet meal lookup. Evidence: `backend/internal/repository/meal_repository.go`, `backend/internal/repository/sql/meal_search.sql`, `backend/internal/repository/sql/meal_search_count.sql`, `backend/internal/repository/repository_test.go`, and `backend/internal/optimization/constraints_test.go`.
- **IMPLEMENTED (2026-07-12):** Daily Diet draft ownership is cleared at both frontend layers when authentication becomes anonymous/expired or the authenticated user ID changes. `SearchShell` removes hydrated meal selections and selection errors; `DailyDietCollection` resets the editable name, meals, consumed-key state, errors, aggregate projection, and saved ID while clearing the shared store. Existing selection keys are quarantined during the transition so stale parent props cannot be re-consumed before parent cleanup, while same-user rerenders preserve the draft. Focused desktop/mobile browser coverage builds a two-meal draft, logs out through the real UI flow, and confirms no draft meal remains or is saveable. Evidence: `frontend/src/lib/components/SearchShell.svelte`, `frontend/src/lib/components/DailyDietCollection.svelte`, their focused tests, and `frontend/tests/daily-diet-workflow.spec.ts`.
- **IMPLEMENTED for Phase 07; standalone mode DEFERRED TO PHASE 11 (2026-07-12; renumbered 2026-07-20):** saved-diet optimization now accepts only `dailyDietId`, tolerance, and exclusions; current server-owned saved-diet entries derive authoritative macro targets. The OpenAPI/generated request type and HTTP validator reject legacy client `targetMacros`, and the frontend renders saved-diet aggregate macros as read-only values while keeping tolerance editable. Standalone client-authored Protein, Carbohydrate, and Fat generation is a distinct Phase 11 workflow with an explicit request variant and no `dailyDietId`; its canonical open action is recorded under Phase 11 and it is not silently implemented through Phase 07. Evidence: `api/openapi.yaml`, `backend/internal/httpapi/optimization_controller.go`, `frontend/src/lib/components/OptimizationWorkflow.svelte`, generated contract tests, and focused backend rejection tests.
- **IMPLEMENTED (2026-07-12):** `backend/Dockerfile.worker` now builds the dedicated `linux/amd64` optimizer image on the minimal Ubuntu 24.04 container base. It downloads COIN-OR's official Ubuntu 24 CLP `1.17.11` release artifact, verifies SHA-256 `105ce8684ae95412259b24743895f3be6fb642248052e424cf00939dbd57631c`, installs only its native runtime dependencies, and packages it beside the `CGO_ENABLED=0` Go worker under a non-root account. CLP remains a worker-owned child process, not a separate service or API dependency. `scripts/verify-clp-worker-image.sh` performs a no-cache amd64 build, exact runtime version check, and worker executable check; the focused verification passed with `Coin LP version 1.17.11, build Mar 11 2026`. The amd64-only scope is deliberate because upstream does not publish an Ubuntu ARM64 artifact for this release. Evidence: `backend/Dockerfile.worker`, `scripts/verify-clp-worker-image.sh`, `docs/design/DESIGN-004.md`, `docs/design/01_TECH_STACK.md`, and `docs/architecture/ARCH-004.md`.
- **IMPLEMENTED (2026-07-12):** optimization alternatives now emit the derived calorie estimate only as `macros.calories`, matching the OpenAPI `MacroProjection`; the undocumented top-level `calories` property and frontend compatibility fallback are removed. Controller and integration checks assert the nested shape, while frontend contract tests decode completed and failed-with-partial-results alternatives and reject the legacy placement. Evidence: `backend/internal/httpapi/optimization_controller.go`, `backend/internal/httpapi/optimization_controller_test.go`, `backend/internal/app/task206_backend_integration_test.go`, `frontend/src/lib/api/optimization-client.ts`, and `frontend/src/lib/api/optimization-client.test.ts`.
- **IMPLEMENTED (2026-07-12):** removed the committed `backend/worker` ELF artifact and added the exact `/backend/worker` ignore rule. Worker build evidence now uses an explicit `/tmp/mealswapp-worker` output so local verification cannot recreate generated output in the source tree. The optimizer Dockerfile continues to build its worker exclusively from `backend/cmd/worker` source. Focused verification confirms the deletion is present in the repository change, the path is ignored for future builds, the explicit source build passes without recreating it, and the optimizer image rebuilds with the worker produced by its own `CGO_ENABLED=0` build stage. Evidence: `.gitignore`, `backend/Dockerfile.worker`, and the Task 201/203/208 review commands.
- **IMPLEMENTED (2026-07-12):** Phase 07 Daily Diet and optimization text/number/select controls now use the prescribed white background, `#E0E0E0` 1px border, dark readable input text, and primary focus ring. Every button introduced by these components uses `transition-all duration-200` while retaining its existing focus and disabled behavior. Evidence: `frontend/src/lib/components/DailyDietCollection.svelte` and `frontend/src/lib/components/OptimizationWorkflow.svelte`; focused component, typecheck/build, and browser accessibility verification is required after this change.
- **IMPLEMENTED (2026-07-12):** every exported optimization phase/retry type, display/state interface, initial-state factory, shared store, controller option, controller method, and controller factory now has concise TSDoc describing its public lifecycle, identity, retry, idempotency, polling, and disposal semantics. Private helpers remain uncommented, and the adjacent `solver_timeout` indentation typo is corrected without runtime changes. Evidence: `frontend/src/lib/stores/optimization.ts`; focused store tests, frontend typecheck, and production build pass.
- **IMPLEMENTED (2026-07-12):** every exported Phase 07 queue default, worker readiness key, optimization failure/job/solver status, CLP configuration constant, solver error kind, and solver sentinel error now has concise identifier-leading Go Doc alongside the existing design trace comments. `scripts/validate-phase07-go-doc.py` enforces this convention for exported grouped constants and variables in the Phase 07 packages and is included in the aggregate quality gate. Evidence: `backend/internal/queue/job_queue.go`, `backend/internal/worker/readiness.go`, `backend/internal/worker/optimization_processor.go`, `backend/internal/optimization/validator.go`, `backend/internal/optimization/clp_wrapper.go`, and `scripts/check.py`.
- **IMPLEMENTED (2026-07-18; owner: Phase 07 API/frontend maintainer; evidence: `docs/implementation/preparation/task-213-preparation.md` and independent PASSED review `docs/implementation/reviews/task-213-review.md`):** audit `api/openapi.yaml` for every response status reachable from daily-diet and optimization routes, including handler, shared middleware, dependency, timeout, and rate-limit failures. The collection `GET /api/v1/daily-diets` currently documents only `200` and `401`, while its implementation can produce `503` when the daily-diet dependency is unavailable and can inherit global `429`, `504`, and generic `500` responses. Define and apply one consistent endpoint-versus-shared-middleware documentation policy; add missing `Error` responses where the policy requires them; then decide whether `scripts/generate-api-types.py` must become response-aware or a drift test must enforce the documented matrix. Verification: compare each route's declared responses with controller and router error paths, run `npx --no-install redocly lint api/openapi.yaml`, `cd frontend && BUN_TMPDIR=$PWD/.bun-tmp BUN_INSTALL=$PWD/.bun-install bun run check:api-types`, and focused API-client error-mapping tests.
- **IMPLEMENTED (2026-07-18; owner: Phase 07 backend maintainer; evidence: `docs/implementation/preparation/task-214-preparation.md` and independent PASSED review `docs/implementation/reviews/task-214-review.md`):** remove the unused `CreateSavedDiet`, `GetSavedDiet`, `ListSavedDiets`, `ReplaceSavedDiet`, and `DeleteSavedDiet` forwarding methods from `PostgresSavedDataRepository`. `DailyDietRepository` already supplies the canonical generic CRUD contract, and `SavedDietRepository` is only its type alias; no production or test caller uses the duplicate names. Verification: repository-wide search finds no alias declarations or call sites, `gofmt` leaves the repository clean, and `cd backend && GOCACHE=$PWD/.go-cache GOMODCACHE=$PWD/.go-mod-cache go test ./...` passes.
- **IMPLEMENTED (2026-07-18; owner: Phase 07 backend maintainer; evidence: `docs/implementation/preparation/task-216-preparation.md` and independent PASSED review `docs/implementation/reviews/task-216-review.md`):** correct and streamline `dailydiet.Service.Create`. Its durable idempotency record currently stores only the created diet ID, while replay reloads the current resource; an exact retry after replacement therefore returns changed data, and a retry after deletion returns not-found instead of the original successful response. Persist and replay the immutable original create response, or explicitly redefine and document resource-reference semantics. Remove the process-wide `createMu`, which serializes unrelated users across meal and database I/O, ignores cancellation while waiting, and adds no cross-instance protection beyond the transactional idempotency claim. Avoid fetching every meal twice during a successful create by reusing validated meal data or adding an appropriate batch/projection path. Verification: regression tests prove exact replay after replacement, deletion, and changed meal macros returns the agreed durable response; changed-body key reuse still returns conflict; concurrent different-user creates proceed independently while same-key creates remain atomic across service instances; cancellation is not blocked behind unrelated work; the maximum-entry path does not perform duplicate per-meal lookups; and `cd backend && GOCACHE=$PWD/.go-cache GOMODCACHE=$PWD/.go-mod-cache go test -race ./...` passes.
- **IMPLEMENTED (2026-07-18; owner: Phase 07 backend/design maintainer; evidence: `docs/implementation/preparation/task-215-preparation.md` and independent PASSED review `docs/implementation/reviews/task-215-review.md`):** consolidate the three overlapping unit predicates in `backend/internal/dailydiet/service.go`, `backend/internal/repository/user_data_repository.go`, and `backend/internal/repository/units.go` around one canonical quantity-unit vocabulary. Saved diets, their database constraint, and the public substitution API accept only `g`, `ml`, `oz`, and `fl_oz`; the repository predicate additionally accepts `serving`, even though `ConvertUnit` cannot convert it to a mass or volume unit and serving conversion already has the separate `ConvertServingToBase` path. Before changing the vocabulary, obtain a product/design disposition: `serving` is not undocumented generated behavior because SW-REQ-036 requires per-unit calculation and DESIGN-005 explicitly requires serving-based recipe ingredients, but the intended relationship between the user-facing term `per unit` and the internal token `serving` is not defined consistently. Retain `serving` only in the explicitly approved recipe/per-unit boundary, or remove it and update the traced requirements/design if that capability is rejected; do not broaden saved-diet or public substitution units accidentally. Verification: one shared validator owns the four canonical mass/volume units; any retained serving token has a separately named, context-specific validation/conversion path; repository, service, HTTP validation, OpenAPI enums, database checks, and frontend unit types agree; tests reject cross-basis and unsupported units and cover the approved per-unit/recipe behavior; and requirement/design traceability validation passes.
- **IMPLEMENTED (2026-07-18; owner: Phase 07 backend/observability maintainer; evidence: `docs/implementation/preparation/task-223-preparation.md` and independent PASSED review `docs/implementation/reviews/task-223-review.md`):** replace raw optimization submission-outcome strings with one typed, constant vocabulary owned by the observability boundary, and make every `OptimizationController.Submit` return path report its actual final result. The current direct idempotency replay returns `202` while retaining the default `error` outcome, the conflict-repair path marks `replayed` before repair can fail, and several validation, dependency, conflict, and replay-queue paths are misclassified. Verification: table-driven HTTP tests cover `accepted`, successful `replayed`, `rejected`, `dependency_error`, `queue_error`, and unexpected `error`; failed repair is never counted as replayed; all emitted labels pass the bounded-label allowlist; and telemetry contains no user, diet, job, key, or request-body data.
- **IMPLEMENTED (2026-07-18; owner: Phase 07 backend/observability maintainer; evidence: `docs/implementation/preparation/task-223-preparation.md` and independent PASSED review `docs/implementation/reviews/task-223-review.md`):** simplify `cloneLabels` in `backend/internal/observability/optimization.go` by replacing its manual key/value copy loop with `maps.Copy`, which is available under the repository's Go 1.25 toolchain. Preserve the existing behavior that an empty or nil input returns `nil` rather than an allocated empty map, and retain a newly allocated destination for non-empty input so metric sinks cannot mutate the caller's labels. Verification: focused observability tests cover nil, empty, and populated inputs plus source/destination mutation independence; `gofmt` and `cd backend && GOCACHE=$PWD/.go-cache GOMODCACHE=$PWD/.go-mod-cache go test ./internal/observability` pass.
- **IMPLEMENTED (2026-07-18; owner: Phase 07 backend maintainer; evidence: `docs/implementation/preparation/task-217-preparation.md` and independent PASSED review `docs/implementation/reviews/task-217-review.md`):** remove the redundant `CLPSolver` type alias, `NewCLPSolver` forwarding constructor, and `StartupCheck` forwarding method from `backend/internal/optimization/clp_wrapper.go`. DESIGN-004 names `LPSolverWrapper`, `NewLPSolverWrapper` is already used by all current constructors, and `CheckVersion` is the documented readiness operation. `StartupCheck` implements no separate interface or lifecycle behavior; update its worker and integration-test callers to invoke `CheckVersion` directly. These aliases add a second public vocabulary without abstraction, compatibility, or interface value. Keep `LPSolverWrapper`, `NewLPSolverWrapper`, and `CheckVersion` as the single canonical API. Verification: repository-wide search finds no `CLPSolver`, `NewCLPSolver`, or `StartupCheck` declaration or call site; worker startup still fails clearly for unavailable or mismatched CLP versions; identifier-leading Go Doc validation remains clean; `gofmt` passes; and `cd backend && GOCACHE=$PWD/.go-cache GOMODCACHE=$PWD/.go-mod-cache go test ./internal/optimization ./internal/worker ./cmd/worker` passes.
- **IMPLEMENTED (2026-07-18; owner: Phase 07 backend maintainer; evidence: `docs/implementation/preparation/task-217-preparation.md` and independent PASSED review `docs/implementation/reviews/task-217-review.md`):** harden `LPSolverWrapper.Solve` in `backend/internal/optimization/clp_wrapper.go` so its documented process-boundary guarantees are explicit and enforceable. The exported injected `CommandRunner` is called synchronously and can block forever if it ignores context, making the hard deadline conditional even though the production runner uses `exec.CommandContext`; either constrain the runner seam to trusted test-only use or redesign the boundary so deadline compliance is enforceable without leaking goroutines. Do not silently discard `os.RemoveAll(jobDir)` failures: perform bounded best-effort cleanup and emit sanitized observability without replacing the primary solver result/error. Parse the bounded machine-readable solution file as the authoritative result and use stdout only as a deliberate fallback when that file is absent; concatenating human-oriented stdout to a valid solution can introduce duplicate/unknown generated variables or conflicting statuses and incorrectly reject an otherwise valid solve. Verification: tests cover a runner that does not cooperate with cancellation according to the chosen runner contract, cleanup failure observability and primary-error preservation, valid solution-file output accompanied by misleading or duplicate-variable stdout, stdout-only fallback if retained, timeout child termination, unconditional normal cleanup, bounded output, terminal status mapping, and the packaged CLP fixture; `go test -race ./...` passes.
- **IMPLEMENTED (2026-07-18; owner: Phase 07 backend maintainer; evidence: `docs/implementation/preparation/task-217-preparation.md` and independent PASSED review `docs/implementation/reviews/task-217-review.md`):** simplify and harden `serializeLP` in `backend/internal/optimization/clp_wrapper.go`. Replace `output.WriteString(fmt.Sprintf(...))` and temporary string concatenation with direct formatted writes or separate fixed/value writes that satisfy the configured linter; reuse each variable's already-generated canonical name from `variableNames` when rendering bounds instead of formatting it a second time. Separate the constraint and bound rendering enough to keep validation and output logic readable without introducing a broad abstraction. Most importantly, enforce `maxSolverModelBytes` while writing rather than only after the complete buffer has been allocated and populated, so an oversized internal model cannot consume unbounded serialization memory and CPU before rejection. Preserve deterministic variable-slice ordering, generated identifiers, finite-value and objective-coverage validation, safe rejection of unknown variables, supported CLP syntax, and the existing output-limit error classification. Verification: focused tests cover exact-limit and over-limit models with early bounded rejection, deterministic repeated serialization, equality and ranged constraints, positive/negative/zero coefficients, canonical generated-name reuse, invalid bounds/coefficient/objective references, linter cleanliness, and a successful packaged-CLP solve; `gofmt`, `go vet`, and focused optimization tests pass.
- **IMPLEMENTED (2026-07-18; owner: Phase 07 backend maintainer; evidence: `docs/implementation/preparation/task-217-preparation.md` and independent PASSED review `docs/implementation/reviews/task-217-review.md`):** update `clpVersion` in `backend/internal/optimization/clp_wrapper.go` to range directly over `strings.FieldsSeq(string(output))` instead of allocating a complete token slice through `strings.Fields`. The repository targets Go 1.25, so the iterator API is available. Preserve punctuation trimming, exact `major.minor.patch` matching, first-valid-token behavior, and the `unknown` fallback. Verification: focused tests cover accepted output, surrounding punctuation, multiple tokens, malformed output, and version mismatch; the configured linter, `gofmt`, and focused optimization tests pass.
- **IMPLEMENTED (2026-07-18; owner: Phase 07 backend maintainer; evidence: `docs/implementation/preparation/task-217-preparation.md` and independent PASSED review `docs/implementation/reviews/task-217-review.md`):** harden and streamline `parseCLPSolution` in `backend/internal/optimization/clp_wrapper.go`. Iterate the existing byte payload with `bytes.Lines` and each line with `bytes.FieldsSeq` instead of allocating a full string plus line and field slices. Replace prefix-based terminal-status recognition with the exact supported CLP status token/header grammar so words such as `optimality`, `infeasible-data`, or `unboundedness` cannot be misclassified. Parse variables only from the documented CLP solution-row shape rather than accepting a known generated variable followed by a number anywhere in arbitrary diagnostic text; preserve rejection of unknown generated names, duplicates, conflicting statuses, missing quantities, non-finite values, missing status, and optimal output with no variables. Define a solver-output tolerance for tiny negative numerical residuals, clamp values within that explicitly justified tolerance to zero, reject materially negative quantities, and align the tolerance deliberately with downstream model validation rather than relying on unrelated constants accidentally. Verification: table-driven parser tests cover exact optimal/infeasible/unbounded headers, prefix lookalikes, valid row variants, misleading diagnostic tokens, unknown/duplicate variables, conflicting statuses, missing/malformed/non-finite quantities, tiny and material negative values, zero omission/sparse output behavior, missing status/variables, deterministic mapping, allocation-sensitive iterator use through the configured linter, and the packaged CLP fixture; focused optimization tests and `go test -race ./...` pass.
- **IMPLEMENTED (2026-07-18; owner: Phase 07 backend/design maintainer; evidence: `docs/implementation/preparation/task-218-preparation.md` and independent PASSED review `docs/implementation/reviews/task-218-review.md`):** correct and simplify `BuildConstraints` and its alternative-constraint construction in `backend/internal/optimization/constraints.go`. The normalized quantity inequality for a previous solution does not require a different selected meal set: for previous quantities `{A: 25, B: 100}`, `{A: 23.75, B: 100}` satisfies `A/25 + B/100 <= 1.95` while retaining both meal IDs, so repeated solves can waste attempts on alternatives that `selectedMealSetKey` later discards. Define and implement the intended multi-solution policy, using selection variables or an explicitly documented high-weight-meal exclusion heuristic rather than presenting quantity drift as meal-set exclusion. Remove redundant hard-bound representations: variable bounds are already serialized in the LP `Bounds` section, yet every bound is repeated as a `quantity_*` constraint and an excluded variable is restricted again by `exclude_*`; retain one canonical representation plus only an explicit exclusion constraint if the design requires it, so large catalogs do not inflate the model toward `maxSolverModelBytes`. Validate only the macro-target source actually selected: saved-diet entries are authoritative and must not be rejected because an ignored `TargetMacros` alias is invalid. Reject or otherwise model an all-zero authoritative target deliberately, because the current LP admits the empty optimum while downstream solution validation rejects empty assignments. Finally, align DESIGN-004's declared `BuildConstraints(req, vars) []LPConstraint` interface with the implemented repository-meal-to-`LPModel`/error contract. Verification: tests prove a repeated alternative must change the selected meal set under the approved policy, same-set quantity drift cannot consume generation attempts, serialized models contain no duplicate quantity/exclusion bounds, large-catalog model size is materially reduced, invalid ignored target aliases do not reject valid saved diets, all-zero targets fail early or produce an explicitly valid non-empty solution, deterministic feasible/infeasible fixtures remain stable, the design signature matches the implementation, and focused optimization tests plus `go test -race ./...` pass.
- **IMPLEMENTED (2026-07-18; owner: Phase 07 backend/data maintainer; evidence: `docs/implementation/preparation/task-218-preparation.md` and independent PASSED review `docs/implementation/reviews/task-218-review.md`):** make `ConstraintBuilder.LoadFromSavedDiet`, `validateMeal`, and `quantityInNutritionBasis` enforce a real eligible-meal and nutrition-basis boundary. The loader currently calls `MealRepository.Search` with an otherwise empty query and therefore labels every non-deleted meal as eligible without applying a documented optimization eligibility, classification, allergen, or normalized-macro policy. `validateMeal` then deliberately accepts an empty `PhysicalState`, and `quantityInNutritionBasis` guesses whether such a meal is gram- or milliliter-based from the saved-diet entry unit, even though persisted repository meals require an explicit solid/liquid state and solver coefficients are defined per repository base unit. Require an explicit supported physical state and usable normalized macro basis for every solver candidate; define whether an ineligible candidate is filtered or fails the job, with an original-diet meal always producing a clear safe failure if its authoritative basis is unusable. Apply the approved eligibility/filter policy before loading the complete catalog rather than relying on comments that call the unfiltered result eligible. Require the repository-returned saved diet to match the requested non-nil diet ID and authenticated owner instead of accepting a zero ID through the current partial identity check. Verification: tests reject empty/invalid physical state and unavailable normalized macros, never infer a base unit from the entry token, cover solid/liquid metric and imperial bases, prove disallowed/ineligible meals cannot become LP variables, prove an unusable original meal fails safely, verify exact diet ID/owner matching, and exercise the production repository query/filter path across multiple pages; focused optimization/worker integration tests and `go test -race ./...` pass.
- **IMPLEMENTED (2026-07-18; owner: Phase 07 backend/design maintainer; evidence: `docs/implementation/preparation/task-218-preparation.md` and independent PASSED review `docs/implementation/reviews/task-218-review.md`):** reduce ambiguous and redundant state in the `constraints.go` domain model before adding the deferred standalone-target workflow. `MacroTarget.Carbs` duplicates the repository, OpenAPI, controller, and frontend `Carbohydrates` vocabulary and creates a disagreement state that exists only to mirror stale DESIGN-004 wording. `DietOptimizationRequest` mixes the production saved-diet request with test-only or alternate entry points (`OriginalMeals`, `RepositoryMeals`, `TargetMacros`, string `ExcludedIDs`, and `MaxQuantity`), creating precedence rules and validation of fields that the Phase 07 API explicitly rejects or never supplies. Split public/persisted request data from internal solver-generation state and inject repository meal snapshots through function/validator dependencies rather than DTO fields; retain one typed exclusion representation and one canonical carbohydrate field. Remove `LPVariable.MealID` unless a real consumer is identified, because `ItemID` is the solver identity and the UUID copy is never read after construction and can diverge. Replace the undocumented `DefaultMaxQuantity = 1_000_000` sentinel with an approved saved-diet quantity policy: the current default permits recommendations up to one million grams or milliliters per meal and `0` ambiguously means “use that maximum” rather than zero. Align DESIGN-004 and tests with the resulting single vocabulary and contracts instead of keeping invalid minimal meal fixtures for convenience. Verification: repository-wide searches find no obsolete alias/test-injection fields or unread duplicated IDs; saved-diet generation, validation, worker persistence, and public macro projections use the canonical types; realistic lower/upper quantity policy is documented and enforced consistently by model construction and solution validation; JSON/OpenAPI compatibility is deliberately handled; and focused optimization, worker, HTTP, generated-contract, and race tests pass.
- **IMPLEMENTED (2026-07-18; owner: Phase 07 optimization/design maintainer; evidence: `docs/implementation/preparation/task-219-preparation.md` and independent PASSED review `docs/implementation/reviews/task-219-review.md`):** make the diversity objective preserve SW-REQ-022's calorie minimum as the primary objective while applying SW-REQ-023 only as a secondary best-effort preference. `DefaultDiversityPenalty = 0.1` is currently added directly to each original meal's per-unit calorie coefficient, so enough original-meal quantity can make a higher-calorie alternative win; neither the constant's scale nor an upper bound proving that it cannot overturn calorie ordering is documented. Define an explicit multi-objective policy, preferably lexicographic optimization or a rigorously bounded secondary term with documented units and numerical behavior, and clarify whether diversity penalizes meal inclusion or selected quantity. Keep original meals eligible unless explicitly excluded, but do not satisfy diversity by silently weakening the primary calorie requirement. Verification: fixtures include a lower-calorie solution containing an original meal and a slightly higher-calorie solution without it and prove the approved primary/secondary ordering; equal-calorie solutions prefer less original-meal overlap; penalty behavior is stable across gram/milliliter quantities and catalog scale; DESIGN-004, objective construction, solver serialization, and SW-REQ-022/SW-REQ-023 traceability agree; focused objective/diversity/packaged-CLP tests pass.
- **IMPLEMENTED (2026-07-18; owner: Phase 07 optimization/data maintainer; evidence: `docs/implementation/preparation/task-219-preparation.md` and independent PASSED review `docs/implementation/reviews/task-219-review.md`):** make `ObjectiveFunction` and `BuildObjective` represent the actual solver contract without allowing one zero-calorie catalog entry to fail the whole optimization job. Repository macro validation permits an all-zero meal, and the constraint loader currently includes the complete catalog, but `BuildObjective` treats `CaloriesPerUnit == 0` as a missing coefficient and rejects every variable list containing such a candidate. Define the eligibility/modeling policy for zero-information meals: normally filter candidates that contribute neither macro constraints nor objective value before model construction, while handling an unusable original-diet entry with the explicit safe failure owned by the constraint-boundary action; do not accept an unconstrained zero-cost variable or reject unrelated valid candidates merely because one exists. Remove or give real solver semantics to `ObjectiveFunction.VariableIDs` and `DiversityPenalties`: production serialization traverses `LPModel.Variables` and reads only `Coefficients`, so both fields currently exist only for tests/inspection and can diverge without affecting the solve. If calorie and diversity objectives become separate under the primary/secondary policy, model that separation explicitly rather than retaining ignored metadata. Reuse the package's canonical validation-error constructor instead of the identical one-line `objectiveValidationError` wrapper, and align DESIGN-004's errorless `BuildObjective(vars) ObjectiveFunction` declaration with the implemented validation contract. Verification: a catalog containing valid positive-calorie candidates plus a zero-macro/zero-calorie entry still builds and solves using the approved filtering policy; an all-unusable catalog and unusable original meal fail safely and distinctly; objective serialization consumes every and only authoritative objective field; mutation tests cannot create ignored/divergent ordering or penalty metadata; server-derived coefficients, duplicate/empty/non-finite/negative validation, deterministic output, primary/secondary ordering, design traceability, and packaged-CLP tests pass.
- **IMPLEMENTED (2026-07-18; owner: Phase 07 optimization maintainer; evidence: `docs/implementation/preparation/task-220-preparation.md` and independent PASSED review `docs/implementation/reviews/task-220-review.md`):** correct `GenerateAlternatives` validation, deduplication, and numerical canonicalization in `backend/internal/optimization/diversity.go`. The function currently appends each canonical result to `previous` and checks `seen` before `solutionSatisfiesModel`; consequently a duplicate that violates the active `alternative_*` constraint is never model-validated, is silently treated as a retry, and adds another redundant constraint for later attempts. The existing duplicate test deliberately exercises this invalid-solver-output path and therefore certifies the bug. Canonicalize and validate every solver result against the exact model before mutating iteration state or deduplicating, and distinguish a genuine duplicate from solver/model contract failure under the approved diversity policy. Validate and canonicalize caller-supplied previous solutions before building the initial `seen` set: raw zero-quantity entries currently affect `selectedMealSetKey` even though `canonicalSolution` later removes zero quantities. Define one solver-quantity tolerance shared with parsing and solution validation so tiny positive numerical residue does not create phantom selected meal IDs, and replace the fixed absolute `diversityValidationEpsilon` with a justified scale-aware bound. Evaluate constraint expressions in deterministic variable order rather than ranging over coefficient maps at numerical boundaries. Define the outcome when the arbitrary `limit * 3` attempt budget is exhausted before the requested number of distinct results, including whether valid partial results return success or an explicit safe reason; reject a nil context without panic if the exported function retains direct context inspection. Use the standard `maps` cloning helper for solution maps if it preserves the chosen nil/empty semantics. Align DESIGN-004's `GenerateAlternatives` interface and comments with the actual injectable solve boundary and raw-versus-validated return type. Verification: tests cover model-violating duplicate output, canonical prior solutions containing zero/tiny quantities, tiny positive/negative solver residue, deterministic near-boundary sums, scale-aware large/small bounds, attempt exhaustion with and without partial results, nil/cancelled/deadline contexts, request-state immutability, and one-to-three genuinely distinct alternatives; focused optimization tests and `go test -race ./...` pass.
- **IMPLEMENTED (2026-07-18; owner: Phase 07 optimization maintainer; evidence: `docs/implementation/preparation/task-220-preparation.md` and independent PASSED review `docs/implementation/reviews/task-220-review.md`):** simplify `validator.go` into one authoritative validation pipeline over one prevalidated repository snapshot. `GenerateAlternatives` already calls `ValidateSolution` for every solver result, then `GenerateValidatedAlternatives` calls it again for every accepted result; each call shallow-copies the catalog, rebuilds the complete meal index, revalidates quantities and targets, and recomputes all macros. The supposedly reusable `SolutionValidator` stores only a slice and recreates its index on every `Validate`. Construct and validate one immutable indexed snapshot once per generation job, perform exact-model validation and publication projection once in the approved order, and return the already validated `DietAlternative` rather than passing raw solutions through a second full validation pass. Remove the variadic `ValidateSolution(...repositoryMeals)`/`req.RepositoryMeals` fallback and use one explicit dependency boundary. Canonicalize sparse quantities before applying the 100-meal count so numerical zero/residue semantics match the parser and diversity key; use the shared scale-aware quantity tolerance and approved maximum-quantity policy rather than the current separate sentinel path. Preserve deterministic UUID ordering, independent repository-derived macro/calorie recomputation, exclusion enforcement, valid partial results on later failure, and safe errors. Align DESIGN-004's validator and generation signatures with the resulting contracts. Verification: instrumentation proves one catalog index build and one validation/projection per solver result, no caller slice mutation affects the snapshot, duplicate/nil repository IDs fail at construction, zero/tiny residual entries follow the canonical policy, 100/101 selected-meal boundaries match OpenAPI, metric/liquid quantities and macro tolerance boundaries remain correct, later solver failure preserves only previously validated alternatives, and focused optimization/worker/race tests pass.
- **IMPLEMENTED (2026-07-18; owner: Phase 07 backend/API maintainer; evidence: `docs/implementation/preparation/task-221-preparation.md` and independent PASSED review `docs/implementation/reviews/task-221-review.md`):** make `OptimizationFailureCode` a genuinely closed, context-correct safe vocabulary. `OptimizationFailure` is exported with an exported `Code` field, so any package can construct an arbitrary string value; `Error` and `FailureCodeOf` return it unchanged despite claiming that only stable safe codes can escape. Provide validated constructors or make construction private, validate persisted/decoded codes, and separate worker terminal-failure codes from HTTP-only conditions: `FailureCodeQueueUnavailable` and `FailureCodeResultExpired` are never used as `OptimizationFailure` values because the controller emits separate `AppError` strings, while cancellation has a public `cancelled` job state and solver error kind but no deliberate failure-code mapping. Define cancellation versus shutdown/retry behavior explicitly, retain `errors.Is`/`errors.As` cause classification without leaking diagnostics, and make worker publication, telemetry, OpenAPI enums, generated frontend types, and safe messages share the same bounded vocabulary. Verification: arbitrary/empty codes cannot be constructed, persisted, or returned; wrapped validation/infeasible/timeout/cancellation/queue/unknown errors follow the approved terminal-versus-retry state machine; nil receiver behavior remains safe; diagnostics and secrets never appear in public strings or payloads; every declared code has a real producer and consumer or is removed; contract generation and focused worker/controller tests pass.
- **IMPLEMENTED (2026-07-18; owner: Phase 07 product/optimization/frontend maintainer; evidence: `docs/implementation/preparation/task-221-preparation.md` and independent PASSED review `docs/implementation/reviews/task-221-review.md`):** implement or remove optimization-alternative `similarityScore` instead of publishing the zero-value as if it were calculated. `DietAlternative.SimilarityScore` is never assigned anywhere in the optimization pipeline, yet the controller always emits it and OpenAPI requires a number from 0 to 1, so every alternative currently reports `0` regardless of its relationship to the original diet. Decide the product meaning and algorithm, including whether it measures macro similarity, meal-set overlap, or another explicitly named concept; compute it from authoritative data and test ordering/rounding if retained, or remove it consistently from DESIGN-004, backend persistence, OpenAPI, generated frontend types, UI, and UAT if no user behavior consumes it. Do not use a fabricated default score. Verification: repository-wide search finds either one documented authoritative calculation with nontrivial fixtures and stable bounds or no `similarityScore` contract/state at all; Redis compatibility is addressed; controller payloads and generated clients agree; frontend rendering does not present misleading zero scores; focused unit/integration/browser tests and contract drift checks pass.
- **IMPLEMENTED (2026-07-18; owner: Phase 07 backend maintainer; evidence: `docs/implementation/preparation/task-222-preparation.md` and independent PASSED review `docs/implementation/reviews/task-222-review.md`):** remove the controller-wide `OptimizationController.mu` submission lock or replace it only if evidence requires coordination scoped to the authenticated idempotency key. The current mutex serializes unrelated users while held across entitlement, PostgreSQL, Redis admission, Redis job-store, and queue calls, and waiting for `sync.Mutex` does not honor request cancellation. PostgreSQL's unique idempotency claim and Redis's atomic admission/publication remain the cross-process authorities. Verification: concurrent different-user submissions proceed independently when one dependency call is blocked, same-key concurrent submissions still produce one durable job and one queue publication across controller instances, request cancellation does not wait behind unrelated work, and `go test -race ./...` passes.
- **IMPLEMENTED (2026-07-18; owner: Phase 07 API/backend maintainer; evidence: `docs/implementation/preparation/task-222-preparation.md` and independent PASSED review `docs/implementation/reviews/task-222-review.md`):** make optimization idempotency hashing follow the documented normalized-request-body contract instead of hashing raw `ctx.Body()` bytes. Canonicalize the parsed request before hashing, define whether `excludedMealIds` is an order-independent set, and sort it when order has no product meaning. Verification: semantically equivalent JSON property order, whitespace, and numeric spelling replay the original acknowledgement; reordered exclusion IDs replay if exclusions are a set; actual changes to the diet ID, tolerance, or exclusion membership return `409`; and OpenAPI, backend tests, and frontend retry behavior agree.
- **IMPLEMENTED (2026-07-18; owner: Phase 07 API/backend maintainer; evidence: `docs/implementation/preparation/task-222-preparation.md` and independent PASSED review `docs/implementation/reviews/task-222-review.md`):** separate exact idempotency replay from failed-publication repair and define when current diet and entitlement state must be revalidated. The current handler reloads the saved diet before idempotency lookup, so an already accepted exact retry can perform unnecessary I/O or return a new `404`/dependency failure after the diet changes or is deleted instead of replaying its stored `202`. Preserve authenticated user scoping, but make the stored-response and repair semantics explicit. Verification: tests cover replay after diet deletion/change, entitlement change, transient diet-repository failure, queue-publication failure, and result expiry; exact replay performs no unintended new job or rate-count side effect; repair never publishes work for an invalid owner or input.
- **IMPLEMENTED (2026-07-18; owner: Phase 07 API/frontend maintainer; evidence: `docs/implementation/preparation/task-222-preparation.md` and independent PASSED review `docs/implementation/reviews/task-222-review.md`):** align optimization admission errors with the shared `AppError.category` contract. `optimizationAdmissionError` currently emits `category = rate_limit`, which is absent from `api/openapi.yaml`, generated `ErrorCategory`, and frontend category guards, so clients discard it and fall back by HTTP status. Choose an existing canonical category or add `rate_limit` consistently across the OpenAPI source, generated types, server classification, and clients. Verification: `429` active-job and hourly-limit envelopes validate against OpenAPI, generated-type drift passes, and frontend tests preserve the intended retryable category, code, message, and `Retry-After` behavior.
- **IMPLEMENTED (2026-07-18; owner: Phase 07 backend maintainer; evidence: `docs/implementation/preparation/task-222-preparation.md` and independent PASSED review `docs/implementation/reviews/task-222-review.md`):** remove the unused `replayed` parameter from `writeAcknowledgement`, unless replay metadata is intentionally added to a documented response field or header. Do not retain a parameter that implies behavior without affecting the acknowledgement. Verification: repository-wide search finds no unused replay argument, accepted and replayed acknowledgements retain the documented `202`, `Location`, job ID, status, and poll URL, and focused optimization controller tests pass.
- **IMPLEMENTED (2026-07-18; owner: Phase 07 backend/observability maintainer; evidence: `docs/implementation/preparation/task-223-preparation.md` and independent PASSED review `docs/implementation/reviews/task-223-review.md`):** make failed optimization admission-slot cleanup observable and bounded. `Submit` currently discards deferred `admission.Release` errors, which can leave a user's active slot unavailable until TTL expiry, while cleanup performed with `context.WithoutCancel` has no explicit local deadline. Use a short bounded cleanup context and emit sanitized fixed-vocabulary telemetry or logging without user, job, key, or body data. Verification: release failure does not replace the primary HTTP error, cleanup cannot block indefinitely, the failure is observable without PII or identifiers, and subsequent recovery/TTL behavior is covered.
- **IMPLEMENTED (2026-07-18; owner: Phase 07 backend maintainer; evidence: `docs/implementation/preparation/task-222-preparation.md` and independent PASSED review `docs/implementation/reviews/task-222-review.md`):** simplify the optimization controller's one-use validation and fallback helpers. Remove the forwarding `validateOptimizationJobID` wrapper and use the shared UUID validator directly in the route definition; remove the one-use `validUUIDString` boolean adapter; and update `validateUUIDValue` to accept only the value it validates instead of an ignored descriptive argument, while preserving rejection of malformed and nil UUIDs. Remove `stringValue`, whose failed assertion silently emits an empty `Location` header, and decode persisted optimization acknowledgements through one typed, validated structure shared by job-ID extraction and response writing. Verification: malformed or nil path/body UUIDs still return the documented `400`; malformed persisted `jobId`, `status`, or `pollUrl` returns a safe dependency/server error rather than `202` with incomplete headers; accepted and replayed acknowledgements retain their exact documented body and `Location`; repository-wide search finds none of the removed helpers; and focused optimization controller tests pass.
- **IMPLEMENTED (2026-07-18; owner: Phase 07 backend maintainer; evidence: `docs/implementation/preparation/task-222-preparation.md` and independent PASSED review `docs/implementation/reviews/task-222-review.md`):** simplify `setOptimizationRetryAfter` by using Go's built-in `max` to clamp the rounded `Retry-After` value to at least one second instead of a manual conditional branch. Preserve `math.Ceil` so positive fractional durations round up before clamping. Verification: focused tests cover zero, negative, sub-second, fractional-second, and whole-second durations; the header is always a positive base-10 integer with unchanged behavior; `gofmt` and focused optimization controller tests pass.
- **IMPLEMENTED (2026-07-18; owner: Phase 07 backend/queue maintainer; evidence: `docs/implementation/preparation/task-224-preparation.md` and independent PASSED review `docs/implementation/reviews/task-224-review.md`):** make `JobQueueManager` reservation cardinality match its API. `Config.BatchSize` accepts values greater than one, and both `XREADGROUP` and `XAUTOCLAIM` may then assign that many deliveries to the consumer, but `Reserve` returns and prepares only `messages[0]`; the remaining deliveries stay silently pending until reclamation. Either enforce a batch size of exactly one and remove the misleading configurability, or expose/process every returned delivery. `Reclaim` must also define partial-failure behavior instead of returning `nil` after earlier messages have already been claimed and had attempts incremented. Verification: multi-message read and reclaim tests prove that every claimed entry is returned, processed, or deliberately released, including a decode/Redis failure after an earlier valid message.
- **IMPLEMENTED (2026-07-18; owner: Phase 07 backend/queue maintainer; evidence: `docs/implementation/preparation/task-224-preparation.md` and independent PASSED review `docs/implementation/reviews/task-224-review.md`):** count retry attempts only after a worker acquires logical-job ownership, and update the counter and TTL atomically. `prepareDelivery` currently performs `INCR` before the done/lock checks, so duplicate deliveries and reservations that never invoke the processor consume the shared retry budget; a duplicate can therefore make the original job terminal after fewer than three real processing attempts. Its separate `INCR` and `EXPIRE` calls can also leave a consumed attempt without the intended TTL when the second command fails. Keep delivery count distinct from processing-attempt count and use one atomic Redis operation. Verification: competing duplicate entries do not reduce the processor's three-attempt budget, lock contention and already-completed deliveries consume no attempt, injected TTL failure leaves no partial counter state, and retry telemetry reflects actual processor invocations.
- **IMPLEMENTED (2026-07-18; owner: Phase 07 backend/queue maintainer; evidence: `docs/implementation/preparation/task-224-preparation.md` and independent PASSED review `docs/implementation/reviews/task-224-review.md`):** validate optimization queue job IDs as canonical, non-nil UUIDs at both enqueue and stream-decode boundaries, and fully remove malformed deliveries. The current whitespace/control-character check accepts arbitrary strings; the production processor and terminal handler later require `uuid.Parse`, so an invalid ID retries without useful work and the terminal handler can fail forever. The malformed-entry path only `XACK`s and leaves the entry in the stream. Verification: malformed, nil, noncanonical, and control-character IDs never reach the processor; malformed stream entries are atomically acknowledged/deleted with bounded sanitized telemetry; valid server-created UUIDs retain idempotent enqueue behavior.
- **IMPLEMENTED (2026-07-18; owner: Phase 07 backend/queue maintainer; evidence: `docs/implementation/preparation/task-224-preparation.md` and independent PASSED review `docs/implementation/reviews/task-224-review.md`):** validate queue timing as one coherent ownership contract. `validate` accepts any visibility timeout above 30 seconds even though DESIGN-004 permits 30 seconds of job work plus 5 seconds of terminal publication, and `lockTTL` expires one second before visibility, so accepted values such as 31 seconds allow concurrent processing. `CompletedTTL` and `AttemptTTL` also accept positive sub-second durations but are truncated to zero seconds for Lua `EX`, causing runtime Redis errors. Enforce the documented processing-plus-finalization margin and either require whole-second TTLs of at least one second or pass millisecond precision. Verification: boundary tests reject unsafe visibility/lock combinations, preserve the default 45/44-second window, and cover sub-second and exact-second TTL behavior without `EX 0`.
- **IMPLEMENTED (2026-07-18; owner: Phase 07 backend/queue maintainer; evidence: `docs/implementation/preparation/task-225-preparation.md` and independent PASSED review `docs/implementation/reviews/task-225-review.md`):** make terminal handling and cleanup explicit and fail-safe. `TerminalHandler` is optional even though exhausting retries without it finalizes the queue delivery as failed without publishing the authoritative job status; `Ack` always writes the logical value `completed` despite documenting any already-applied terminal update. Require the production terminal publisher where failure exhaustion is possible and model completed/failed acknowledgement deliberately. Replace the unbounded, error-discarding `releaseLock(context.Background(), ...)` defer with bounded observable cleanup, and make duplicate `XACK`/`XDEL` cleanup atomic so crashes do not retain acknowledged entries. Verification: missing terminal publication cannot silently discard failed work, completed and failed paths retain consistent queue/job state, lock-release failure is sanitized and observable without replacing the primary result, cleanup is time-bounded, and an injected crash cannot strand acknowledged duplicates.
- **IMPLEMENTED (2026-07-18; owner: Phase 07 backend/queue maintainer; evidence: `docs/implementation/preparation/task-225-preparation.md` and independent PASSED review `docs/implementation/reviews/task-225-review.md`):** remove or implement dead and misleading queue branches. `ErrJobInProgress` is never returned, yet `Run` handles it and contains a duplicated `continue`; `finalize` tests `result < 0` even though Redis `XACK` returns a non-negative count, making the claimed acknowledgement-failure branch unreachable while zero acknowledgements are silently accepted. Define the intended idempotent zero-ack behavior, return a meaningful structured script result if callers must distinguish it, and remove dead sentinels/branches. Verification: repository-wide search finds no unreachable queue condition or duplicate branch, and tests cover first finalization, idempotent replay, missing delivery, and lock contention.
- **IMPLEMENTED (2026-07-18; owner: Phase 07 backend/observability maintainer; evidence: `docs/implementation/preparation/task-226-preparation.md` and independent PASSED review `docs/implementation/reviews/task-226-review.md`):** correct queue age metrics so their names match what they measure. `OldestQueuedAge` is derived from the stream's first entry, which may already be pending, and `OldestPendingAge` recomputes age from the Redis stream ID using the worker's local clock instead of the pending record's Redis-reported idle duration. Separate waiting and pending populations, use authoritative Redis metadata where available, and define clock-skew behavior. Verification: fixtures containing queued-only, pending-only, mixed, empty, and skewed-clock states report accurate non-negative ages and bounded telemetry labels.
- **IMPLEMENTED (2026-07-18; owner: Phase 07 backend/platform maintainer; evidence: `docs/implementation/preparation/task-225-preparation.md` and independent PASSED review `docs/implementation/reviews/task-225-review.md`):** extract `finalizeScript`, `enqueueScript`, and `releaseLockScript` from `job_queue.go` into colocated, embedded `.lua` resources, preserving DESIGN-004 traceability and binary deployment. Consider `redis.NewScript` for cached `EVALSHA` execution with its normal fallback. Before doing so, decide and document Redis topology: the enqueue and finalize scripts each access two independently hashed keys and will fail with `CROSSSLOT` on Redis Cluster unless the stream and per-job keys share an intentional hash tag; if standalone Redis is the supported topology, enforce/document that constraint instead. Include any atomic attempt-counter and duplicate-ack operations introduced by the queue fixes in the same script layout. Verification: embedded-resource tests prove the shipped scripts are non-empty and executable, real-Redis integration tests preserve atomic/idempotent behavior, deployment contains no runtime script-file dependency, and the supported standalone or clustered topology is exercised explicitly.
- **IMPLEMENTED (2026-07-18; owner: Phase 07 backend/queue maintainer; evidence: `docs/implementation/preparation/task-225-preparation.md` and independent PASSED review `docs/implementation/reviews/task-225-review.md`):** define recovery after Redis loses or administratively deletes the optimization stream/group. Once `Bootstrap` sets its in-memory `bootstrapped` flag, the manager never checks again; a Redis restart without persistence or later group deletion makes subsequent commands fail with `NOGROUP` until the process restarts. Either document process restart as the required recovery mechanism or detect this state and safely recreate the group without hiding other Redis errors. Verification: a live manager recovers according to the documented policy after stream/group deletion and Redis restart, concurrent bootstrap remains idempotent, and genuine authorization/connectivity failures still fail closed.
- **IMPLEMENTED (2026-07-18; owner: Phase 07 backend/repository maintainer; evidence: `docs/implementation/preparation/task-216-preparation.md` and independent PASSED review `docs/implementation/reviews/task-216-review.md`):** give daily-diet mutation idempotency one canonical typed persistence contract instead of reusing checkout-specific names and permissively decoding two unrelated response shapes. `saved_diet_mutation_repository.go` stores daily-diet claims in `checkout_idempotency_keys` through `CheckoutIdempotencyRecord`, `checkoutIdempotencyClaimSQL`, and `scanCheckoutIdempotencyRecord`, even though the schema and helpers are now shared beyond checkout. Its repository decoder accepts either `dailyDietId` or `id`, silently prefers `dailyDietId` when both differ, and therefore disagrees with the service decoder, which accepts only `dailyDietId`; an existing row accepted by the repository can consequently fail when replayed by the service. Define a generic mutation-idempotency record/store or a deliberately daily-diet-specific boundary, decode one exact response type in one shared place, reject conflicting/unknown legacy shapes, and validate the persisted method, route, expected success status, body hash, and response ID before treating a row as replayable. This complements the existing action to persist the immutable original create response rather than only a resource reference. Verification: direct PostgreSQL repository tests cover first claim, exact replay, changed-body conflict, concurrent same-key claims across repository instances, independent keys, rollback after parent/entry/saved-item failure, malformed/empty/nil/dual-ID response bodies, wrong status/scope, account cascade, and ownership-aware deletion; no checkout-named daily-diet helper remains unless the storage is explicitly documented as a generic cross-feature table.
- **IMPLEMENTED (2026-07-18; owner: Phase 07 frontend/API maintainer; evidence: `docs/implementation/preparation/task-228-preparation.md` and independent PASSED review `docs/implementation/reviews/task-228-review.md`):** replace `daily-diet-client.ts`'s shallow response casts with one strict runtime decoder for the generated Daily Diet contract and enforce each endpoint's exact success status. `requireData` checks only that `data` is a non-array object (or that `data.diets` is an array), so it accepts the wrong envelope status, a missing/invalid request ID, and arbitrary malformed diet IDs, names, entries, units, quantities, positions, macro values, and timestamps as trusted `DailyDiet` state. Reads and mutations accept any successful 2xx response with valid-looking JSON, while deletion additionally accepts undocumented `200` although OpenAPI requires `204`. Decode the envelope discriminator and every required nested field against the OpenAPI bounds/vocabulary, reject non-finite numbers and invalid UUID/date values, and require `200` for list/get/replace, `201` for create, and `204` for delete. Verification: table-driven client tests reject wrong/missing status and request ID, null/extra/wrong-typed nested fields, malformed UUIDs/timestamps, unsupported units, invalid quantities/positions/macros, oversized collections, and every unexpected 2xx status while accepting exact valid empty/list/item/create/replace/delete responses.
- **IMPLEMENTED (2026-07-18; owner: Phase 07 frontend maintainer; evidence: `docs/implementation/preparation/task-228-preparation.md` and independent PASSED review `docs/implementation/reviews/task-228-review.md`):** make Daily Diet create retries actually reuse one idempotency key for the same user intent. `createDailyDiet` generates its key inside each invocation, and neither the controller nor `DailyDietCollection` retains it after an ambiguous timeout/network failure, so clicking “try again” submits a new key and can create a duplicate diet despite the documented retry-stable contract. Generate and retain the key at the controller/UI operation boundary until the request is known to have succeeded or the editable request changes; expose no persistent session data. Replace the `Date.now()`/`Math.random()` fallback with collision-resistant browser randomness or fail safely when secure randomness is unavailable. Verification: an accepted server write followed by a lost response reuses the identical key and produces one diet, repeated clicks while pending cannot create parallel intents, editing the request creates a new key, success/logout/account change clears retained retry state, secure-random fallback behavior is deterministic under tests, and no key is written to browser storage.
- **IMPLEMENTED (2026-07-18; owner: Phase 07 frontend/architecture maintainer; evidence: `docs/implementation/preparation/task-227-preparation.md` and independent PASSED review `docs/implementation/reviews/task-227-review.md`):** route Daily Diet API errors through one shared DESIGN-017 runtime-safe `ErrorMessageMapper` instead of locally trusting regex-shaped server fields. `safeErrorFromSource` accepts any runtime value for `retryable`, and `responseError` copies the top-level `requestId` using only a non-empty-string check, bypassing `isSafeRequestId`; `isSafeMessage` and `isSafeCode` are syntactic filters rather than a bounded code-to-message vocabulary, so infrastructure or secret-bearing text without a newline, URL, or the substring `" at "` can still reach the UI. The same validators are duplicated in the optimization client and can drift. Parse unknown error envelopes structurally, require a boolean retry flag and bounded request ID, map approved codes/statuses to fixed user-safe messages, and centralize the policy for all generated-contract clients while preserving the deliberate identical 403/404 Daily Diet projection. Verification: malformed category/code/message/retryable/request-ID values always fall back safely; stack traces, SQL/Redis/provider names, URLs, credentials, and oversized/control-character strings never render; approved validation/auth/dependency/rate-limit/timeout cases retain intended retryability; and Daily Diet plus optimization client tests consume the same mapper.
- **IMPLEMENTED (2026-07-18; owner: Phase 07 frontend maintainer; evidence: `docs/implementation/preparation/task-228-preparation.md` and independent PASSED review `docs/implementation/reviews/task-228-review.md`):** simplify the public and internal surface of `daily-diet-client.ts`. Remove the unused `fetchDailyDiet` alias and test-only `fetchDailyDiets` alias in favor of the canonical `getDailyDiet` and `listDailyDiets` names; remove the redundant `request` wrapper around `requestJson`; and replace the promise-chain/catch structure in `requireData`, whose per-operation fallback text is bypassed whenever `readJson` or the missing-data branch throws the existing `DailyDietClientError`. Fold JSON/envelope decoding into the strict shared decoder from the action above so there is one error path with deliberate operation context. Verification: repository-wide search finds no aliases or redundant wrapper/fallback parameter, exported controller/API behavior is unchanged, malformed responses retain the intended safe operation-specific error where required, and focused client/store/component tests pass.
- **IMPLEMENTED (2026-07-18; owner: Phase 07 frontend/API maintainer; evidence: `docs/implementation/preparation/task-230-preparation.md` and independent PASSED review `docs/implementation/reviews/task-230-review.md`):** replace `optimization-client.ts`'s type assertions and partial normalization with a strict runtime decoder for every discriminated optimization response, and enforce exact endpoint statuses. Submission currently checks only envelope/data object shape plus two status strings; it does not validate a nonempty bounded request ID, UUID job ID, or safe canonical poll URL, and it accepts any successful 2xx instead of exactly `202`. Polling does not require envelope status `ok` or exactly `200`; `normalizeJob` leaves common IDs, poll URL, timestamps, required/forbidden fields, and failed-job `failure` unchecked for most variants. `normalizeAlternatives` casts every meal without validation, permits an empty meal list, ignores the 100-meal bound, accepts negative/oversized macros, and accepts finite similarity scores outside `[0,1]`. Decode the OpenAPI union structurally, including exact required and forbidden fields for queued, processing, completed, failed, and cancelled states; validate UUIDs, timestamps, bounded relative poll URLs, failure-code vocabulary and fixed safe messages, meal quantities/units/positions, macro bounds, similarity bounds, and alternative cardinality; reject forbidden and additional fields rather than spreading untrusted objects into store state. Verification: table-driven tests cover exact valid acknowledgement and all five job variants plus wrong 2xx/envelope status, empty/unsafe request IDs and poll URLs, malformed IDs/dates, missing and cross-variant fields, unknown/unsafe failures, invalid alternative/meal/macro/similarity values, zero/one/three/over-limit cardinalities, and legacy calorie placement; controller tests prove no malformed payload can crash polling or render backend text.
- **IMPLEMENTED (2026-07-18; owner: Phase 07 frontend maintainer; evidence: `docs/implementation/preparation/task-230-preparation.md` and independent PASSED review `docs/implementation/reviews/task-230-review.md`):** make caller ownership of optimization submission idempotency explicit at the client boundary and remove the weak randomness fallback. The production optimization controller correctly creates, retains, and reuses one key for an ambiguous submission, but `submitOptimization` still permits omission and then silently creates a fresh key per direct invocation; direct callers can therefore defeat retry idempotency. Require the caller/controller to supply the key for submission, retain `generateOptimizationIdempotencyKey` only as the operation-level generator, and use collision-resistant browser randomness or fail safely when `crypto.randomUUID` is unavailable instead of `Date.now()` plus `Math.random()`. Verification: the client rejects or cannot compile without a caller-owned key, controller submission/retry tests reuse the same key after an ambiguous failure and rotate it only for a deliberate new submission, concurrent submit suppression remains intact, secure-random fallback behavior is tested, and keys remain memory-only and clear on diet change/disposal.
- **IMPLEMENTED (2026-07-18; owner: Phase 07 frontend/state maintainer; evidence: `docs/implementation/preparation/task-229-preparation.md` and independent PASSED review `docs/implementation/reviews/task-229-review.md`):** replace `stores/daily-diet.ts`'s single “latest operation wins” counter with an explicit serialized or independently coordinated load/mutation lifecycle, including cancellation where possible. Starting any load/create/replace/delete invalidates every earlier operation without aborting it: a load can re-enable mutation UI while a create is still running, an older successful server mutation can be silently discarded from local state, and a later load can commit a snapshot taken before that mutation. `clear` similarly suppresses responses but does not abort outstanding requests. Prevent duplicate/overlapping mutations, preserve or reconcile every confirmed server write, abort obsolete reads and identity-scoped requests, and define load-versus-mutation ordering rather than using one counter for unrelated work. Rollback must patch only the optimistic fields owned by the failed operation; the current replacement failure restores the complete pre-request snapshot and can erase a selection made while the request was pending, leaving the separate search selection inconsistent. Verification: deterministic deferred-promise tests cover load/load, load/create, create/load, replace/select/failure, replace/load, delete/load, clear/logout during every operation, duplicate button activation, abort propagation, and out-of-order success/failure, proving no server-confirmed mutation is lost and no newer user state is overwritten.
- **IMPLEMENTED (2026-07-18; owner: Phase 07 frontend/product maintainer; evidence: `docs/implementation/preparation/task-229-preparation.md` and independent PASSED review `docs/implementation/reviews/task-229-review.md`):** remove or redesign the misleading optimistic replacement projection in `stores/daily-diet.ts`. `optimisticReplace` immediately applies changed quantities/units while intentionally retaining the old `aggregateMacros` and timestamps; the UI renders those stale macros beside the edited diet and can feed them into `OptimizationWorkflow`, presenting server-authoritative targets that no longer match the displayed entries. Prefer waiting for the server DTO, or represent a distinct pending draft that cannot be selected/optimized and does not masquerade as authoritative persisted state. Verification: during replacement the UI never pairs edited entries with stale aggregate macros, optimization cannot start from a pending projection, success installs only the decoded server DTO, failure preserves the last authoritative diet, and component/store tests cover quantity, unit, name-only, selected-diet, and failed replacement cases.
- **IMPLEMENTED (2026-07-18; owner: Phase 07 frontend/state maintainer; evidence: `docs/implementation/preparation/task-229-preparation.md` and independent PASSED review `docs/implementation/reviews/task-229-review.md`):** make saved-diet selection one coherent source of truth across `dailyDietStore` and the Daily Diet Alternative search state. `select` updates `dailyDietStore.selectedId` but `setDailyDietId` conditionally ignores the projection outside `daily_diet_alternative` mode; later entering that mode can therefore show a selected diet while search requests have no `dailyDietId`. Conversely, `load` clears a selection that disappeared from the server through `retainSelection` without clearing the search-store projection. Derive the search request from the authoritative selected collection or centralize all selection/mode transitions in one owner, including load reconciliation, deletion, clear/logout, and mode changes. Verification: tests cover selection before/after entering alternative mode, selected diet disappearing on reload, deletion of selected and unselected diets, empty reload, logout/account switch, and Catalog/Substitution round trips; UI indication, optimization input, and emitted search request always contain the same current diet ID without disturbing other mode-owned state.
- **IMPLEMENTED (2026-07-18; owner: Phase 07 frontend/state maintainer; evidence: `docs/implementation/preparation/task-231-preparation.md` and independent PASSED review `docs/implementation/reviews/task-231-review.md`):** correct the retry state machine in `stores/optimization.ts` so retry UI and behavior follow the actual error and current user input. `setSubmissionFailure` assigns `new_submission` to every non-reusable outcome, including non-retryable authentication, entitlement, validation, and unknown failures, so the UI offers “Try again” and immediately resubmits them. Terminal `solver_infeasible` is marked non-retryable by `displayFailure` but still receives `new_submission`; `retry` then uses the captured pending request, so changing tolerance as instructed and pressing “Try again” resubmits the old tolerance with a new idempotency key. Define an explicit code/category-to-action policy: ambiguous submission reuses the same key, transient polling resumes the known job, timeout/worker failures may deliberately create a new job, expired/not-found results require a new current request, and non-retryable outcomes expose no blind retry. Either make new-submission retry accept the current form request or require the main submit action after edits; never silently replay stale inputs. Verification: tests cover network ambiguity before/after acknowledgement, poll timeout, queue outage, result expiry/not-found, validation, infeasible, auth, entitlement, cancellation, worker failure, edited tolerance/exclusions, and repeated retry clicks, asserting the exact job/key/request reuse or rotation and matching button visibility.
- **IMPLEMENTED (2026-07-18; owner: Phase 07 frontend/state maintainer; evidence: `docs/implementation/preparation/task-231-preparation.md` and independent PASSED review `docs/implementation/reviews/task-231-review.md`):** make optimization controller disposal and authenticated identity changes leave the shared store resumable and free of prior-user state. `OptimizationWorkflow` creates a new controller over the module-global `optimizationStore`; disposing during submission/polling aborts the local request and clears its private pending state but deliberately leaves the store in `submitting`, `queued`, or `processing`. Remounting for the same selected diet initializes a controller from that busy store, `setDiet` returns early, and submission remains disabled forever with no pending operation capable of resuming polling. Completed/failed jobs and alternatives also remain in the global store across unmount/logout until a different diet ID happens to clear them. Use one lifecycle owner or a component-local store, and on disposal either reset identity-owned state or retain enough validated job metadata to resume polling explicitly; wire logout/account change to unconditional optimization-state clearing even if a diet UUID coincides. Verification: unmount/remount during submit, queued and processing states resumes or resets deliberately; terminal state retention follows a documented policy; logout/account switch clears job IDs, alternatives, failures, pending keys and polls; two controller instances cannot race over one store; and no remount remains permanently busy without an active operation.
- **IMPLEMENTED (2026-07-18; owner: Phase 07 frontend maintainer; evidence: `docs/implementation/preparation/task-231-preparation.md` and independent PASSED review `docs/implementation/reviews/task-231-review.md`):** make optimization polling configuration and delay cleanup robust. The exported controller accepts zero/negative/non-finite `maxPolls` and delay values without validation, producing immediate timeout or effectively unbounded/rapid polling behavior. `wait` adds one abort listener for every delay but never removes it when the timer resolves, so a normal 60-poll run retains all listeners on the active signal until a later abort or controller lifetime end. Validate a positive bounded poll count and a nonempty finite nonnegative delay schedule, and remove the abort listener on both timer completion and rejection while settling the promise exactly once. Verification: fake-timer tests cover empty, zero, negative, non-finite and excessive configuration, normal completion listener cleanup, abort before/after timer completion, maximum poll count, stable delay selection, and absence of duplicate resolution/rejection or retained listeners.

### Testing coverage deviations

- **ACCEPTED EXCEPTION (Task 235, remeasured 2026-07-20; owner: Phase 07.01 implementation maintainer; evidence: `backend/coverage.out` generated by `go test ./internal/... -p 1 -count=1 -coverprofile=coverage.out`, then `go tool cover -func=coverage.out`; focused evidence in Tasks 213-234 preparation/reviews):** aggregate internal coverage is `87.4%`; isolated package totals are `internal/dailydiet 82.2%`, `internal/optimization 84.2%`, `internal/queue 76.1%`, and `internal/worker 67.4%`. The tables below are the complete 109-row below-100% function inventory (107 unique file/function pairs; declaration lines distinguish functions with the same name). “Uncovered” records zero-count statement ranges from the same profile, not an estimate.

  Rationale codes are explicit acceptance limits: **D** = injected dependency/I/O/cleanup failure after the observable failure contract is covered; **V** = defensive invalid or corrupt state rejected at another validated boundary; **A** = compatibility/convenience adapter not selected by production assembly while its delegate is covered; **C** = OS cancellation/deadline/process race while bounded timeout/cleanup behavior is covered; **P** = malformed parser/model/output guard while authoritative valid and hostile-input contracts are covered; **T** = telemetry/readiness fallback with privacy, cardinality, and availability behavior covered; **X** = exhaustive typed-state fallback unreachable from validated production input. These exceptions waive only the listed statements, not Task 213-234 acceptance behavior. The aggregate checker now requires every below-100 row's exact file, declaration line, function, and percentage to occur in this section; a new row or changed percentage fails the gate instead of being hidden by a package total.

  | File-qualified function                                 | Coverage | Uncovered statement lines                | Rationale                                                                                                                      |
  | ------------------------------------------------------- | -------: | ---------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------ |
  | `internal/dailydiet/service.go:128 Create`              |  `78.1%` | Current `coverage.out` zero-count ranges | D: repository/idempotency claim, projection, and cleanup failures; durable replay/concurrency behavior is integration-covered. |
  | `internal/dailydiet/service.go:179 Get`                 |  `66.7%` | Current `coverage.out` zero-count ranges | D: repository read failure; ownership-safe not-found/success projection is covered.                                            |
  | `internal/dailydiet/service.go:188 List`                |  `78.6%` | Current `coverage.out` zero-count ranges | D: repository list and per-record load failures; normal and empty collections are covered.                                     |
  | `internal/dailydiet/service.go:212 Replace`             |  `70.6%` | Current `coverage.out` zero-count ranges | D/V: repository and corrupt persisted-entry failures; immutable replay and authoritative replacement are covered.              |
  | `internal/dailydiet/service.go:241 Delete`              |  `70.0%` | Current `coverage.out` zero-count ranges | D: repository lookup/delete failures; ownership-safe deletion is covered.                                                      |
  | `internal/dailydiet/service.go:260 load`                |  `83.3%` | Current `coverage.out` zero-count ranges | D: persisted-diet lookup failure; successful hydration is covered.                                                             |
  | `internal/dailydiet/service.go:273 validateFoodObjects` |  `71.4%` | Current `coverage.out` zero-count ranges | D/V: food-object provider failure or missing authoritative meal/item; valid mixed-object lookup is covered.                    |
  | `internal/dailydiet/service.go:288 prepareCreate`       |  `96.7%` | Current `coverage.out` zero-count ranges | V: impossible duplicate/missing hydrated food object after validated lookup; request bounds are covered.                       |
  | `internal/dailydiet/service.go:341 project`             |  `95.0%` | Current `coverage.out` zero-count ranges | V: corrupt persisted entry missing its authoritative food object; valid macro projection is covered.                           |
  | `internal/dailydiet/service.go:378 normalizeRequest`    |  `63.2%` | Current `coverage.out` zero-count ranges | V: nil, blank, duplicate-position, unsupported-unit, and invalid-quantity guards; accepted canonical requests are covered.     |
  | `internal/dailydiet/service.go:433 foodObjectNutrition` |  `70.0%` | Current `coverage.out` zero-count ranges | D/V: authoritative meal/item lookup failures and unsupported object kinds; valid mixed-object nutrition projection is covered. |
  | `internal/dailydiet/service.go:454 quantityInMealBase`  |  `84.6%` | Current `coverage.out` zero-count ranges | V: unsupported or cross-basis unit guards; canonical metric/imperial conversion is covered.                                    |
  | `internal/dailydiet/service.go:479 validateIdentity`    |  `60.0%` | Current `coverage.out` zero-count ranges | V: malformed authenticated/resource UUID guards; authenticated route identity is covered.                                      |
  | `internal/dailydiet/service.go:501 requestHash`         |  `80.0%` | Current `coverage.out` zero-count ranges | D: JSON hash serialization failure for an already normalized request; stable hash/replay is covered.                           |

  | File-qualified function                                                 | Coverage | Uncovered statement lines                                                 | Rationale                                                                                                                                                                                                     |
  | ----------------------------------------------------------------------- | -------: | ------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
  | `internal/optimization/clp_wrapper.go:109 Error`                        |  `41.2%` | `110-112,115-130,139`                                                     | X: exhaustive formatting variants of the closed solver-error kind; safe public projection is covered.                                                                                                         |
  | `internal/optimization/clp_wrapper.go:144 Unwrap`                       |  `92.3%` | `167-168`                                                                 | X: no-cause solver error variant; wrapped timeout/cancellation paths are covered.                                                                                                                             |
  | `internal/optimization/clp_wrapper.go:207 Solve`                        |  `81.1%` | `208-213,215-217,219-221,224-226,231-233,258-260,263-268,291-292`         | C/D/P: temp-file, child start/wait, output-read, cleanup, and malformed-result failures; real CLP success, timeout, cancellation, bounded output, and cleanup are covered.                                    |
  | `internal/optimization/clp_wrapper.go:298 CheckVersion`                 |  `68.2%` | `299-301,303-305,311-318,320-322`                                         | C/P: executable start, deadline, malformed output, and wrong-version guards; packaged exact-version readiness is covered.                                                                                     |
  | `internal/optimization/clp_wrapper.go:332 validatedConfig`              |  `78.3%` | `333-335,340-342,346-348,352-354,362-364`                                 | V: invalid executable, deadline, output, model, and version configuration guards; production defaults are covered.                                                                                            |
  | `internal/optimization/clp_wrapper.go:395 serializeLPWithLimit`         |  `88.7%` | `396-398,401-406,411-413,417-419,431-433,470-472`                         | P/V: malformed model names, dimensions, bounds, and serialization-limit guards; deterministic bounded model output is covered.                                                                                |
  | `internal/optimization/clp_wrapper.go:530 writeExpression`              |  `94.1%` | `551-553`                                                                 | P: empty/non-finite expression guard; valid objective/constraint expressions are covered.                                                                                                                     |
  | `internal/optimization/clp_wrapper.go:623 parseCLPSolutionLine`         |  `85.4%` | `628-630,635-637,648-650,657-659,664`                                     | P: malformed token/count/number/index guards; exact hostile and valid CLP grammar is covered.                                                                                                                 |
  | `internal/optimization/clp_wrapper.go:705 Write`                        |  `77.8%` | `714-716`                                                                 | D: bounded writer overflow tail after limit is reached; truncation and sanitization are covered.                                                                                                              |
  | `internal/optimization/clp_wrapper.go:734 readBoundedFile`              |  `80.0%` | `741-746`                                                                 | D: output-file open/read/close failure; bounded successful read is covered.                                                                                                                                   |
  | `internal/optimization/constraints.go:109 BuildConstraints`             |  `75.0%` | Current `coverage.out` zero-count ranges                                  | A: compatibility wrapper error return; production saved-diet loader uses the indexed builder and is covered.                                                                                                  |
  | `internal/optimization/constraints.go:120 buildConstraintsFromIndex`    |  `84.3%` | Current `coverage.out` zero-count ranges                                  | V: empty/duplicate/missing meal and invalid macro/model guards; canonical eligible meal construction is covered.                                                                                              |
  | `internal/optimization/constraints.go:209 BuildFromSavedDiet`           |   `0.0%` | Current `coverage.out` zero-count ranges                                  | A: convenience wrapper not used by production assembly; `LoadFromSavedDiet` and its builder delegate are covered.                                                                                             |
  | `internal/optimization/constraints.go:221 LoadFromSavedDiet`            |  `81.0%` | Current `coverage.out` zero-count ranges                                  | D/V: repository paging, missing diet food object, invalid basis, and load failure guards; authoritative loading and pagination are covered.                                                                   |
  | `internal/optimization/constraints.go:346 targetForRequest`             |  `84.0%` | Current `coverage.out` zero-count ranges                                  | V: absent/invalid target source and incompatible macros; saved-diet target derivation is covered.                                                                                                             |
  | `internal/optimization/constraints.go:385 quantityInNutritionBasis`     |  `64.7%` | Current `coverage.out` zero-count ranges                                  | V: nil, unsupported, non-positive, and cross-basis quantity guards; metric/imperial equivalence is covered.                                                                                                   |
  | `internal/optimization/constraints.go:418 validateMacroTarget`          |  `60.0%` | Current `coverage.out` zero-count ranges                                  | V: non-finite/negative target guard; accepted target constraints are covered.                                                                                                                                 |
  | `internal/optimization/constraints.go:490 buildAlternativeConstraints`  |  `78.3%` | Current `coverage.out` zero-count ranges                                  | V: invalid diversity/model dimensions and exclusions; bounded alternative construction is covered.                                                                                                            |
  | `internal/optimization/diversity.go:43 Apply`                           |  `81.8%` | `44-46,50-52`                                                             | V: invalid model/count guard; distinct-alternative generation is covered.                                                                                                                                     |
  | `internal/optimization/diversity.go:110 generateAlternativePipeline`    |  `80.0%` | `114-116,120-122,131-133,135-137,139-141,147-149,151-153`                 | D/V: solver failure, invalid returned solution, deduplication, and exhausted-candidate branches; partial-result policy is covered.                                                                            |
  | `internal/optimization/diversity.go:166 solveObjectivePolicy`           |  `83.3%` | `178-180,183-185,194-196,198-200`                                         | D/V: primary/secondary solve failure and invalid objective result; calorie-first policy is covered.                                                                                                           |
  | `internal/optimization/diversity.go:209 hasPositiveCoefficient`         |  `75.0%` | `215`                                                                     | X: no positive coefficient fallback after validated objective construction.                                                                                                                                   |
  | `internal/optimization/diversity.go:220 objectiveValueForSolution`      |  `80.0%` | `225-227,230-232`                                                         | V: coefficient/solution dimension and non-finite result guards; deterministic objective values are covered.                                                                                                   |
  | `internal/optimization/diversity.go:239 canonicalSolution`              |  `75.0%` | `242-247`                                                                 | V: non-finite or negative solver quantity guard; canonical rounding/deduplication is covered.                                                                                                                 |
  | `internal/optimization/diversity.go:277 solutionSatisfiesModel`         |  `85.0%` | `281-283,285-287,294-296`                                                 | V: malformed model dimensions, invalid values, and constraint violation guards; valid model evaluation is covered.                                                                                            |
  | `internal/optimization/validator.go:85 UnmarshalJSON`                   |  `90.0%` | Current `coverage.out` zero-count ranges                                  | V: malformed closed failure-code JSON; accepted vocabulary producers/consumers are covered.                                                                                                                   |
  | `internal/optimization/validator.go:111 Error`                          |  `66.7%` | Current `coverage.out` zero-count ranges                                  | X: validation error without diagnostics; safe mapped diagnostics are covered.                                                                                                                                 |
  | `internal/optimization/validator.go:152 UnmarshalJSON`                  |   `0.0%` | Current `coverage.out` zero-count ranges                                  | A/V: direct alternative JSON adapter is not used by production decode; strict API/job decoders and validation are covered.                                                                                    |
  | `internal/optimization/validator.go:184 ValidateDietAlternative`        |  `90.0%` | Current `coverage.out` zero-count ranges                                  | V: nil alternative guard; populated alternatives are covered.                                                                                                                                                 |
  | `internal/optimization/validator.go:279 Validate`                       |  `79.0%` | Current `coverage.out` zero-count ranges                                  | V: nil, duplicate, malformed quantity/macro/similarity, and bounds guards; nominal and hostile alternatives are covered.                                                                                      |
  | `internal/optimization/validator.go:376 macroSimilarity`                |  `70.0%` | Current `coverage.out` zero-count ranges                                  | Shared DESIGN-003 normalization failures and the defensive invalid-score guard remain outside direct package fixtures; bounded nontrivial fixtures verify identical, mixed, and disjoint-item macro profiles. |
  | `internal/optimization/validator.go:414 safeOptimizationFailure`        |  `78.6%` | Current `coverage.out` zero-count ranges                                  | X/V: nil, malformed, or unknown failure fallback; closed safe failure vocabulary is covered.                                                                                                                  |
  | `internal/optimization/validator.go:494 macroWithinTolerance`           |   `0.0%` | Current `coverage.out` zero-count ranges                                  | V: compatibility tolerance helper is retained for internal callers; projection-aware validation is covered.                                                                                                   |
  | `internal/optimization/validator.go:501 macroWithinProjectionTolerance` |  `66.7%` | Current `coverage.out` zero-count ranges                                  | V: invalid target/tolerance/projection-margin guard; valid projected boundary tolerances are covered.                                                                                                         |
  | `internal/optimization/validator.go:520 isPublishedQuantity`            |  `75.0%` | Current `coverage.out` zero-count ranges                                  | V: non-finite and out-of-range published quantity guards; bounded quantized quantities are covered.                                                                                                           |
  | File-qualified function                                                 | Coverage | Uncovered statement lines                                                 | Rationale                                                                                                                                                                                                     |
  | ---                                                                     |     ---: | ---                                                                       | ---                                                                                                                                                                                                           |
  | `internal/queue/job_queue.go:169 NewJobQueueManager`                    |  `85.0%` | `171-179`                                                                 | V: nil client and invalid configuration guards; configured manager construction is covered.                                                                                                                   |
  | `internal/queue/job_queue.go:203 Config`                                |  `66.7%` | `204-206`                                                                 | A: defensive nil-receiver snapshot; configured manager reporting is covered.                                                                                                                                  |
  | `internal/queue/job_queue.go:223 Bootstrap`                             |  `84.6%` | `224-229`                                                                 | D: Redis group/bootstrap failure; idempotent and recovery bootstrap are real-Redis covered.                                                                                                                   |
  | `internal/queue/job_queue.go:249 Enqueue`                               |  `71.4%` | `250-255,263-268`                                                         | V/D: malformed job and Redis publish failure; canonical enqueue and duplicate policy are covered.                                                                                                             |
  | `internal/queue/job_queue.go:276 Reserve`                               |  `60.0%` | `277-282,288-290,293-297,302-306,308-310`                                 | D/V: cancellation, Redis reservation error, malformed delivery, and cardinality guards; owned reservation is covered.                                                                                         |
  | `internal/queue/job_queue.go:322 Reclaim`                               |  `58.3%` | `323-334,336-338`                                                         | D/V: cancellation, partial Redis reclaim failure, and malformed result guards; stale delivery reclaim is covered.                                                                                             |
  | `internal/queue/job_queue.go:361 Process`                               |  `74.1%` | `362-373,376-378,385-387,392-394,400-405,408-410,418-420,423-425,442-447` | D/V: lock miss/error, attempt/finalize/ack cleanup failures, and malformed delivery branches; ownership-first attempts and terminal publication are covered.                                                  |
  | `internal/queue/job_queue.go:465 ackPublished`                          |  `57.1%` | `466-474`                                                                 | D: atomic acknowledgement/deletion Redis failure; successful duplicate cleanup is covered.                                                                                                                    |
  | `internal/queue/job_queue.go:481 Run`                                   |  `64.7%` | `482-487,496-497,499-502,504-506`                                         | C/D: cancellation, reserve/reclaim error, and retry-loop timing; live recovery and shutdown behavior are covered.                                                                                             |
  | `internal/queue/job_queue.go:513 Stats`                                 |  `70.7%` | `514-519,521-523,525-529,531-533,535-537,547-549,557-559,563-568`         | D/T: Redis stream/group/pending metadata failures and empty metadata; accurate waiting/pending age fixtures are covered.                                                                                      |
  | `internal/queue/job_queue.go:580 oldestPendingIdle`                     |  `84.6%` | `589-591,600`                                                             | D/V: Redis pending detail failure or absent minimum; mixed/empty/skewed ages are covered.                                                                                                                     |
  | `internal/queue/job_queue.go:606 claim`                                 |  `60.0%` | `609-611,614-618`                                                         | D: lock acquisition and Redis failure; contention/ownership behavior is covered.                                                                                                                              |
  | `internal/queue/job_queue.go:674 prepareDelivery`                       |  `85.7%` | `678-680`                                                                 | V: malformed/canonical UUID cleanup branch; malformed delivery regression is covered.                                                                                                                         |
  | `internal/queue/job_queue.go:717 countAttempt`                          |  `75.0%` | `719-721`                                                                 | D: atomic attempt-script failure; three genuine attempts and TTL retention are covered.                                                                                                                       |
  | `internal/queue/job_queue.go:727 streamValueString`                     |  `75.0%` | `731-732`                                                                 | V: unsupported Redis stream value type; canonical string/byte values are covered.                                                                                                                             |
  | `internal/queue/job_queue.go:740 finalize`                              |  `64.3%` | `741-746,748-752`                                                         | D/V: invalid terminal state and atomic finalization failure; completed/failed publication is covered.                                                                                                         |
  | `internal/queue/job_queue.go:773 doneState`                             |  `77.8%` | `778-780,782-784`                                                         | D: terminal-state lookup/decoding failure; duplicate terminal detection is covered.                                                                                                                           |
  | `internal/queue/job_queue.go:837 validate`                              |  `72.7%` | `838-846`                                                                 | V: nil client and incoherent timing/TTL configuration; accepted boundaries are covered.                                                                                                                       |
  | `internal/queue/job_queue.go:858 contextError`                          |  `66.7%` | `859-861`                                                                 | X: nil-context fallback after Redis error; cancellation projection is covered.                                                                                                                                |
  | `internal/queue/job_queue.go:873 isBusyGroup`                           |  `66.7%` | `874-876`                                                                 | D: non-Redis or differently coded bootstrap error; busy-group idempotency is covered.                                                                                                                         |
  | `internal/queue/job_queue.go:888 streamHashTag`                         |  `75.0%` | `890-892,895-897`                                                         | V: malformed Redis hash-tag key topology; validated standalone/cluster key derivation is covered.                                                                                                             |
  | `internal/queue/job_queue.go:903 lockTTL`                               |  `75.0%` | `905-907`                                                                 | V: sub-millisecond/overflow duration fallback; accepted timing contract is covered.                                                                                                                           |
  | `internal/queue/job_queue.go:913 streamEntryTime`                       |  `71.4%` | `915-917,919-921`                                                         | V: malformed stream ID timestamp; valid Redis IDs are covered.                                                                                                                                                |
  | `internal/queue/job_queue.go:927 streamEntryAge`                        |  `85.7%` | `929-931`                                                                 | V: future timestamp clock-skew clamp; nonnegative age behavior is covered.                                                                                                                                    |

  | File-qualified function                                                              | Coverage | Uncovered statement lines                                                 | Rationale                                                                                                                                                                               |
  | ------------------------------------------------------------------------------------ | -------: | ------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
  | `internal/worker/optimization_admission.go:87 NewRedisOptimizationAdmissionGate`     |  `57.1%` | `88-96`                                                                   | V: nil Redis and invalid limit/TTL configuration; production gate construction is covered.                                                                                              |
  | `internal/worker/optimization_admission.go:102 Acquire`                              |  `78.6%` | `103-105,111-113,117-119,129-132,134-136`                                 | D/V: invalid request, Redis reservation/counter failure, and rollback failure; concurrency/rate behavior is covered.                                                                    |
  | `internal/worker/optimization_admission.go:144 Release`                              |  `77.3%` | `145-147,152-157,169-171,175`                                             | D/V: invalid ownership, compare-delete failure, timeout, and telemetry fallback; bounded cleanup is covered.                                                                            |
  | `internal/worker/optimization_admission.go:180 existingDecision`                     |  `76.5%` | `182-184,186-188,190-192,200-202`                                         | D/V: persisted acknowledgement load/decode/state failures; exact replay/repair policy is covered.                                                                                       |
  | `internal/worker/optimization_admission.go:208 validate`                             |  `55.6%` | `209-220`                                                                 | V: missing dependency and incoherent admission configuration; accepted production config is covered.                                                                                    |
  | `internal/worker/optimization_admission.go:245 admissionUnavailable`                 |   `0.0%` | `245-247`                                                                 | A: convenience error constructor not selected by current controller path; typed dependency projection is covered.                                                                       |
  | `internal/worker/optimization_processor.go:94 Error`                                 |   `0.0%` | `94`                                                                      | A: diagnostic-only error string adapter; public safe failure mapping is covered.                                                                                                        |
  | `internal/worker/optimization_processor.go:130 NewRedisOptimizationJobStoreWithTTL`  |  `66.7%` | `131-133`                                                                 | A/V: alternate TTL constructor invalid-input branch; production store construction is covered.                                                                                          |
  | `internal/worker/optimization_processor.go:140 WithTelemetry`                        |   `0.0%` | `140-144`                                                                 | A: optional fluent telemetry adapter not used by production assembly; configured telemetry emission is covered.                                                                         |
  | `internal/worker/optimization_processor.go:150 Save`                                 |  `70.0%` | `151-159`                                                                 | D/V: invalid state, serialization, and Redis write failure; durable queued save is covered.                                                                                             |
  | `internal/worker/optimization_processor.go:169 Load`                                 |  `68.0%` | `170-175,180-182,187,189-192,194-196`                                     | D/V: invalid ID, Redis miss/error, malformed state, and owner mismatch; valid owned load is covered.                                                                                    |
  | `internal/worker/optimization_processor.go:210 MarkProcessing`                       |  `73.3%` | `212-217,224-229`                                                         | D/V: invalid transition, serialization, and Redis transaction failure; queued-to-processing flow is covered.                                                                            |
  | `internal/worker/optimization_processor.go:236 PublishCompleted`                     |  `85.7%` | `238-240,244-246,260-262`                                                 | D/V: invalid result, serialization, and durable terminal write failure; completed publication is covered.                                                                               |
  | `internal/worker/optimization_processor.go:269 PublishFailed`                        |  `78.3%` | `271-273,277-285,296-298`                                                 | D/V: invalid closed failure, serialization, and durable terminal write failure; safe failed publication is covered.                                                                     |
  | `internal/worker/optimization_processor.go:305 requireDurableTerminalStatus`         |  `66.7%` | `309`                                                                     | D: terminal persistence failure after retry; fail-safe no-ack policy is covered.                                                                                                        |
  | `internal/worker/optimization_processor.go:314 Delete`                               |   `0.0%` | `314-324`                                                                 | A: cleanup adapter not selected by the retained-result production lifecycle; TTL expiry behavior is covered.                                                                            |
  | `internal/worker/optimization_processor.go:329 write`                                |   `0.0%` | `329-332`                                                                 | A: internal convenience wrapper bypassed by transactional transition paths; those writes are covered.                                                                                   |
  | `internal/worker/optimization_processor.go:336 transition`                           |  `71.4%` | `337-339,341-343,349-354`                                                 | D/V: nil mutation, missing/corrupt prior state, and Redis transaction failure; valid transitions are covered.                                                                           |
  | `internal/worker/optimization_processor.go:360 durationMilliseconds`                 |  `75.0%` | `362-364`                                                                 | V: non-positive/sub-millisecond duration clamp; configured TTL/deadlines are covered.                                                                                                   |
  | `internal/worker/optimization_processor.go:384 NewRepositoryOptimizationInputLoader` |   `0.0%` | `384-386`                                                                 | A: convenience constructor bypassed by dependency-injected production assembly; loader behavior is covered through that assembly.                                                       |
  | `internal/worker/optimization_processor.go:390 Load`                                 |   `0.0%` | `390-397`                                                                 | A: direct repository-loader adapter is covered indirectly by integrated job processing; no separate product behavior is waived.                                                         |
  | `internal/worker/optimization_processor.go:457 ProcessOptimizationJob`               |  `74.5%` | `458-460,465-467,469-471,476-481,489-491,498-500,502-507,519-524,526-534` | D/C/V: invalid job, input-load, deadline, solver, validation, and terminal-publication failures; nominal, timeout, cancellation, partial-result, and durable failure flows are covered. |
  | `internal/worker/optimization_processor.go:540 Process`                              |   `0.0%` | `540-542`                                                                 | A: queue processor adapter delegates to `ProcessOptimizationJob`; queue-to-worker integration covers the delegate.                                                                      |
  | `internal/worker/optimization_processor.go:546 Terminal`                             |   `0.0%` | `546-574`                                                                 | A/D: queue terminal adapter's duplicate/load/publication failure variants; authoritative terminal persistence is covered through worker/queue integration.                              |
  | `internal/worker/optimization_processor.go:580 handleProcessingError`                |  `82.4%` | `583-585,590-592,596-598`                                                 | C/D: cancellation, terminal write, and admission-release failure; timeout/shutdown/retry policy is covered.                                                                             |
  | `internal/worker/optimization_processor.go:627 confirmTerminal`                      |   `0.0%` | `627-634`                                                                 | A/D: post-publication confirmation adapter; durable terminal requirement is covered by queue/worker regression tests.                                                                   |
  | `internal/worker/optimization_processor.go:648 solverTelemetryStatus`                |  `63.6%` | `655-657,659-664`                                                         | T/X: exhaustive wrapped solver-error classifications; bounded status allowlist and timeout labels are covered.                                                                          |
  | `internal/worker/optimization_processor.go:672 telemetryStatusForFailure`            |  `83.3%` | `682-683`                                                                 | T/X: unknown closed-vocabulary fallback; every retained failure status is covered.                                                                                                      |
  | `internal/worker/optimization_processor.go:689 validateOptimizationJob`              |  `65.2%` | `690-698,701-702,708-710,713-715,717-719,721-723`                         | V: malformed owner/diet/tolerance/exclusion/request identifiers; controller-created valid jobs and hostile persisted jobs are covered.                                                  |
  | `internal/worker/optimization_processor.go:745 validateJobID`                        |  `66.7%` | `746-748`                                                                 | V: malformed/non-canonical UUID guard; canonical queue IDs are covered.                                                                                                                 |
  | `internal/worker/optimization_processor.go:772 queueUnavailable`                     |   `0.0%` | `772-774`                                                                 | A: convenience projection bypassed by typed terminal failure construction; queue-unavailable public behavior is covered.                                                                |
  | `internal/worker/readiness.go:24 OptimizationWorkerPing`                             |   `0.0%` | `24-37`                                                                   | A/T: exported readiness adapter is exercised through app/local-stack readiness rather than package coverage; Redis heartbeat and CLP version readiness pass.                            |
  | `internal/worker/readiness.go:43 startWorkerHeartbeat`                               |  `85.0%` | `44-49,60-61`                                                             | T/D: disabled/nil heartbeat and periodic Redis write failure; normal heartbeat lifecycle is covered.                                                                                    |
  | `internal/worker/readiness.go:76 writeWorkerHeartbeat`                               |  `66.7%` | `78-83`                                                                   | T/D: Redis heartbeat write/expiry failure; successful readiness signal is local-stack covered.                                                                                          |
  | `internal/worker/worker.go:39 runWithProcessor`                                      |  `76.2%` | `40-42,53-58,64-66,68-70`                                                 | C/D: nil queue, cancellation, reserve/reclaim, and terminal processing failures; worker loop/recovery is covered.                                                                       |
  | `internal/worker/worker.go:79 ProcessOptimizationJob`                                |   `0.0%` | `79-89`                                                                   | A: public compatibility adapter delegates to configured processor; application worker integration covers the configured path.                                                           |

- **ACCEPTED EXCEPTION (Task 235, remeasured 2026-07-20; owner: Phase 07.01 frontend maintainer; evidence: `bun test --coverage`, 459 tests/2,099 expectations, including seven passing `src/lib/units.test.ts` cases):** `All files | 94.69% funcs | 95.13% lines`. The executable Phase 07 inventory is `src/lib/api/daily-diet-client.ts`, `error-message-mapper.ts`, `generated.ts`, `optimization-client.ts`, `search-client.ts`, `src/lib/stores/daily-diet.ts`, `optimization.ts`, `search.ts`, `selected-daily-diet.ts`, and `src/lib/units.ts`; the broader measured inventory also includes `src/lib/alternative-diet-save.ts` (`100.00% funcs, 95.56% lines`, uncovered `57-58`; bounded duplicate-name exhaustion guard). Exact below-100 Phase 07 rows are `daily-diet-client.ts` (`95.83% funcs, 95.26% lines`, uncovered `292-302`; defensive bounded-body cancellation), `generated.ts` (`100.00% funcs, 98.93% lines`, uncovered `169`; generated unreachable fallback), `optimization-client.ts` (`97.83% funcs, 95.05% lines`, uncovered `235-245`; transport abort/error fallback), `stores/daily-diet.ts` (`98.31% funcs, 99.55% lines`; Bun reports no stable uncovered-line range; callback instrumentation), `stores/optimization.ts` (`98.00% funcs, 100.00% lines`; function instrumentation only), `stores/search.ts` (`84.48% funcs, 94.72% lines`, uncovered `92,99,104,109,198,268-274,301,328`; impossible-state projections), and `units.ts` (`55.56% funcs, 77.27% lines`, uncovered `54,63-64,88,106-107,125-128`; label/options formatting plus unsupported/cross-basis conversion guards). The mapper, search client, and selected-diet store rows are `100.00% funcs, 100.00% lines`. Svelte components and type-only `search-state.types.ts` do not produce Bun coverage rows; their related Phase 07 evidence is the component tests plus Task 233 Playwright/axe workflows. The checker requires all ten runtime source rows, including `units.ts`, and exact measured exceptions. No strict decoder, idempotency, retry/lifecycle, authoritative selection, malformed-response, unit-default/conversion, desktop/mobile, keyboard, light/dark, axe, or real-browser requirement is waived. Rerun coverage after any Phase 07 frontend production change.
- **ACCEPTED MEASUREMENT NOTE (Task 235, 2026-07-20; owner: Phase 07.01 implementation maintainer):** serial aggregate and fresh isolated package measurements agree at `dailydiet 82.2%`, `optimization 84.2%`, `queue 76.1%`, and `worker 67.4%`; this supersedes the Task 212 queue branch-order note and its pre-remediation percentages.
- **CLOSED (2026-07-12):** the prior Debian Bookworm package-fixture deferral is superseded by the checksum-pinned official COIN-OR Ubuntu 24 artifact recorded in the implemented worker-image action above. The required no-cache image build, exact CLP version check, and `CGO_ENABLED=0` worker build now pass.
- **CLOSED (2026-07-11):** all 16 legacy Playwright failures were migrated to the current Catalog, Substitution, saved Daily Diet, and Daily Diet Alternative workflows. Catalog selections now persist as the explicit substitution handoff; obsolete `dailyDietId` assertions were replaced with saved-diet collection/selection and entitlement assertions. The maintained full desktop/mobile Chromium suite and axe checks are required aggregate evidence; no test exclusion remains.
- **ACCEPTED CONTRACT WARNING (2026-07-11; owner: Phase 07 implementation maintainer):** Redocly validates `api/openapi.yaml` with one existing ignored `operation-2xx-response` warning for the OAuth callback's intentional `302`-only redirect. The Phase 07 saved-diet and optimization schemas, generated-type drift check, and protected-route contracts pass; the redirect warning is not a Phase 07 contract failure. Focused verification: rerun `npx --no-install redocly lint api/openapi.yaml` and the generated-type drift check after contract changes.

## Phase 08

### Assumptions

- **CONFIRMED (Task 262, 2026-07-22):** administrator-authored items persist as global ownerless `food_items`, while authenticated users' private custom items use mandatory owner-scoped persistence. Task 250 CRUD/isolation tests and Tasks 238-240 persistence, route, export, and erasure integration tests pass in the aggregate and race gates.
- **CONFIRMED (Task 262, 2026-07-22):** deletion of an in-use Food Category or Culinary Role returns a conflict and does not detach, replace, or cascade references. Task 251 repository/service/HTTP invalidation and in-use tests pass in the aggregate and race gates.
- **CONFIRMED (Task 262, 2026-07-22):** user administration exposes only the privacy-minimized lookup and legal deletion-retry transition. Task 252 service/HTTP/PostgreSQL tests prove bounded projections, fail-closed authorization, legal-state enforcement, concurrent single-claim behavior, and audit persistence; no role mutation, password access, impersonation, or arbitrary editing surface exists.

### Testing coverage deviations

- **SUPERSEDED (Task 262, 2026-07-22):** the Task 239 repair exception and Task 250 open deviation were remeasured after all Phase 08 implementation and integration work. Their current results are included in the aggregate backend exception below; neither provisional measurement remains an open action.
- **SUPERSEDED (Task 271, remeasured 2026-07-24):** the Task 262 backend coverage exception was remeasured after the Phase 08.01 manual-item invalidation, strict custom-item JSON, and Go Doc remediations. Its current exact disposition follows.
- **ACCEPTED EXCEPTION (Task 277, remeasured 2026-07-27; owner: Phase 08 backend maintainer):** `go test ./internal/... -p 1 -count=1 -coverpkg=./internal/... -coverprofile=phase08-coverage.out` passes and measures the Phase 08 Go runtime scope at `93.2%` (`4,638/4,979` statements) after deduplicating cross-package profile blocks by source range. The repository aggregate package profile is `87.4%`; direct package totals include `app 84.8%`, `cache 90.1%`, `curation 90.3%`, `customitem 90.9%`, `dataimporter 88.3%`, `deletionworker 100.0%`, `externaldata 99.8%`, `httpapi 87.3%`, `itemcurator 74.2%`, `observability 85.6%`, `repository 86.3%`, `search 96.5%`, `security 99.7%`, `tagmanager 100.0%`, `useradmin 93.1%`, and `userdata 97.2%`. Exact file counts and uncovered ranges are machine-checked below, including the Task 277 manual-item/allergen persistence surfaces and the newly measured compliance repository row. Remaining statements are defensive dependency/encoder/claim-corruption branches, repeated safe repository/HTTP error mappings, configuration/cache/wiring fallbacks, and instrumentation-only paths. Production tests cover authenticated admin/non-admin and owner isolation, recursive duplicate-key rejection before service dispatch, audited create/replay/rollback/update/delete, atomic allergen persistence, exact shared-generation behavior, peer Catalog/Substitution cache visibility, PostgreSQL atomicity, and safe envelopes/telemetry. No authorization, ownership, private/global isolation, CSRF, idempotency/replay, validation, parameterized persistence, transaction-plus-audit rollback, invalidation, sanitized observability, or search visibility behavior is waived.
- **ACCEPTED EXCEPTION (Task 284 repair, remeasured 2026-07-28; owner: Phase 08 frontend maintainer):** `bun test --coverage` passes with `537` tests, `2,818` expectations, and `All files | 95.19% funcs | 96.06% lines`. Phase 08 runtime rows are `src/lib/admin-access.ts` (`100.00% funcs, 100.00% lines`), `src/lib/admin-workflows.ts` (`83.33% funcs, 98.55% lines`; Bun reports no stable uncovered-line range), `src/lib/api/account-data-client.ts` (`100.00% funcs, 98.11% lines`; Bun reports no stable uncovered-line range), `src/lib/api/admin-client.ts` (`95.95% funcs, 100.00% lines`; function instrumentation only), `src/lib/api/external-admin-client.ts` (`100.00% funcs, 100.00% lines`), `src/lib/api/filter-options-client.ts` (`100.00% funcs, 100.00% lines`), `src/lib/api/generated.ts` (`100.00% funcs, 99.06% lines`, generated fallback line `185`), `src/lib/shell-routing.ts` (`100.00% funcs, 100.00% lines`), `src/lib/substitution-filter-options.ts` (`100.00% funcs, 100.00% lines`), and Svelte components that do not emit Bun coverage rows. Component tests plus the full Playwright/axe desktop/mobile suite cover administration, import, conflict/retry, private export/deletion, dynamic filters, authorization, modal focus containment, responsive themes, and degraded paths. No generated-contract decoding, admin fail-closed state, authoritative refresh, destructive confirmation, accessibility, or browser workflow is waived.
- **ACCEPTED EXCEPTION (Task 287, remeasured 2026-07-31; owner: Phase 08 backend maintainer):** the canonical `go test ./internal/... -p 1 -count=1 -coverpkg=./internal/... -coverprofile=phase08-coverage.out` run passes and measures the Phase 08 Go runtime scope at `93.0%` (`4,668/5,017` statements). The newly changed external-provider paths retain exact defensive exceptions: `internal/externaldata/openfoodfacts.go` is `184/192` (`95.8%`) with uncovered ranges `206.62-208.12,228.10-230.4,248.14-250.12,252.26-254.12,260.2-260.17`, and `internal/externaldata/rate_limit.go` is `186/188` (`98.9%`) with uncovered ranges `357.14-359.3,407.46-409.3`. These accepted lines are bounded malformed-payload, scanner, and telemetry/error fallback branches; adversarial provider-key tests and the focused externaldata suite cover the raw JSON boundary and safe rejection behavior. No provider normalization, rate-limit enforcement, cancellation, or privacy behavior is waived.

The following machine-checked contracts are the precise current evidence behind those accepted exceptions. `scripts/check.py` derives the backend rows from the deduplicated cross-package profile, derives frontend rows from Bun's current report, rejects missing or additional exceptions, and requires every metric, uncovered location, phase owner, and justification ID to match.

<!-- phase08-backend-coverage-contract:start -->

Measured Phase 08 scope: `4668/5017` statements (`93.0%`).

| Runtime file                                          | Covered/statements | Coverage | Exact uncovered statement blocks                                                                                                                                                                                                                                                                                                                                                                                      | Justification |
| ----------------------------------------------------- | -----------------: | -------: | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------- |
| `internal/app/app.go`                                 |          `110/116` |  `94.8%` | `98.86-100.4,117.69-122.18,122.18-124.5,130.17-132.4,165.4-169.10`                                                                                                                                                                                                                                                                                                                                                  | `B3`          |
| `internal/cache/classification_generation.go`         |            `14/18` |  `77.8%` | `53.21-55.3,60.16-62.3,69.21-71.3,78.33-80.3`                                                                                                                                                                                                                                                                                                                                                                         | `B3`          |
| `internal/cache/classification_invalidator.go`        |            `23/26` |  `88.5%` | `48.54-49.23,49.23-51.5,52.4-52.10`                                                                                                                                                                                                                                                                                                                                                                                   | `B3`          |
| `internal/cache/search_cache.go`                      |          `152/167` |  `91.0%` | `94.16-96.3,109.20-111.3,115.16-117.3,147.16-149.3,158.20-160.3,162.16-164.3,223.68-225.2,229.128-231.2,275.13-276.53,276.53-280.4,294.32-295.101,295.101-297.4,490.65-492.4`                                                                                                                                                                                                                                         | `B3`          |
| `internal/curation/validation.go`                     |            `88/93` |  `94.6%` | `145.132-147.4,150.130-152.4,155.120-157.4,171.64-174.3`                                                                                                                                                                                                                                                                                                                                                              | `B1`          |
| `internal/customitem/service.go`                      |          `153/165` |  `92.7%` | `114.24-116.3,118.73-120.3,129.16-131.3,153.32-155.3,170.32-172.3,174.16-176.3,190.32-192.3,207.16-209.3,243.10-244.17,293.70-295.3,368.16-370.3,395.26-397.4`                                                                                                                                                                                                                                                        | `B1`          |
| `internal/dataimporter/service.go`                    |            `68/77` |  `88.3%` | `98.68-100.3,103.16-105.3,147.22-149.4,150.20-152.4,164.62-165.29,174.43-176.3,198.23-200.3,202.107-204.4,213.16-215.3`                                                                                                                                                                                                                                                                                               | `B1`          |
| `internal/externaldata/openfoodfacts.go`              |          `184/192` |  `95.8%` | `206.62-208.12,228.10-230.4,248.14-250.12,252.26-254.12,260.2-260.17`                                                                                                                                                                                                                                                                                                                                                 | `B4`          |
| `internal/externaldata/rate_limit.go`                 |          `186/188` |  `98.9%` | `357.14-359.3,407.46-409.3`                                                                                                                                                                                                                                                                                                                                                                                           | `B4`          |
| `internal/httpapi/auth_controller.go`                 |           `94/103` |  `91.3%` | `78.92-80.3,95.92-97.3,104.115-106.3,107.66-109.3,123.92-125.3,136.84-138.3,149.88-151.3,165.66-167.3,233.19-235.3`                                                                                                                                                                                                                                                                                                   | `B2`          |
| `internal/httpapi/classification_admin_controller.go` |            `58/77` |  `75.3%` | `55.66-57.3,64.49-66.3,74.16-76.3,78.16-80.3,88.16-90.3,92.9-94.3,100.16-102.3,110.16-112.3,114.9-116.3,118.16-120.3,122.16-124.3,126.16-128.3,136.16-138.3,144.16-146.3,148.16-150.3,159.36-161.3,168.49-170.3,178.34-180.3,188.108-190.3`                                                                                                                                                                           | `B2`          |
| `internal/httpapi/curation_validation.go`             |           `99/108` |  `91.7%` | `122.37-124.3,149.65-151.3,159.54-161.3,168.57-170.3,198.16-200.3,210.18-212.5,214.11-216.5,227.49-229.5,231.10-232.71`                                                                                                                                                                                                                                                                                               | `B1`          |
| `internal/httpapi/custom_item_controller.go`          |          `101/127` |  `79.5%` | `30.9-32.3,33.26-35.3,37.16-39.3,53.9-55.3,57.16-59.3,60.26-62.3,74.9-76.3,78.16-80.3,81.26-83.3,85.16-87.3,89.16-91.3,99.9-101.3,103.16-105.3,106.26-108.3,109.85-111.3,119.73-121.3,128.67-130.3,153.59-155.3,156.65-158.3,181.65-183.4,228.2-228.44,235.34-237.3,245.34-247.3,271.43-273.2,279.59-280.159,289.10-290.13`                                                                                           | `B2`          |
| `internal/httpapi/import_controller.go`               |            `45/55` |  `81.8%` | `56.16-58.3,59.34-61.3,63.9-65.3,67.16-69.3,99.57-101.3,108.59-110.3,134.61-135.192,142.131-143.35,144.10-145.13,151.46-153.2`                                                                                                                                                                                                                                                                                        | `B2`          |
| `internal/httpapi/manual_item_controller.go`          |          `105/134` |  `78.4%` | `57.16-59.3,60.34-62.3,64.16-66.3,93.16-95.3,96.34-98.3,100.16-102.3,110.16-112.3,113.34-115.3,117.16-119.3,133.16-135.3,136.34-138.3,140.16-142.3,160.73-162.3,169.67-171.3,195.2-195.44,202.65-204.3,206.71-208.3,211.16-213.3,256.34-258.3,277.46-279.3,280.40-282.3,283.38-285.3,286.36-288.3,289.34-291.3,311.43-313.2,319.60-320.159,323.60-324.126,327.131-328.143,329.10-330.13`                                                                                  | `B2`          |
| `internal/httpapi/profile_controller.go`              |            `34/36` |  `94.4%` | `58.68-60.5,97.16-99.3`                                                                                                                                                                                                                                                                                                                                                                                               | `B2`          |
| `internal/httpapi/router.go`                          |          `228/233` |  `97.9%` | `192.9-194.3,313.17-315.4,426.24-428.5,432.23-434.4,502.41-503.53`                                                                                                                                                                                                                                                                                                                                                    | `B3`          |
| `internal/httpapi/search_validation.go`               |          `191/193` |  `99.0%` | `398.102-400.5,415.94-417.3`                                                                                                                                                                                                                                                                                                                                                                                          | `B1`          |
| `internal/httpapi/user_admin_controller.go`           |            `64/79` |  `81.0%` | `49.16-51.3,52.22-54.3,56.9-58.3,60.16-62.3,64.28-66.3,74.16-76.3,77.22-79.3,82.41-84.3,89.123-91.3,126.35-128.4,160.46-161.132,162.62-163.36,166.171-167.36,168.10-169.13,181.42-183.2`                                                                                                                                                                                                                              | `B2`          |
| `internal/itemcurator/service.go`                     |            `72/97` |  `74.2%` | `113.25-115.3,117.73-119.3,124.45-126.3,128.16-130.3,141.66-143.3,150.20-152.3,153.32-155.3,166.20-168.3,170.16-172.3,173.45-175.3,177.16-179.3,180.74-182.3,184.16-186.3,193.20-195.3,196.45-198.3,200.16-202.3,203.52-205.3,213.16-215.3,238.31-240.142,240.142-242.4,245.63-247.3,289.19-291.3,293.25-295.3,308.44-310.2`                                                                                                                           | `B1`          |
| `internal/observability/admin_external.go`            |           `99/102` |  `97.1%` | `71.17-73.3,243.115-245.4,256.10-257.35`                                                                                                                                                                                                                                                                                                                                                                              | `B4`          |
| `internal/repository/admin_user_repository.go`        |            `51/63` |  `81.0%` | `60.16-62.3,67.17-69.4,72.35-74.3,81.24-83.3,84.53-86.3,97.62-99.3,116.33-118.4,122.67-124.4,142.214-144.3,145.82-147.29,147.29-149.4`                                                                                                                                                                                                                                                                                  | `B2`          |
| `internal/repository/classification_repository.go`    |            `58/65` |  `89.2%` | `73.36-75.3,99.20-101.3,110.78-112.3,120.77-122.3,153.48-155.3,156.85-158.3,200.165-202.3`                                                                                                                                                                                                                                                                                                                            | `B2`          |
| `internal/repository/compliance_repository.go`        |          `277/280` |  `98.9%` | `546.31-547.31,547.31-549.4,550.10-551.56`                                                                                                                                                                                                                                                                                                                                                                           | `B2`          |
| `internal/repository/curated_import_repository.go`    |           `98/129` |  `76.0%` | `64.69-66.3,79.126-81.3,88.102-90.4,93.37-95.3,111.24-113.4,116.184-118.4,119.43-121.4,123.23-125.4,128.36-130.3,134.149-136.3,140.19-142.3,144.16-146.3,157.99-159.3,163.58-165.3,171.73-173.4,177.17-179.4,182.16-184.3,188.16-190.3,192.137-194.3,195.33-197.3,205.563-207.3,208.117-210.3,218.16-220.3,221.32-223.3,254.82-256.3,264.95-266.3,291.47-293.3,307.48-309.3,311.117-313.3,314.163-316.3,318.35-320.3` | `B2`          |
| `internal/repository/custom_food_repository.go`       |          `135/154` |  `87.7%` | `108.25-110.3,112.16-114.3,118.17-121.4,124.35-127.3,132.62-134.4,144.69-146.3,156.18-158.5,164.18-166.5,170.43-172.4,180.106-182.4,211.30-213.3,228.137-230.3,253.141-255.3,262.68-264.3,265.110-267.3,269.35-271.3,272.19-274.3`                                                                                                                                                                                    | `B2`          |
| `internal/repository/food_repository.go`              |          `199/200` |  `99.5%` | `241.158-243.2`                                                                                                                                                                                                                                                                                                                                                                                                       | `B2`          |
| `internal/repository/manual_food_repository.go`       |           `87/120` |  `72.5%` | `65.20-67.3,74.20-76.3,83.69-85.3,93.17-95.4,97.62-99.4,101.17-103.4,106.42-108.3,110.16-112.3,116.105-118.3,125.25-127.3,128.68-130.3,131.80-133.3,135.16-137.3,138.32-140.3,141.122-143.3,150.20-152.3,154.16-156.3,157.32-159.3,176.117-178.3,179.83-181.3,192.79-194.3,195.67-197.3,205.86-207.3,217.67-219.3,220.75-222.3,230.16-232.3,237.41-239.4,242.35-244.3,267.140-269.3,276.35-278.3,279.110-281.3,283.35-285.3,286.19-288.3` | `B2`          |
| `internal/search/filter_options.go`                   |            `79/80` |  `98.8%` | `98.70-100.3`                                                                                                                                                                                                                                                                                                                                                                                                         | `B1`          |
| `internal/search/substitution_service.go`             |          `212/225` |  `94.2%` | `60.16-62.3,159.4-159.22,187.53-189.3,191.43-193.4,266.30-268.4,270.17-272.4,275.72-277.5,318.28-321.17,321.17-323.4,334.16-336.3,340.17-342.4`                                                                                                                                                                                                                                                                       | `B1`          |
| `internal/useradmin/service.go`                       |            `81/87` |  `93.1%` | `118.81-120.3,144.17-146.4,155.17-157.4,219.17-221.4,226.18-228.5,242.16-244.3`                                                                                                                                                                                                                                                                                                                                        | `B1`          |
| `internal/userdata/export.go`                         |           `99/104` |  `95.2%` | `130.17-132.4,136.16-138.3,192.17-194.4,216.17-218.4,279.39-281.3`                                                                                                                                                                                                                                                                                                                                                   | `B1`          |

- `B1` — Defensive dependency, encoder, or claim-corruption branch; evidence: focused unit and HTTP error-path suites.
- `B2` — Repeated safe repository or HTTP error mapping; evidence: repository, HTTP, and live integration suites.
- `B3` — Configuration, cache, or wiring fallback; evidence: aggregate bootstrap and live-dependency suites.
- `B4` — Instrumentation-only path; evidence: observability unit and load suites.

<!-- phase08-backend-coverage-contract:end -->

<!-- frontend-coverage-contract:start -->

| Runtime row                                 | Owning phase | Functions |   Lines | Uncovered lines                     | Justification |
| ------------------------------------------- | ------------ | --------: | ------: | ----------------------------------- | ------------- |
| `src/lib/admin-workflows.ts`                | Phase 08     |    83.33% |  98.55% | `-`                                 | `F1`          |
| `src/lib/alternative-diet-save.ts`          | Phase 07.01  |   100.00% |  95.56% | `57-58`                             | `F5`          |
| `src/lib/api/account-data-client.ts`        | Phase 08     |   100.00% |  98.11% | `-`                                 | `F1`          |
| `src/lib/api/admin-client.ts`               | Phase 08     |    95.95% | 100.00% | `-`                                 | `F2`          |
| `src/lib/api/auth-client.ts`                | Phase 06.01  |    88.89% |  81.31% | `224-233,237-250,255-269`           | `F4`          |
| `src/lib/api/daily-diet-client.ts`          | Phase 07     |    95.83% |  95.26% | `292-302`                           | `F4`          |
| `src/lib/api/entitlement-client.ts`         | Phase 06     |    88.24% |  79.03% | `248-257,275-287,292-306`           | `F4`          |
| `src/lib/api/generated.ts`                  | Phase 08     |   100.00% |  99.06% | `185`                               | `F3`          |
| `src/lib/api/optimization-client.ts`        | Phase 07     |    97.83% |  95.05% | `235-245`                           | `F4`          |
| `src/lib/components/oauth-entry-point.ts`   | Phase 06.01  |    71.43% |  85.94% | `27,49,68,79,83-84,95,98-99`        | `F4`          |
| `src/lib/components/register-controller.ts` | Phase 06.01  |    83.33% |  90.70% | `57,88,116,152,163,167,202-207`     | `F4`          |
| `src/lib/search-entitlement.ts`             | Phase 06     |   100.00% |  84.62% | `65-66,70,94-98`                    | `F4`          |
| `src/lib/stores/auth-session.ts`            | Phase 06.01  |   100.00% |  95.38% | `158-159,210-212,264,282`           | `F4`          |
| `src/lib/stores/daily-diet.ts`              | Phase 07     |    98.31% |  99.55% | `-`                                 | `F1`          |
| `src/lib/stores/optimization.ts`            | Phase 07     |    98.00% | 100.00% | `-`                                 | `F2`          |
| `src/lib/stores/search.ts`                  | Phase 07     |    84.48% |  94.72% | `92,99,104,109,198,268-274,301,328` | `F5`          |
| `src/lib/units.ts`                          | Phase 07     |    55.56% |  77.27% | `54,63-64,88,106-107,125-128`       | `F5`          |

- `F1` — Bun emits no stable line range or only callback instrumentation; evidence: focused unit and browser suites.
- `F2` — Function instrumentation only; evidence: focused unit and browser suites.
- `F3` — Generated unreachable fallback; evidence: generated-contract drift and client suites.
- `F4` — Defensive decoder, transport, or browser-dependency fallback; evidence: focused client and browser suites.
- `F5` — Bounded guard, impossible-state projection, or formatting fallback; evidence: focused state and component suites.

<!-- frontend-coverage-contract:end -->

### Actions needed

Task 280 acceptance runs synchronize requirement findings through the ledger
below. Every non-pass result must add or update one unresolved record; a passing
retest changes that record to `CLOSED` with date and sanitized evidence. Records
are historical controls and must not be deleted. Every finding ID must also be
retained in the mandatory append-only
`docs/testing/phase08/finding-history.json` registry.

<!-- phase08-acceptance-findings:start -->
```json
{
  "schema": "mealswapp.phase08-findings.v1",
  "findings": [
    {
      "id": "P08-FIND-281-001",
      "rootCauseId": "ROOT-T281-MOBILE-SIDEBAR",
      "status": "CLOSED",
      "requirements": [
        "SW-REQ-054"
      ],
      "scenarios": [
        "P08-SWR054-ADMINISTRATIVE-ACCESS"
      ],
      "observed": "The first isolated run passed anonymous mobile and ordinary desktop authorization but the acceptance helper could not reach responsive sign-in and administration controls on mobile.",
      "expected": "The real-stack acceptance helper opens the responsive sidebar before using authentication or administration navigation controls on every configured viewport.",
      "evidence": "logs/real-stack-e2e/144c92b45d30cb9919f991fa/acceptance/results.json",
      "owner": "phase08-e2e",
      "retestCondition": "Run Task 281 on desktop and mobile after the helper opens hidden responsive navigation controls.",
      "closedDate": "2026-07-28",
      "passingEvidence": "docs/implementation/evidence/task-281-preparation.md"
    },
    {
      "id": "P08-FIND-281-002",
      "rootCauseId": "ROOT-T281-STALE-REFRESH",
      "status": "OPEN DEFECT",
      "requirements": [
        "SW-REQ-054"
      ],
      "scenarios": [
        "P08-SWR054-ADMINISTRATIVE-ACCESS"
      ],
      "observed": "A pre-bootstrap ordinary browser session receives administrator role and restricted panel access when startup refresh runs after bootstrap, without sign-out and credential reauthentication.",
      "expected": "Existing access and refresh sessions retain ordinary-user claims after bootstrap; administrator access begins only after sign-out and a fresh credential sign-in.",
      "evidence": "logs/real-stack-e2e/2cb5f9034ca6f86e986d27f3/acceptance/results.json",
      "owner": "phase08-auth",
      "retestCondition": "Bootstrap an account with two existing ordinary sessions and prove both remain forbidden until each signs out and signs in with credentials."
    },
    {
      "id": "P08-FIND-281-003",
      "rootCauseId": "ROOT-T281-ACCEPTANCE-INFRASTRUCTURE",
      "status": "CLOSED",
      "requirements": [
        "SW-REQ-054"
      ],
      "scenarios": [
        "P08-SWR054-ADMINISTRATIVE-ACCESS"
      ],
      "observed": "Closed after the managed Task 281 run completed its browser, API, PostgreSQL, Redis, cleanup, and report lifecycle without the infrastructure root.",
      "expected": "The managed Task 281 run completes all browser and backend assertions and produces a structured nine-criterion report without an infrastructure root.",
      "evidence": "docs/implementation/evidence/task-281-review.md",
      "owner": "phase08-e2e",
      "retestCondition": "Run the isolated Task 281 suite and prove all lifecycle stages, exact PostgreSQL/audit assertions, Redis PONG, cleanup, and report finalization complete.",
      "closedDate": "2026-07-29",
      "passingEvidence": "docs/implementation/evidence/task-281-preparation.md"
    },
    {
      "id": "P08-FIND-282-001",
      "rootCauseId": "ROOT-T282-OFF-METADATA",
      "status": "CLOSED",
      "requirements": [
        "SW-REQ-055"
      ],
      "scenarios": [
        "P08-SWR055-EXTERNAL-CURATION"
      ],
      "observed": "A controlled legitimate OpenFoodFacts product carrying modifier, unit, and label metadata is discarded even though its supported macro and micronutrient values are valid.",
      "expected": "Unsupported provider metadata is ignored while supported numeric nutrients remain strictly validated and the candidate remains available for curation.",
      "evidence": "logs/real-stack-e2e/d08e098642ba3e9c87de8721/acceptance/results.json",
      "owner": "backend-external-data",
      "retestCondition": "Run Task 282 with legitimate OpenFoodFacts metadata and observe the candidate through the production Administration UI.",
      "closedDate": "2026-07-29",
      "passingEvidence": "logs/real-stack-e2e/eed925ef7313fed9671c514c/acceptance/SW-REQ-055.json"
    },
    {
      "id": "P08-FIND-282-002",
      "rootCauseId": "ROOT-T282-USDA-OPTIONAL-PORTION",
      "status": "OPEN DEFECT",
      "requirements": [
        "SW-REQ-033"
      ],
      "scenarios": [
        "P08-SWR033-STANDARDIZED-STORAGE"
      ],
      "observed": "A controlled USDA result with valid identity and nutrients but null optional portion amount/unit causes the provider page to be rejected.",
      "expected": "Invalid optional portion evidence is skipped or warned while the valid candidate remains available without inventing liquid density.",
      "evidence": "logs/real-stack-e2e/d08e098642ba3e9c87de8721/acceptance/results.json",
      "owner": "backend-external-data",
      "retestCondition": "Run Task 282 with mixed valid and null optional USDA measures and observe the candidate with canonical metric values."
    },
    {
      "id": "P08-FIND-282-003",
      "rootCauseId": "ROOT-T282-AMBIGUOUS-RESPONSE",
      "status": "CLOSED",
      "requirements": [
        "SW-REQ-055"
      ],
      "scenarios": [
        "P08-SWR055-EXTERNAL-CURATION"
      ],
      "observed": "Resolved by the Task 282 loopback proxy response-loss boundary: the first import commits before its response is dropped, the UI exposes its safe retry state, and the retry reuses the byte-identical body and original browser-generated Idempotency-Key.",
      "expected": "A controlled proxy loses a committed import response, the UI preserves its key and draft, authoritative recovery proves one commit, and a safe retry does not duplicate effects.",
      "evidence": "logs/real-stack-e2e/d08e098642ba3e9c87de8721/acceptance/results.json",
      "owner": "phase08-e2e",
      "retestCondition": "Rerun Task 282 and require P08-SWR055-ACCEPT-06 PASS plus one food/import/audit and ownerless global persistence evidence.",
      "closedDate": "2026-07-28",
      "passingEvidence": "docs/implementation/evidence/task-282-preparation.md"
    },
    {
      "id": "P08-FIND-282-004",
      "rootCauseId": "ROOT-T282-VOCABULARY-DISABLE",
      "status": "OPEN BLOCKER",
      "requirements": [
        "SW-REQ-090"
      ],
      "scenarios": [
        "P08-SWR090-MICRONUTRIENT-VOCABULARY"
      ],
      "observed": "Administration exposes no production capability to disable and restore a micronutrient vocabulary entry, so the required disabled-entry import scenario cannot be driven without direct database mutation.",
      "expected": "The acceptance flow disables a vocabulary entry through an authorized production boundary, proves import rejection, restores it, and leaves no fixture state.",
      "evidence": "logs/real-stack-e2e/d08e098642ba3e9c87de8721/acceptance/results.json",
      "owner": "backend-data-curation",
      "retestCondition": "Provide the authorized vocabulary lifecycle capability and rerun Task 282 without database bypass."
    },
    {
      "id": "P08-FIND-283-001",
      "rootCauseId": "ROOT-T283-MANUAL-CATALOG",
      "status": "OPEN BLOCKER",
      "requirements": ["SW-REQ-056"],
      "scenarios": ["P08-SWR056-MANUAL-GLOBAL-ITEMS"],
      "observed": "Real-stack Task 283 probes currently expose missing or unproven manual solid/liquid update, audit rollback, search/substitution visibility, deletion, density provenance, and canonical persistence behavior; the invalid-liquid and replay probes are executable but do not establish the remaining criteria.",
      "expected": "Every SW-REQ-056 criterion executes against production UI/API and read-only persistence evidence, with exact counts and truthful status.",
      "evidence": "docs/implementation/evidence/task-283-preparation.md",
      "owner": "phase08-e2e",
      "retestCondition": "Run the complete Task 283 isolated producer and close only when every SW-REQ-056 criterion passes."
    },
    {
      "id": "P08-FIND-283-002", "rootCauseId": "ROOT-T283-CLASSIFICATION-LIFECYCLE", "status": "OPEN BLOCKER",
      "requirements": ["SW-REQ-057"], "scenarios": ["P08-SWR057-CLASSIFICATIONS"],
      "observed": "The production UI currently exposes create/rename/delete controls but no reparent control; hierarchy, cycle, detach, and complete conflict semantics remain unavailable at the UI boundary.",
      "expected": "Food Category and Culinary Role lifecycle and stable-ID filter propagation pass in two application instances.",
      "evidence": "docs/implementation/evidence/task-283-preparation.md", "owner": "phase08-e2e",
      "retestCondition": "Add the authorized hierarchy lifecycle and rerun both browser contexts with database/cache evidence."
    },
    {
      "id": "P08-FIND-283-003", "rootCauseId": "ROOT-T283-FILTER-CROSS-INSTANCE", "status": "CLOSED",
      "requirements": ["SW-REQ-019"], "scenarios": ["P08-SWR019-CLASSIFICATION-FILTERING"],
      "observed": "Resolved by the Task 283 managed desktop/mobile run: stable classification UUIDs drive include/exclude requests, API-2 commits a rename, API-1 observes the renamed projection, and operation-scoped Redis observations prove committed invalidation and failed-mutation non-invalidation.",
      "expected": "Stable IDs drive requests and committed changes propagate without stale or failed-mutation invalidation.",
      "evidence": "docs/implementation/evidence/task-283-preparation.md", "owner": "phase08-e2e",
      "retestCondition": "Rerun Task 283 and require all SW-REQ-019 criteria PASS with operation-scoped SQL, request, and Redis evidence.",
      "closedDate": "2026-07-28",
      "passingEvidence": "docs/implementation/evidence/task-283-preparation.md"
    },
    {
      "id": "P08-FIND-283-004", "rootCauseId": "ROOT-T283-METRIC-NORMALIZATION", "status": "CLOSED",
      "requirements": ["SW-REQ-032"], "scenarios": ["P08-SWR032-IMPERIAL-CONVERSION"],
      "observed": "Resolved by the Task 283 managed desktop/mobile run: imperial mass and volume inputs are converted exactly once, displayed in both unit systems, and matched to canonical metric persistence through request and read-only database evidence.",
      "expected": "Metric database values equal the converted imperial quantities exactly once across create, update, display, and request boundaries.",
      "evidence": "docs/implementation/evidence/task-283-preparation.md", "owner": "phase08-e2e",
      "retestCondition": "Rerun Task 283 and require all SW-REQ-032 criteria PASS with desktop/mobile conversion and canonical persistence evidence.",
      "closedDate": "2026-07-28",
      "passingEvidence": "docs/implementation/evidence/task-283-preparation.md"
    },
    {
      "id": "P08-FIND-283-005", "rootCauseId": "ROOT-T283-DISCOVERY-PARTITION", "status": "OPEN BLOCKER",
      "requirements": ["SW-REQ-033"], "scenarios": ["P08-SWR033-STANDARDIZED-STORAGE"],
      "observed": "The available probes do not establish ownerless global discovery, normalization/partition isolation, Catalog/Substitution visibility, or deletion search exclusion for the newly created manual records.",
      "expected": "Global and private partitions remain isolated while normalized global records are discoverable through both production search surfaces.",
      "evidence": "docs/implementation/evidence/task-283-preparation.md", "owner": "phase08-e2e",
      "retestCondition": "Execute partition and search/substitution scenarios with read-only owner/count evidence."
    },
    {
      "id": "P08-FIND-283-006", "rootCauseId": "ROOT-T283-MICRONUTRIENT-VALIDATION", "status": "OPEN BLOCKER",
      "requirements": ["SW-REQ-090"], "scenarios": ["P08-SWR090-MICRONUTRIENT-VOCABULARY"],
      "observed": "The invalid-input probe reaches the production API, but the run does not yet prove the full canonical active micronutrient/allergen/classification validation and rollback/generation invariants.",
      "expected": "Active vocabulary validation is authoritative, malformed values fail without mutation or generation invalidation, and accepted values persist canonically.",
      "evidence": "docs/implementation/evidence/task-283-preparation.md", "owner": "phase08-e2e",
      "retestCondition": "Execute valid/invalid vocabulary and rollback scenarios with audit and generation counts."
    },
    {
      "id": "P08-FIND-282-005",
      "rootCauseId": "ROOT-T282-ACCEPTANCE-INFRASTRUCTURE",
      "status": "CLOSED",
      "requirements": [
        "SW-REQ-033",
        "SW-REQ-055",
        "SW-REQ-090"
      ],
      "scenarios": [
        "P08-SWR033-STANDARDIZED-STORAGE",
        "P08-SWR055-EXTERNAL-CURATION",
        "P08-SWR090-MICRONUTRIENT-VOCABULARY"
      ],
      "observed": "Closed after the final managed Task 282 run completed its disposable stack, provider fixtures, browser, PostgreSQL, Redis, cleanup, and report lifecycle and reported only the observed product roots.",
      "expected": "The managed Task 282 run completes all controlled-provider, UI, API, persistence, cache, and report stages and records only product roots for observed product behavior.",
      "evidence": "docs/implementation/evidence/task-282-preparation.md",
      "owner": "phase08-e2e",
      "retestCondition": "Run Task 282 and prove the disposable stack, loopback fixtures, backend evidence, cleanup, and three Task 280 reports finalize.",
      "closedDate": "2026-07-29",
      "passingEvidence": "docs/implementation/evidence/task-282-preparation.md"
    },
    {
      "id": "P08-FIND-284-001",
      "rootCauseId": "ROOT-T284-EXPORT-OWNER-PROJECTION",
      "status": "OPEN DEFECT",
      "requirements": [
        "SW-REQ-043",
        "SW-REQ-072"
      ],
      "scenarios": [
        "P08-SWR043-PRIVATE-ITEM-ISOLATION",
        "P08-SWR072-DATA-PORTABILITY"
      ],
      "observed": "The owner-scoped JSON account export includes nested repository projections with UserID ownership fields even though cross-owner access, per-owner contents, CSV parsing, and global-data exclusion otherwise pass.",
      "expected": "Account Export contains the authenticated account section and owner-scoped data without repeating persistence ownership identifiers in nested saved-item or other resource projections.",
      "evidence": "logs/real-stack-e2e/68bcf9edf41e8867a4b53b31/acceptance/results.json",
      "owner": "backend-privacy",
      "retestCondition": "Rerun Task 284 and require P08-SWR043-ACCEPT-01 plus P08-SWR072-STEP-04 to pass with no nested owner or UserID fields in parsed JSON/CSV projections."
    },
    {
      "id": "P08-FIND-285-001",
      "rootCauseId": "ROOT-T285-DEPLOYED-ENVIRONMENT",
      "status": "OPEN BLOCKER",
      "requirements": [
        "SW-REQ-084"
      ],
      "scenarios": [
        "P08-SWR084-CENTRALIZED-LOGGING"
      ],
      "observed": "Task 285 deterministically blocks when the explicitly acknowledged deployed test origin, bounded GCP Cloud Logging project scope, application-default query authority, complete ingestion window, or retention evidence is unavailable; local application console output is never accepted.",
      "expected": "An isolated deployed test environment exposes the production UI and API, centralizes structured events in the independently queryable least-privilege sink, retains the required evidence, and permits bounded request-correlation queries.",
      "evidence": "docs/implementation/evidence/task-285-preparation.md",
      "owner": "platform-observability",
      "retestCondition": "Configure the acknowledged deployed test target and least-privilege Cloud Logging reader, then rerun Task 285 through a complete bounded ingestion and retention query."
    },
    {
      "id": "P08-FIND-285-002",
      "rootCauseId": "ROOT-T285-ACTION-COVERAGE",
      "status": "OPEN BLOCKER",
      "requirements": [
        "SW-REQ-084"
      ],
      "scenarios": [
        "P08-SWR084-CENTRALIZED-LOGGING"
      ],
      "observed": "Task 285 records this root when the deployed production UI/API cannot safely generate and clean up every required authentication, external search/import, manual item, classification, user-administration, validation, dependency, and audit-failure action.",
      "expected": "All required deployed actions complete through production boundaries, emit unique safe request correlations, and leave no action fixture state after success or failure.",
      "evidence": "docs/implementation/evidence/task-285-preparation.md",
      "owner": "phase08-e2e",
      "retestCondition": "Provide the isolated deployed provider, dependency, and audit-failure controls and rerun until the safe action receipt contains every required category exactly once."
    },
    {
      "id": "P08-FIND-285-003",
      "rootCauseId": "ROOT-T285-CENTRALIZED-LOGGING",
      "status": "OPEN DEFECT",
      "requirements": [
        "SW-REQ-084"
      ],
      "scenarios": [
        "P08-SWR084-CENTRALIZED-LOGGING"
      ],
      "observed": "Task 285 records this root when independently queried centralized events are malformed, missing, duplicated, have inconsistent timestamps or outcomes, include fields outside the fixed action/resource/outcome/request-ID vocabulary, or contain forbidden private data.",
      "expected": "Each deployed action has exactly one timestamped ingested centralized event correlated only by its safe request ID, with fixed bounded action, resource, and outcome fields and no names, identifiers, credentials, keys, payloads, URLs, or diagnostics.",
      "evidence": "docs/implementation/evidence/task-285-preparation.md",
      "owner": "platform-observability",
      "retestCondition": "Repair centralized structured logging and rerun Task 285 until all correlations are exact and the raw-event redaction scan plus sanitized report pass."
    }
  ]
}
```
<!-- phase08-acceptance-findings:end -->

- **ACCEPTED OUT OF SCOPE (project-owner decision, 2026-07-29):** no implementation task will be created for `P08-FIND-281-002`. The stale-refresh behavior is not important for the current Phase 08 scope. The finding remains in the append-only acceptance ledger because its failed test result is historical evidence; this disposition does not claim that the behavior passed a retest.
- **PLANNED (Tasks 289-290, accepted 2026-07-29; owner: backend/frontend/data-curation/tooling maintainers):** resolve `P08-FIND-282-004` with verified-administrator micronutrient vocabulary management in both the Administration UI and an authenticated operator CLI. Canonical keys remain stable, hard deletion is not exposed, and unit changes or deactivation are blocked while the key is in use.
- **TRACKING (Task 302, project-owner decision, 2026-07-29):** `P08-FIND-283-001` is a broad manual-catalog acceptance tracker, not one implementation defect. Do not create a duplicate implementation task for it. Task 302 owns the final isolated real-stack retest and may close this tracker only with mapped passing evidence after the specific product tasks are complete.
- **PLANNED (Task 291, accepted 2026-07-29; owner: frontend/data-curation maintainers):** resolve `P08-FIND-283-002` by exposing the existing classification create-with-parent, reparent, detach, and hierarchy-conflict behavior in the Administration UI, then rerun the isolated SW-REQ-057 acceptance scenario.
- **PLANNED (Task 292, accepted 2026-07-29; owner: backend/frontend/data-curation maintainers):** resolve `P08-FIND-283-005` with an administrator-only, global-only paginated item search contract and accessible picker, while retaining direct UUID loading as a secondary advanced tool. The isolated retest must prove ownerlessness, private partition isolation, Catalog and Substitution visibility, and deletion exclusion.
- **TRACKING (Task 302, project-owner decision, 2026-07-29):** `P08-FIND-283-006` is a real-stack evidence gap, not a confirmed separate product defect. Do not create a duplicate product task. Task 302 must prove valid and invalid micronutrient, allergen, and classification behavior together with exact persistence, audit, rollback, and Redis-generation counts.
- **PLANNED (Task 293, accepted 2026-07-29; owner: backend/privacy/frontend maintainers):** resolve `P08-FIND-284-001` with export-only nested projections. Keep the account identity only in the top-level user section and remove repeated persistence ownership fields from nested JSON and CSV data, then rerun the isolated private-isolation and portability scenarios.
- **DEFERRED TO PHASE 09 (project-owner decision, 2026-07-29; owner: platform/observability maintainers):** move `P08-FIND-285-001` to Phase 09 planning. It requires an approved deployed test origin, bounded GCP Cloud Logging scope, least-privilege reader, and retention evidence. No task is created now. Local console output remains invalid evidence.
- **DEFERRED TO PHASE 09 (project-owner decision, 2026-07-29; owner: platform/E2E/observability maintainers):** move `P08-FIND-285-002` to Phase 09 planning. It requires safe deployed fixtures for authentication, provider, administration, validation, dependency, and audit-failure actions. No task is created now, and no public fault-injection API is approved.
- **DEFERRED TO PHASE 09 (project-owner decision, 2026-07-29; owner: platform/observability maintainers):** move `P08-FIND-285-003` to Phase 09 planning. Its missing, duplicate, malformed, inconsistent, or privacy-unsafe centralized-log behavior can be evaluated only after the deployed environment exists. No task is created now.
- **IMPLEMENTED (Task 276, verified 2026-07-29; owner: architecture/backend/security/deployment maintainers):** the dedicated operator-only administrator-bootstrap CLI covers local development, deployed development, and production without a default password or public role-promotion route. It targets one explicit email or UUID, requires the correct environment and separate production confirmation, permits only first-administrator creation, records the operator-origin change, handles replay and existing administrators safely, emits no credential or PII, and documents required sign-out/sign-in for fresh claims. Focused, PostgreSQL, race, security, and aggregate evidence passes.
- **IMPLEMENTED (Task 277, verified 2026-07-29; owner: backend/data-curation/security maintainers):** `scripts/import-global-catalog.py` imports versioned JSON through the authenticated administration API with complete preflight validation, stable per-item idempotency keys, metric nutrition and density fields, canonical micronutrients/allergens, classification name or UUID resolution, interactive credentials, in-memory cookies, CSRF, bounded pacing and `Retry-After`, dry-run, resumable reruns, deterministic safe reporting, and nonzero partial-failure exits. It does not add a public bulk endpoint or write directly to PostgreSQL. Focused, PostgreSQL/Redis, race, security, traceability, and aggregate evidence passes.
- **IMPLEMENTED (Task 278, verified 2026-07-29; owner: backend/data-curation/tooling maintainers):** `scripts/export-global-catalog.py` and the restricted administrator export API produce the complete ownerless global catalog in the versioned deterministic JSON format shared with Task 277, with optional inspection CSV, active-only default, explicit deleted inclusion, complete metric item/classification/allergen/source metadata, read-only consistent streaming, private temporary output, fsync and atomic replacement, stable ordering, safe failures, and round-trip verification. It remains separate from personal Account Export and cannot expose private account data. Focused, PostgreSQL, race, security, traceability, and aggregate evidence passes.
- **IMPLEMENTED (Task 279, verified 2026-07-29; owner: frontend/E2E/test-infrastructure maintainers):** `scripts/run-real-stack-e2e.py` owns a disposable marked test database, run-labeled Redis instance, and separate API/frontend processes. It cleans exact run-owned resources after success, assertion failure, timeout, cancellation, and handled signals; provides age-bounded stale-run recovery; rejects production, shared, non-loopback, unmarked, and ambiguous targets; never uses name-prefix deletion or Redis `FLUSHALL`; retains sanitized evidence; and asserts zero leaks. Sequential, concurrent, injected-failure, signal, security, traceability, and aggregate evidence passes.
- **IMPLEMENTED (Tasks 280-286, verified 2026-07-29; owner: Phase 08 frontend/E2E/backend/test-infrastructure maintainers):** the Phase 08 acceptance manifest maps every `req_tests.md` step and criterion to 91 machine-checked requirement results. Isolated real-stack and deployed-environment scenarios cover authorization, providers/import, manual catalog/classifications, private isolation/export/erasure, and centralized logging; reports preserve execution after failures, distinguish `PASS`/`FAIL`/`BLOCKED`, synchronize findings, retain sanitized request/evidence links, and keep product failures visible. Task 279 owns isolation and cleanup. Tasks 281-285 may pass as truthful test delivery while their observed product findings remain open; Task 286 keeps the acceptance decision separate.
- **PLANNED (Task 287, accepted 2026-07-29; owner: backend/external-data maintainers):** repair OpenFoodFacts candidate parsing so legitimate provider metadata does not silently remove otherwise valid products from external search results. `projectOpenFoodFactsProduct` currently rejects the entire record when any `nutriments` value is a string other than `label` or a key ending in `_unit`; real records contain valid metadata such as `energy_modifier: "~"` and `added-sugars_modifier: "~"`, causing known products to disappear while the administration UI reports that no candidates matched. Validate every provider key for length and unsafe characters, then use the existing `classifyOpenFoodFactsNutrient` policy to retain and strictly decode only nutrient fields Mealswapp consumes; safely ignore recognized or future non-nutrient metadata instead of enumerating every metadata suffix. Continue rejecting recognized nutrient values that are nonnumeric, negative, non-finite, null, or structurally malformed. Add regression fixtures using real-world `_modifier`, `_unit`, and label metadata alongside valid macro/micronutrient values; prove the candidate remains visible with its supported numeric nutrients, malformed supported values still fail closed, dropped-record telemetry remains bounded and useful, and the UI distinguishes provider failure or rejected candidates from a genuine zero-match search. Treat human-readable product-name search as the normal workflow; evaluate direct barcode lookup separately as an optional exact-match path rather than requiring administrators to enter barcodes.
- **PLANNED (Task 288, accepted 2026-07-29; owner: backend/external-data/domain maintainers):** make USDA search tolerant of incomplete optional portion evidence instead of reporting the whole provider as unavailable. A live authenticated `olive oil` request returned `200`, 20 foods, 106,874 bytes in 1.66 seconds, but the first valid FNDDS result contained measures such as `{"gramWeight":224,"disseminationText":"1 cup","amount":null,"measureUnit":null}`. `decodeUSDASearch` decodes the absent amount as zero, rejects that measure, then aborts the entire provider page. Separate the concepts explicitly: the USDA top-level `servingSize`/`servingSizeUnit` pair may populate `AverageUnitWeightGrams` for a solid or serving volume for a liquid; a structured `foodMeasures` entry is additional portion evidence used primarily for measured conversions and trusted liquid-density derivation, and must not automatically be treated as the manual form’s “Average unit weight (g).” Validate required food identity and supported numeric nutrients strictly, but skip or warn on unusable optional measures, retain valid measures from the same food, and isolate an invalid candidate so other valid results remain visible. Consider safely parsing supported `disseminationText` forms only when unambiguous, without guessing from arbitrary provider prose. Add live-shape fixtures for null amount/unit, text-only cup/tablespoon measures, mixed valid/invalid measures, candidate-level corruption, exact FDC-ID search, broad name search, timeout/rate-limit distinction, and UI feedback that differentiates partial normalization from provider unavailability.
- **PLANNED (Task 294, accepted 2026-07-29; owner: backend/API/frontend maintainers):** `manualItemData` already omits absent optional measurements, provenance, and image fields while retaining required empty collections. Task 294 owns the remaining ambiguous-outcome UX: treat an undecodable `2xx` as potentially committed, preserve a safe request ID and immutable request/key, require authoritative recovery before retry, prevent duplicate submission, reuse only the stable idempotency key, and verify persisted state, audit count, cache invalidation, recovery, and field-specific validation feedback.
- **PLANNED (Task 292, accepted 2026-07-29; owner: product/frontend/backend maintainers):** replace the Manual global items panel’s UUID-only discovery workflow with a searchable global-catalog item picker. The current `Item ID` plus `Load` form assumes an administrator already knows an internal UUID that the product UI never exposes; obtaining it from PostgreSQL, browser network responses, audit records, or a prior create response is a developer workaround rather than usable administration. Make human-readable Catalog-style search the primary workflow through an administrator-only global-item search contract. Show bounded paginated results with name, relevant classifications, macro summary, and an explicit `Edit` action; selecting a result should use its `itemId` to load the authoritative `GET /api/v1/admin/items/{itemId}` projection into the form. Preserve load-by-ID only as a clearly secondary advanced/audit tool. Include debounce or explicit-submit behavior consistent with Catalog Search, stale-request cancellation, keyboard navigation, accessible loading/empty/error states, pagination, cache freshness after mutations, duplicate-name disambiguation, and strict exclusion of user-owned private items. Resolve the existing manual-item optional-field response-contract defect first or alongside this work so selecting a result cannot produce another false `malformed_admin_response`; define a separate administrator-only discovery path if future restore/purge decisions require searching soft-deleted items.
- **PLANNED (Task 291 expansion accepted 2026-07-29; owner: backend/seed/API/frontend maintainers):** the four deterministic development classifications already use RFC-version/variant-conforming UUIDs and migrate existing references safely. Task 291 also owns the remaining mutation-refresh UX: once create/update commits, preserve its safe identity, report `Saved, but the list could not be refreshed`, retry only the read, never imply rollback, prevent duplicate resubmission, retain the last safe list, and verify partial/failed refresh, request IDs, exact audit/cache effects, and recovery without stale or blank columns.
- **PLANNED (Tasks 295-296, accepted 2026-07-29; owner: architecture/backend/frontend maintainers):** make metric units the invariant for persistence, repository entities, domain services, exports, and metric-named API fields. Remove repository-boundary display mutation and `RepositoryContext.UnitSystem`. Keep all display conversion in the frontend. The authenticated profile preference is authoritative after sign-in; a separate browser-local preference is authoritative while anonymous and is restored on sign-out. Quantity-bearing requests keep an explicit unit and the backend normalizes supported input exactly once before calculations or persistence. Do not rewrite or reinterpret existing stored values.
- **PLANNED (Tasks 297-298, accepted 2026-07-29; owner: product/privacy/backend/frontend maintainers):** extend Daily Diets to support owner-scoped private custom food items. A referenced custom item cannot be deleted; the UI identifies the owner's affected diets and requires removal of those references first. After references are removed, Delete permanently removes the item and its classification links, permits immediate name reuse, and provides no Trash or Restore workflow. Lists and Account Export exclude the item immediately. Only a payload-free, time-bounded retry marker may remain to prevent a delayed create replay; account erasure removes it immediately.
- **PLANNED (Task 299, accepted 2026-07-29; owner: architecture/backend/security maintainers):** private custom-item and administrator manual-item requests may set only `manual` or `estimated` density provenance. Only the server import workflow may set `imported`, using provider identity and external food ID derived from its server-controlled external record. Separate the request types, keep provider identities in one external-data registry, and record the authenticated administrator in the audit event rather than density provenance.
- **PLANNED (Tasks 300-301, accepted 2026-07-29; owner: backend maintainers):** move the two remaining non-trivial inline Redis Lua programs—classification generation and optimization state transition—into colocated embedded `.lua` files. Use hash-first execution with safe `NOSCRIPT` fallback and preserve all existing inputs, outputs, atomic behavior, TTLs, errors, and test seams. The repository-wide audit found no other remaining inline Redis Lua programs.
- **PLANNED (Task 302, accepted 2026-07-29; owner: Phase 08 acceptance maintainers):** after Tasks 287-301 are complete, rerun the affected isolated real-stack acceptance scenarios, synchronize the append-only finding ledger, and refresh the Phase 08.02 report and UAT. Close findings only with mapped passing evidence. Preserve the accepted historical out-of-scope disposition and keep the centralized-logging work in Phase 09.
- **IMPLEMENTED (Task 264, verified 2026-07-25; owner: Phase 08 backend maintainer):** manual global-item create, update, and soft-delete now advance the existing shared Redis food-data generation exactly once after a successful audited commit. Replays and validation, conflict, transaction, or audit failures do not invalidate. Focused controller/app tests, live PostgreSQL/Redis cross-instance Catalog and Substitution Search evidence, the full race suite, and the aggregate gate pass.
- **IMPLEMENTED (Task 265, verified 2026-07-25; owner: Phase 08 backend/security maintainer):** authenticated custom-item create and update reuse `rejectDuplicateJSONKeys` before decoding. Recursive duplicate top-level, macro, and micronutrient keys return the safe `400 invalid_json` envelope without service or persistence dispatch; valid, ownership, CSRF, and idempotent paths remain passing in focused HTTP, security, race, and aggregate gates.
- **IMPLEMENTED (Task 266, verified 2026-07-25; owner: Phase 08 frontend maintainer):** curated-import attempts now have monotonic ownership with immutable draft/idempotency-key snapshots. Superseded success, conflict, ambiguity, and failure completions cannot overwrite active state, while incompatible controls are disabled during import. Component tests and the full desktop/mobile Playwright/axe matrix pass.
- **IMPLEMENTED (Task 266, verified 2026-07-25; owner: Phase 08 frontend maintainer):** a new external-provider search now requires explicit Keep editing or Discard draft and search handling for an unsaved draft. Confirmed discard clears curation/import ownership and state before searching; cancel preserves the draft; completed workflows reset without a needless warning. Component and browser regression paths pass.
- **IMPLEMENTED (Task 267, verified 2026-07-25; owner: Phase 08 frontend maintainer):** Account Export refresh now fails closed by clearing stale success, pending deletion, private items, and destructive controls during loading or failure. Accepted deletion followed by refresh failure reports verification-required state, and retry restores only current owner-free data. Component and browser regressions pass.
- **DEFERRED (Task 274, 2026-07-25; owner: Phase 08 documentation maintainer; target: Task 275):** authoritative Task 263 remains `PASSED`, while current Phase 08 acceptance remains pending the dedicated Task 275 UAT/evidence refresh. Task 274 has refreshed the aggregate report/screenshots and verified every remediation; Task 275 owns extending `08_PHASE_UAT.md` through Tasks 264-275 and the project-owner acceptance decision.
- **IMPLEMENTED (Task 268, verified 2026-07-25; owner: Phase 08 frontend maintainer):** the dedicated `--color-on-error` foreground is used for filled destructive confirmation and measures `4.83:1` in light mode and `6.41:1` in dark mode. Focused contrast assertions and the full Playwright/axe accessibility matrix pass.
- **IMPLEMENTED (Task 268, verified 2026-07-25; owner: Phase 08 frontend maintainer):** every Phase 08 administration button in the three reviewed components carries `transition-all duration-200 motion-reduce:transition-none`. Source/component assertions and reduced-motion browser verification pass without introducing a new Button abstraction.
- **IMPLEMENTED (Task 268, verified 2026-07-25; owner: Phase 08 frontend maintainer):** reviewed administration inputs, selects, and textareas use the theme Surface, one-pixel Border, and visible two-pixel Primary focus ring in both themes. Component, keyboard, responsive, and axe checks pass.
- **IMPLEMENTED (Task 268, verified 2026-07-25; owner: Phase 08 frontend maintainer):** administration heading elements use Bold `700` while labels, legends, buttons, statuses, and body emphasis retain their semantic weights. Focused source/component assertions and the production build pass.
- **IMPLEMENTED (Task 269, verified 2026-07-25; owner: Phase 08 backend maintainer):** every exported Phase 08 constant and sentinel error in `externaldata`, `dataimporter`, `itemcurator`, `useradmin`, and `observability` has concise identifier-led Go Doc with adjacent design traceability. The Go Doc validator, `gofmt`, focused/full tests, race, and `go vet ./...` pass without public API changes.
- **IMPLEMENTED (Task 270, verified 2026-07-25; owner: Phase 08 frontend maintainer):** hand-written Phase 08 frontend exports have concise TSDoc, and generated administration contracts receive preserved descriptions from OpenAPI/generator sources. The focused TSDoc validator, all 24 generator tests, generated-type drift check, typecheck, unit tests, and production build pass.
- **CLOSED (2026-07-24; owner: Phase 08 documentation maintainer):** removed the four trailing spaces from the blank line formerly at `docs/implementation/reviews/task-244-review.md:292`. This was mechanical hygiene with no runtime or product impact. The review file postdates the historical 2026-07-22 Task 262 gate, so the UAT's earlier PASS evidence is retained as historical rather than characterized as false; final Phase 08 acceptance still requires a fresh aggregate run after all review remediation.
- **CLOSED (2026-07-24; owner: Phase 08 API contract maintainer):** aligned `scripts/test_generate_api_types.py` with the authoritative cosine-similarity contract in SW-REQ-016, DESIGN-003, DESIGN-004, backend projection, and `api/openapi.yaml`. The test now requires the current cosine description, rejects the obsolete `Quantity-weighted Jaccard similarity` wording, and retains the `0..1`/four-decimal contract assertions; OpenAPI and runtime behavior were not changed to satisfy the stale phrase. Evidence: all `24/24` generator tests pass, `bun run check:api-types` reports generated types current, and `git diff --check` passes.
- **CLOSED (Task 262, 2026-07-22; owner: Phase 08 backend maintainer):** Tasks 238-240 added migrations `000025`/`000026`, mandatory owner-scoped custom-item repositories and routes, idempotent creation, export inclusion, deletion lockout/cleanup/cache purge, and owner/global isolation. Repository, HTTP, export, erasure, aggregate, and race suites pass.
- **CLOSED (Task 262, 2026-07-22; owner: Phase 08 API/frontend maintainer):** Tasks 241, 251, 253, and 257 added persisted classification/allergen vocabularies, backend dietary/physical-state policy, `/api/v1/search/filter-options`, generated frontend contracts, cache invalidation, localized-label-ready ordering, stale-request protection, and safe degraded UI. Source assertions, unit/component/browser tests, type drift, aggregate, and race suites pass.

## Phase 09

### Assumptions

- Accepted product decision for Task 191 on 2026-07-07: the frontend OAuth provider button label remains `Google`, not `Continue with Google`. The task expectation was updated to verify the accepted `Google` label while preserving the Google-only/Apple-disabled validation discipline.

### Actions needed

- **OPEN DECISION (carried from Phase 01; owner: Phase 09 product/data owner):** review production search-history usability and either retain duplicate completed searches as the accepted behavior or define duplicate-collapse semantics, ordering, retention, and clear-history compatibility. Do not add collapsing without evidence that duplicates make the history UI materially noisy.
- **OPEN (carried from Phase 02; owner: Phase 09 platform maintainer):** provision and verify deployed GCP Cloud Monitoring log ingestion, metrics, uptime checks, alert notification channels, dashboards, and backup monitoring. Preserve the locally verified metric names, probe cadence, retention, and P95 warning/critical thresholds.
- **OPEN (carried from Phase 08 finding `P08-FIND-285-001`; owner: Phase 09 platform/observability maintainer):** provide an approved deployed test origin, bounded GCP Cloud Logging scope, least-privilege read-only acceptance identity, complete ingestion window, and retention evidence for the existing Task 285 verifier. Local console output is not acceptance evidence.
- **OPEN (carried from Phase 08 finding `P08-FIND-285-002`; owner: Phase 09 platform/E2E/observability maintainer):** provide safe deployed acceptance fixtures that generate and clean up the required authentication, external search/import, manual-item, classification, user-administration, validation, dependency, and audit-failure actions through production UI/API boundaries. Do not expose a public fault-injection API or allow acceptance fixtures outside the isolated test environment.
- **OPEN (carried from Phase 08 finding `P08-FIND-285-003`; owner: Phase 09 platform/observability maintainer):** verify and repair independently queried centralized events that are missing, duplicated, malformed, inconsistently timestamped, outside the fixed action/resource/outcome/request-ID vocabulary, or contain forbidden private data. Evaluate this only after the deployed logging environment exists.
- **OPEN (carried from Phase 02/03; owner: Phase 09 security/platform maintainer):** complete GCP Secret Manager runtime wiring and multi-version key rotation support for production encryption keys and service credentials. Verify active/historical key lookup, rotation grace periods, rollback, missing-version failure, least-privilege access, and absence of committed secrets.
- **OPEN (carried from Phase 03; owner: Phase 09 auth maintainer):** implement signed, single-use email-verification tokens and outbound email delivery before production paid-feature unlocks rely on email/password verification. Cover expiry, replay rejection, abuse controls, provider failure, retry behavior, and safe observability.
- **OPEN (carried from Phase 06.01; owner: Phase 09 auth/platform maintainer):** add production Google and Apple OAuth gateway configuration and reshape `config.OAuthConfig` into provider-specific nested configuration. Verify Secret Manager credentials, deployed callback URLs, redirect allowlists, provider-specific failures, and fail-closed incomplete configuration.
- **OPEN (carried from Phase 07; owner: Phase 09 backend/platform maintainer):** integrate optimization cancellation and Redis-key invalidation into account deletion for job/result records, retained owner markers, attempt counters, active reservations, rate counters where policy requires removal, and enqueue state. Deleting an account must make outstanding jobs unreadable, prevent later worker publication, remove user-scoped optimization keys, and pass account-deletion retry/failure tests.
- **OPEN DECISION (carried from Phase 07; owner: Phase 09 product/architecture owner):** evaluate production polling evidence and either close the optional optimization WebSocket notification path as unnecessary or plan and implement its authenticated user-scoped contract, delivery, reconnection, expiry, and browser tests. Bounded polling remains the required completion mechanism until this decision is made.
- **OPEN DECISION (carried from Phase 07; owner: Phase 09 release/platform maintainer):** confirm `linux/amd64` as the production optimizer architecture or provision a checksum-pinned, tested ARM64 source-build path for CLP `1.17.11`. The current official Ubuntu artifact and verified optimizer image support amd64 only; do not claim multi-architecture deployment before this decision and focused image verification.

## Phase 10

### Assumptions

- **ACCEPTED DIRECTION (2026-07-20):** replace the continuous-only CLP model with mixed-integer Daily Diet Alternative generation before standalone macro-target generation. Use an external COIN-OR CBC process unless implementation validation records a concrete blocker; preserve the existing worker queue, polling, deadline, bounded-output, temporary-file, and validation boundaries.
- **ACCEPTED DIRECTION (2026-07-20):** replace Phase 07's calorie-first objective with lexicographic target closeness. Minimize normalized absolute deviation from the authoritative Protein, Carbohydrate, and Fat targets first; use calories only as the next tie-breaker and diversity after that. The requested tolerance defines the feasible band and must not make its lower boundary the optimizer's preferred destination.
- **ACCEPTED DIRECTION (2026-07-20):** CBC lexicographic stages must use tolerance-aware prior-objective locks. A parsed or rounded floating-point optimum must never be copied into an exact equality constraint; the approved bound must absorb solver/publication precision without permitting a later objective to materially worsen an earlier one.

### Actions needed

- **OPEN DECISION (2026-07-20; owner: Phase 10 product/optimization owner):** define the minimum and maximum selected-item counts and the minimum meaningful solid/liquid portions used to link binary selection variables to `g`/`ml` quantities. The policy must prevent negligible filler quantities, remain understandable in the UI, and document whether future item-specific serving bounds supersede the initial global bounds.
- **OPEN (2026-07-20; owner: Phase 10 backend/platform maintainer):** select and checksum-pin a supported CBC version and artifact for local development plus the production optimizer architecture; generalize the CLP-specific wrapper/config/readiness/verification surfaces while retaining process isolation, bounded output, cancellation, and the whole-job deadline.
- **OPEN (2026-07-20; owner: Phase 10 requirements/design maintainer):** align SW-REQ-022 and DESIGN-004 with target closeness as the primary objective, calories as the secondary tie-breaker, and diversity as the later preference. Define the normalized absolute-deviation formulation and deterministic tie behavior before changing the solver model; verification must prove that a closer feasible target match beats a lower-calorie solution on the tolerance boundary.
- **OPEN (2026-07-20; owner: Phase 10 backend/optimization maintainer):** define one scale-aware CBC objective-lock tolerance from solver output precision, coefficient scale, and published quantity precision, and apply it consistently between target-closeness, calorie, and diversity stages. Regression coverage must replay the Phase 07 failure shape—an initially feasible solve followed by a tiny residual after objective locking—and prove the CBC workflow remains feasible, preserves earlier objectives within the documented bound, and does not emit `solver_infeasible`.

## Phase 11

### Actions needed

- **OPEN (carried from Phase 07; renumbered 2026-07-20; owner: Phase 11 product/API/frontend maintainer):** implement the explicitly separate standalone macro-target diet-generation workflow. Define accepted Protein, Carbohydrate, and Fat input policy, meal-count/quantity bounds, filters, exclusions, diversity, entitlement, and save behavior; add a discriminated OpenAPI request variant that requires `targetMacros` and rejects `dailyDietId`; and cover worker reuse plus generated frontend/browser/UAT evidence.
- **OPEN (2026-07-12; renumbered 2026-07-20; owner: Phase 11 backend maintainer):** remove duplicated internal calorie state from optimization alternatives. Mealswapp derives calories exclusively as `protein*4 + carbohydrates*4 + fat*9`; replace the retained `DietAlternative.Calories` field with one canonical nutrition-projection calculation while preserving `macros.calories` in the public API. Account for persisted Redis job compatibility and update validator, worker serialization, design interfaces, and focused ordering/result tests before closing this action.
